# The Program — one queue for the whole game (2026-09-07)

This is the one document. It merges `docs/inny-poziom-program.md` (the second pass: its
diagnosis, its registers, its 91-row queue) with the audits of 2026-09-06 and 2026-09-07 —
collision, driving, the running gear, destruction, ballistics, terrain, and the owner's
graphics review (`output/design-review`, VR-01…VR-09) — and with every decision the owner
took in those two days. From here on there is one queue, one register per lane, and one rule
for closing a row: **a row closes with a number or a frame, never with a sentence.**

Authority: the owner delegated the open decisions on 2026-09-07 („wszystko, co jest do
podjęcia moją decyzją — podejmuj Ty”). Every decision below is dated, names its owner's words
where they exist, and either cites a chapter of `docs/game-design.md` or adds a dated row to
that document's reconciliation table (rows 25–35 were added with this document). Where the
table and the text disagree, the table wins.

Where the old texts live: `docs/inny-poziom-program.md` (the diagnosis, the closed rows and
the full text of every open row quoted here by id), `docs/art-direction-program.md` (the D
register, closed rows and D1–D33 in full), `docs/contact-and-tracks-program.md` (the tracks
program, its measurements and the rollover arithmetic), `docs/interface-program.md` (closed;
its policy is `docs/interface-policy.md`), `docs/game-modes.md` (the M lane, its own document
by the owner's decision). Those files are history and reference; the queue is here.

## 1. Decisions of 2026-09-06 and 2026-09-07

Each entry: the decision, the owner's words where they were the decision, the reconciliation
row it adds (`GDD row N`), and the lane that carries it.

1. **Collision is honest 2.5D, not full 3D and not Rapier** (the owner, 2026-09-06: „Idziemy w
   2,5D”). Hull and cover become upright boxes with a yaw and a height band; two upright boxes
   overlap iff their XZ footprints overlap (today's SAT) AND their y-intervals overlap — exact
   algebra for upright boxes. A solid no taller than the vehicle's step height enters the
   support envelope like rubble does today; a taller one blocks in plan where the bands
   overlap. Cover becomes an immovable body in the roster solver (the veto path goes).
   Outside the model, deliberately: hull-on-hull stacking, rollover from contact, passing
   under solids, the gun as a body, windows as holes in the collision proxy. GDD row 25. Lane X.
2. **No rollover, full stop** (the owner, 2026-09-07: „bez przewrotek czołgów”). The gate
   `rollover_unreachable.rs` stays; the landing roll excursion is a sway, not a flip. GDD §4
   stands (suspension and sway), reconciled in row 25.
3. **Climbing is skill and positions are never taken away** (the owner, 2026-09-07, on WoT
   removing the Himmelsdorf and El Halluf climbs: „zabierali graczom rozrywkę i wiedzę/skilla”).
   §3.3 „Jeśli fizyka pozwala wjechać na skałę, wjeżdżasz” stands literally; the map report
   must KNOW the climbs (the passable graph with the step height), never remove them. GDD row 26.
4. **P4.7 (terrain throws a track) is rejected** („odpada”). Not proposed again.
5. **Driving comes first** (the owner, 2026-09-07: „ciężko go przewidzieć i trudno jest szybko
   wychamować, stanąć i wycelować w wroga; w World of Tanks jest inaczej”). S brakes; the brake
   is traction-limited (7.2 m/s² on grass, worse in mud and water); releasing W engine-brakes;
   the steer spool is halved; **the dive share of the hull pitch is stabilised out of every
   gun** (the owner's choice, 2026-09-07: „stabilizować udział nurka dla każdego działa jak
   w WoT”) while the Centurion keeps its historical terrain stabiliser; **a gearbox as data**
   (3–4 gears, a flat torque curve, shift points — §4's „krzywa momentu × przełożenie” adopted
   as data, replacing the 2026-08-06 „no gearbox” decision). GDD rows 27 and 28. Lane J.
6. **The running gear is honest before it is dense.** The top run rides its carriers' live
   height, the wheels fit the physics support line with heave subtracted and one symmetric
   clamp, the horn and plate seat derive from the wheel's radii. Zero triangles. Lane G (G1, K10, J7).
7. **Buildings before their collision** (the owner, 2026-09-07). The building kit (B1–B6)
   defines the box data (yaw, height, segments); the sim consumes it in lane X afterwards.
8. **Destruction falls, it does not swap** (the owner, 2026-09-07: „bez sensu … drzewo nie
   upada tylko jakby znika i pojawiają się jakieś boxy. Tak samo jest z budynkami”). Trees
   topple with a direction byte, wall segments fall as rigid pieces inside the dust, a ruin
   keeps two walls and a floor. **Destruction classes are a data axis separate from the kind**
   (the owner's taxonomy: fences, boxes, signs, trees, walls, houses, terrain, mountains, big
   buildings): Immovable / Stateful-per-segment / Breach / Topple / Crush / Prop / Terrain.
   Havok is the lesson, not the plan (Wargaming announced full building physics in 2014 and
   shipped 9.14's driving physics in 2016; buildings stayed authored states under dust). GDD row 29.
   Lane Z.
9. **Destroyed terrain is „beznadziejny”** (the owner, 2026-09-07). Craters are the one honest
   mechanic (ledger, 0.625 m patch geometry); everything else is a decal that fades. Lane T
   (T7–T9): scorch as a splat with material, spoil and clods, ruts with memory in soft ground,
   furrows as displacement in the patch, and the per-HE whole-mesh re-upload (Q1) gone.
10. **No camouflage stat; bushes hide** (the owner, 2026-09-07: „kamuflażów ja nie chcę,
    dokument projektowy kłamie. Oczywiście czołgi muszą mieć swój zasięg widzenia, wykrywanie
    itd, także zwiadowca ma swoją rolę”; and: „krzaki mają chować”). V2 and the concealment
    stat of R1 are out; V1 (optical density of foliage on the sight line) is in; view range per
    vehicle from the dossier stays; scenery still never blocks a shell. GDD row 30. Lane V.
11. **Ballistics** (the audit of 2026-09-07, 7/10): air drag STAYS and the design text is
    reconciled (GDD row 31); HE direct-hit damage becomes a function of plate thickness at the
    point (§3.1 implemented); the dispersion seed gets a per-battle salt before the first public
    match; the casemate gets a limited traverse arc; the aim time is documented as three
    e-folds and the bloom as additive (GDD row 32); `r·u²` stays (row 5); the glance-band loss
    is shown in the reticle; splash is occluded by wrecks; the non-penetrating module lump goes
    through the wound scale. Lane S.
12. **Ramming is billed from the peak closing speed per contact, and both hulls pay, weighted
    by mass** (GDD row 33 amends §4's „boli tylko średniego”: a free weapon for heavies is a
    bot-spam vector at 15v15). Lane X.
13. **Wrecks can be pushed** (one flag and a parking friction); **cover is a body** (no
    momentum is deleted at a wall). Lane X.
14. **Windows are not apertures for sight** (GDD row 34 amends §13.4): a shell pays the wall's
    thickness (a window is thin wood in the same table), the sight line is blocked. Shooting
    through windows at 20 Hz snapshots is „trafił mnie przez ścianę” for the victim.
15. **The terrain grid goes to 2.5 m map-wide, with chunk LOD and geomorphing, gated by the
    map-swap and frame measurements T1 asks for.** Tank-scale relief stays analytic through
    `sample_height` (craters today; ruts and authored micro-strokes next), never a denser
    raster. 1 m map-wide and the GDD's 0.5 m are refused for the same bake reason. Lane T (T1).
16. **Maps are gameplay topology first** (the owner, 2026-09-07: „lanes, crossfire, cover,
    hull-down, flanking, sniper positions, rotation paths, fallback positions”). The roles
    become data (append-only) and the map report computes them from geometry. GDD row 35. Lane W.
17. **The picture**, from the graphics review verified in code (2026-09-07): the low sun owns
    the field (profile data), shadow strength per look, cloud shade with depth, a second
    canonical view INTO the sun; leaf sprites re-baked into the world's albedo window; the
    vehicle material chain becomes a real response per role with mips, a macro octave, edge
    wear and a dirt band (K6 — K24's colour is done, the materials are not); the ground gets
    form (dirt darker than grass, plots with edges, clumps with occupancy, the hedge body as
    foliage); the garage hero pivots 1.0 m forward. **Every one of these PRs rewrites the lying
    lock in the same PR** (O2). Lane D (D34–D45).
18. **Interface rows route to the interface session** (`docs/interface-policy.md` ownership):
    labels leave the stencil for Plex SemiBold 18 u (a dated policy row replaces
    `interface-policy.md:92`); texture never competes with text (enamel under any plate that
    carries text ≤ 12 u); the reticle numbers get their 3:1 (already the policy's promise);
    the hit log gets a compact default; the range prints once; the zoom label is burned.
    No HUD or garage overlay file is touched from any other lane. Lane U (U12–U17).
19. **The representative fragment** (the review's own next step): Kamienna on Bystra, the
    T-54 (the benchmark is unchanged — the owner withdrew the Tiger I proposal), ~10 s: out
    from behind the parapet, hull-down on the bridgehead bluff, aim into the market lane at a
    Tiger, shot, wall breach, drive through the breach. The one sequence that passes through
    every lane above. It is the acceptance frame of blocks 1–4, not a block of its own.
20. **The one thing only the owner can do:** N9 needs `wsl --install -d Ubuntu` on the owner's
    machine. Everything else in this document is delegated.
21. Reaffirmed: no physics engine (rapier's `enhanced-determinism` is same-binary only);
    `parry3d` and `legacy_boxes.rs` are deleted (zero callers, a dead kernel with a real bug);
    weather is presentation only (row 9); the scope is rigid (row 6); zero aim assist.

## 2. The queue

**Priority update, 2026-09-09 (GDD row 36):** the owner reports FPS degrading through the
battle until aiming becomes impossible. Q11 attribution and the proven fixes from Q12–Q14
take priority over further visual expansion; Q15 owns full-battle acceptance. Q16 (120+ FPS
on stronger PCs) follows the stable MX330 floor. This does not reopen completed instrumentation
or change N9's separate second-OS dependency. Work and evidence: [sustained-performance plan](sustained-performance-plan.md).

Throughput assumption unchanged from the second pass: the gate on one laptop is the ceiling
(10–15 min per PR gate, 25–30 min the full one), not the writing of code. Estimates are
continuous sessions. Blocks are ordered by what the player sees in the first minute and by
dependency; a block is done when every row in it is closed.

| Order | Block | Rows | Estimate |
|---|---|---|---|
| 0 | **Housekeeping, any time, between gates** | X9 (`parry3d` out), S22 (`legacy_boxes` out), D17 close, S21 (bible tint = code), S19/S20 (reticle Clear-on-ground, detached turret in `trace_sets`), Z4, S6, A2 | 0.5 day |
| 1 | **Jazda** | J1 → J2 → J3 → J4 → J5 → J6, then G1 → J7 (the running gear), K10; each PR with the stop-and-aim lock and the owner at the wheel | 2–3 days |
| ~~2~~ | ~~**Światło i materiały, zero triangles**~~ DONE 2026-09-07 (#817–#829) | D34 → D35 → D36 → D37 (light); D38 (leaf sprites); D39 → D40 → D41 → D42 (vehicle material = K6's first half); D43 → D44 (ground form); D45 (garage pivot, turntable, crew strip); O2 rewritten with each | 2–3 days |
| ~~3~~ | ~~**Budynki i destrukcja (Destruction 2.0)**~~ DONE 2026-09-07 (#830–#844; raport `output/raport-blok3-2026-09-07.md`) | B3 → B1 → B2 → B4 → B5 → B6 → B7; Z8 → Z9 → Z10 → Z11 → Z12 → Z13; T7 → T8 → T9; V0; Z7; Q1 | 7–10 days |
| ~~4~~ | ~~**Kolizje 2,5D**~~ DONE 2026-09-07 (#846–#855; raport `output/raport-blok4-2026-09-07.md`) | X1 → X2 (one district of Kamienna at an angle) → X3 → X4 → X5 → X6 → X7 → X8; X10, X11 — all closed in one day, no wire change | 3–4 days |
| ~~5~~ | ~~**Balistyka**~~ DONE 2026-09-08 (#857–#861; raport `output/raport-blok5-2026-09-08.md`) | S14 → S15 → S16 → S17 → S18; S23 — all closed, no wire change; N9 (the owner's WSL) stays the other half of "before the first public match" | 2 days |
| 6 | **Widoczność** | V1 (bushes hide) → V3 → V4; R1 as amended | 2 days |
| 7 | **Teren** | T1 (2.5 m + LOD + geomorph, measured) → T2 → T4 → T5; C4 after T1 | 4–6 days |
| 8 | **Determinizm i klatka** | ~~Q7~~ ~~Q2~~ ~~Q4~~ DONE 2026-09-08 (#867 Q2, #870 Q7, Q4 `fix/q4-audio-mailbox`; with them #866 N12, #868/#869 Q8, #871 Q9b). **OPEN: Q11–Q15 sustained 60 FPS (priority update above); Q16 stronger-PC scaling.** N9 remains separate, after the owner's second OS | Estimate after Q11 attribution |
| 9 | **Kamera** | C1 → C2 → C3 (after G7b) → C4 | 2 days |
| 10 | **Mapy jako topologia** | W1 → W2 → W3 → W4 | 3 days |
| 11 | **Forge 2.0 (W3 of the second pass)** | K0 block → K3 → K9, K11–K23 → K4/K5 → K6's second half → P1–P3 | 10–15 days |
| 12 | **Interface session** | U12 → U13 → U14 → U15 → U16 → U17, then U1–U11 as that program orders them | its own program |
| 13 | **The rest** | G2–G6, G7b, R2, R3, H1–H6, N1–N8, N11, F8–F11, O1, L1–L3, M5b/M7b/M8 (`docs/game-modes.md`) | as the second pass estimated |

Dependencies that decide the order: G1 before any support-from-solids (X4) and before J7 —
otherwise a fourth ground sampler; G7b makes the step-over (X4) readable; B-rows before X1
(the kit defines the box data); V0 before V1; T1 before C4 and before T2/T4 (cliffs make the
height band X3 matter); N9 before the first public match (with S16); the representative
fragment (decision 19) is rendered after block 4 and judged by the owner playing it.

Machine facts that no agent changes: the cold measurement needs the box idle and rested to
≤ 60 °C; two worktrees on one target dir hand each other's binaries around; HUD and garage
goldens belong to the interface session; a blueprint/armour change gates with `-Crates
...,client,sim`; a wire enum append bumps `PROTOCOL_VERSION`; every replay a sim number moves
is re-pinned in the same PR and says why.

## 3. Registers

New rows carry their full text. Rows inherited from the second pass keep their id and one
line; their full text, evidence and "closes when" stay in `docs/inny-poziom-program.md`
(or the register named). Each new row: defect · evidence · closes when.

### X — collision (2.5D)

| ID | Defect | Evidence | Closes when |
|---|---|---|---|
| ~~X1~~ | ~~**Every solid is an axis-aligned box; towns are grids.**~~ **CLOSED (2026-09-07, `feat/x1-one-cover-box`).** `StaticCoverObject.yaw_rad` (`serde(default)`, so every blueprint and fixture reads as before) and ONE `terrain::CoverBox` (`of/to_local/to_world/corners_xz/bounds_xz/contains_xz/segment_interval/xz_disjoint_from_segment/clamp_to_surface`) read by every consumer: the SAT (`cover_obstacle` passes the yaw), the sight slab and the shell slab (`segment_hits_cover`), the shell contact (`first_cover_impact` in the box's frame), the splash, the scar (`struck_face`/`record_cover_scar` local), the opened building's slabs (turned with the box), the rubble mound's height, the camera boom (`CameraObstacle.yaw_rad`), the minimap (a turned quad), the bake (`turn_baked_with_box` — positions and normals), the kit (`Mat4::from_rotation_y` between the centre and the plan; party walls only between boxes turned alike), the tree line's stations, the hosted scenery, the statics buckets, the atlas raster (per-pixel `contains_xz` inside the turned bounds), the editor (pick in the box's frame, the outline turned, the inspector's yaw line), the map report (bounds, overlap in the shared frame, zone distance, coverage, the mirror twin modulo π) and the golden (yaw hashed only when nonzero — every golden unchanged). The yaw = 0 path branches to the arithmetic it had, bit for bit: the replay locks (`replay_regression`, `combat_replay_regression`, `perforation_replay`) and the 7v7 hashes are the proof; the wire is untouched (the yaw is map data, not a snapshot field). Locks: `the_axis_aligned_box_is_the_old_arithmetic_bit_for_bit`, `a_yawed_box_is_the_same_box_turned` (terrain); `a_yawed_wall_blocks_where_it_stands_not_where_its_bounds_do` (physics); `a_yawed_box_blocks_the_eye_and_the_shell_where_it_stands` (sim); `a_turned_box_bakes_turned_with_it` (scene_build); `the_boom_meets_a_turned_plate_where_it_stands` (client). NOT DONE: no map carries a yaw yet (X2); the bot's route planner reads boxes through the sim's sight and the physics' SAT only | `terrain/src/cover_box.rs`, `physics/src/cover.rs`, `sim/src/{spotting,cover_damage,shell_splash}.rs`, `sim/src/shell_trace/cover.rs`, `scene_build/src/{battlefield,building_kit,tree_line,tree_lod}.rs`, `client/src/camera/types.rs`, `tools/src/atlas.rs`, `editor/src/{pick,markers,objects}.rs`, `map_forge/src/{report,golden,blueprint,compile}.rs` | done |
| ~~X2~~ | ~~**One district of Kamienna at an angle**~~ **CLOSED (2026-09-07, `feat/x2-kamienna-turned-district`).** `TownGrid.yaw_rad` (`serde(default)`): the grid is ONE district turned about its own centre (`town_grid_frame` / `TownGridFrame::place` — the mean column, the mean row; at yaw 0 the position untouched), every house and annex wearing the yaw and the twin the twin's (`SymmetrySpec::twin_yaw`; the report's mirror-twin check reads the twin's yaw modulo π). Kamienna is two districts: the west column square on the town street (`town_house`), the three east columns `town_house_east` turned 0.17 rad (10°) — the cells seeded by (column x, row offset) as before, so the houses are the houses they were. The scatter's containment (`covers_containing`) reads the box, so no tree stands in a turned house; the kit's doctrine test measures parts in the box's frame. Bystra golden re-blessed (`4fc9100378b2efb5`); the four Bystra look frames re-recorded (0.14–0.64 % of pixels — the turned district in the lane view). The district is on the fragment (Kamienna is the fragment). Lock: `kamiennas_east_district_is_turned_as_one` (yaws, the centre, a far house off its square spot, no scenery inside). NOT DONE: the streets between the turned houses are still the square town street and the back lane (a turned district wants its own lane — a `Road` at the district's angle is a blueprint line, owed with B7's kerbs); Ostrogorsk's 48-block grid stays square | `map_forge/src/{blueprint,compile,report}.rs`, `map_forge/blueprints/bystra-valley.map.ron`, `terrain/src/scenery.rs`, `map_forge/tests/suite/bystra_map.rs` | done |
| ~~X3~~ | ~~**Height does not exist in contact.**~~ **CLOSED (2026-09-07, `feat/x3-height-in-contact`).** Contact is the XZ SAT ∧ an overlap of height bands (`physics::HeightBand`, `TankObstacle.band`): a hull's from its support height (`center.y`) up `HullPlan::height_m` — NEW on the plan, the shell volume's top (`HitboxProfile::center_y_m + half_height_m`, T-54 2.53 m), carried by `TankFootprint::height_m`; a standing solid's from below every support up to its top (`TankObstacle::grounded_solid`, `bottom = −∞`): the forge grounds every box by its centre, so on a slope the box's bottom face floats over its downhill corners while the wall it stands for does not — the TOP decides, a support at or over it passes. `footprint_contact_within` returns no contact when the bands are disjoint and takes the old XZ test expression for expression otherwise: every hull on the same ground has overlapping bands, so the replay locks and the 7v7 hashes hold as they are. Hull–hull (the roster solve, `footprint_penetration_m`) and hull–cover (the blocker, the yaw veto, the crush probe) both read it. Locks: `a_hull_three_metres_above_a_one_metre_wall_passes_and_one_on_the_ground_does_not` (the boundary is the top: 0.99 m blocks, 1.0 m passes; the resolver lets the move through y included), `a_standing_solid_blocks_from_the_ground_up_whatever_its_box_bottom_says`, `a_hull_on_a_mound_does_not_shove_the_hull_below` (the hull on real rubble — a collapsed 8 m house, 3.2 m of debris via `support_height` — over a hull on the ground: no pair, nothing exchanged; the same pair level: one pair, the lower hull shoved), `bands_touching_edge_to_edge_do_not_overlap` (a support exactly at a solid's top is carried, never blocked — X4's rule stated once); `game_core`'s plan lock gained the height. NOT DONE: no solid is a support yet (X4); the shell's and the sight's slabs already read the full box (X1) | `game_core/src/hull_plan.rs`, `physics/src/{collision,cover}.rs`, `physics/tests/suite/height_in_contact.rs`, `docs/vehicle-movement-policy.md` | done |
| ~~X4~~ | ~~**Nothing is climbable but terrain and rubble.**~~ **CLOSED (2026-09-07, `feat/x4-low-solids-as-support`), content and physics in one PR.** THE STEP: `HullPlan::step_m` = `TrackShape::top_y` − `STEP_CLEARANCE_M` (0.10) — the T-54 0.805 m (the dossier's ~0.8 m vertical obstacle, locked within 0.05), the fleet 0.68 (IS-3) to 0.81 m, carried by `TankFootprint::step_m`. THE RULE (`physics::is_step_for`): a solid whose top is no higher than the hull's CURRENT support plus its step is ground — read by two readers off one predicate: the SAT (`TankObstacle::climbing`: the hull's band for cover starts a step above its support, so the solid does not block; hull–hull keeps the full band) and the support envelope (`physics::GroundLayers { rubble, steps }` — the third layer under every station and every probe, `ground::surface_at` = terrain ∨ rubble ∨ the tops of the low solids inside their plans; `step_solids_near` gathers, per hull per tick, the ≤ 8 solids within reach that are steps from where the hull stands, in cover order, on the stack). So the parapet is climbed like a mound: the rigid beam rises onto it, the hull tilts, the probe cross pays the slope. Measured from the current support, so a hull carried up rubble steps onto a wall it could not have climbed from the street. THE TIER: `StaticCoverKind::LowWall` (appended after StoneTower; `Immovable`, never crushed — the hull goes over, not through; a shell stops in it, an eye reads over it; the stone wall's bake, its own atlas tint, the editor's `ParapetRun`). Kamienna's two bridge parapets are the first of it: 0.6 m of `LowWall` (were 1.1 m of RailCover), under the fleet's shortest step; Bystra's golden re-blessed. Wrecks settle onto steps too (`settle_wrecks` gathers them); the client's wheels stand on them (`GroundTruth.cover`). No wire byte: the kind is map data compiled on both sides (the StoneTower append set the precedent). Locks: `a_low_wall_within_the_step_is_crossed_with_a_tilt_and_a_speed_loss` (the T-54 over a 0.6 m parapet a metre thick: past it, peaked over 0.45 m, pitched over 0.05 rad, short of the dirt run by 0.15 m+), `a_wall_taller_than_the_step_still_holds_the_hull_at_its_face` (the 1.6 m churchyard wall: the nose at the face, never lifted), `the_step_is_measured_from_the_current_support_by_both_readers` (a 1.2 m wall: from the street the SAT blocks and the gather is empty; from 0.5 m up the SAT passes and the gather hands it over), the gather and surface unit locks (`ground.rs`), `every_hull_steps_as_high_as_its_belt_line_allows` (game_core), `every_low_wall_on_every_shipped_map_is_a_step_for_every_hull` (sim, per map), the X3 wall lock restated at `top − step`; the replay locks and the 7v7 hashes hold (no hull crosses a low solid in any recording). NOT DONE: G1's "drive slope and attitude slope as one number" stands open (both samplers now read `surface_at`, so the step added no fourth sampler); the map report does not yet KNOW the climbs (W-lane); only Bystra carries the tier — Ostrogorsk's 1.8 m crossing parapets and the field walls stay walls until authored | `game_core/src/hull_plan.rs`, `terrain/src/battlefield.rs`, `physics/src/{ground,collision,cover,track_contact,contact,world}.rs`, `physics/tests/suite/step_over_low_solids.rs`, `sim/src/wreck.rs`, `sim/tests/suite/low_tier_fleet.rs`, `client/src/vehicle/render_frame.rs`, `scene_build/src/battlefield.rs`, `editor/src/objects.rs`, `map_forge/blueprints/{bystra-valley.map.ron,goldens.ron}` | done |
| ~~X5~~ | ~~**Rocks are walls or ghosts.**~~ **CLOSED (2026-09-07, `feat/x5-rocks-boxed`).** The census was wrong by an order of magnitude: the scatters plant 216 erratics on four maps (Orliny 124, Mazurski 60, Bystra 20, Prokhorovka 12; Ostrogorsk none), 0.35–1.45 m × the instance's scale, and EVERY one stood over the 0.40 m belly line as a ghost. Now each earns `StaticCoverKind::Boulder` (appended after LowWall; Immovable, never crushed; bakes NO box — the stone is the scenery pass's, like a hero oak's bole; its own atlas tint; off the minimap, a stone is not a landmark): `map_forge::boulder_box_of` bakes the SAME stone the picture bakes (`world_forge::rock::rock_seed`, one seed for both) and takes its bounds — symmetric about the origin per axis, scaled, at the instance's yaw, from the ground it is set into up to its crest. The belly line is a fleet MEASUREMENT read from one place (`game_core::fleet_belly_line_m`, the T-34-85's 0.40 m; `clutter`'s constant is locked to it). Within a hull's step (X4) a stone is ground it climbs; taller, it is the wall it looks like — and a `Crag` is in the same model by construction (locked: granite from the street, a shelf from a support within its step). FAIRNESS: the map report holds every cover box to its mirror twin, and scattered stones were never fair (a different stone at a random yaw each side) — so a stone's twin is now the SAME stone at the mirrored yaw: `SceneryInstance.seed` (`serde(default)`, hashed only when set — the pair's seed from the original's position bits, `terrain::pair_seed_at`) and `SceneryKind::twins_as_a_solid` (`scatter_mirrored` and the rows give a Rock's twin `twin_yaw`; every other kind keeps A2.1's own yaw). All five goldens re-blessed (the seed is in the hash). Locks: `every_shipped_rock_is_under_the_belly_line_or_boxed_by_its_own_bounds` (map_forge: per map, every stone over the line has its box, every vertex of the drawn stone inside it ± 1 cm, the box from the ground to the crest, no box without a stone, ≥ 200 stones checked), `dressed_by_a_stone` in scene_build's two bake-honesty walks (a Boulder box is the box its stone earns), `a_crag_is_a_wall_from_the_street_and_a_shelf_from_a_support_within_its_step` (physics), the rot180 document keeps its spawn rings clear of stones (a stone in a spawn ring is now the contract error it should be). NOT DONE: Ostrogorsk has no rock (content, not physics); the spotting cost of +124 boxes on Orliny is not measured cold (the owner's cool-machine condition — the sight slab's per-box early-out is two compares) | `terrain/src/{battlefield,scenery}.rs`, `world_forge/src/rock.rs`, `map_forge/src/{compile,golden,report}.rs`, `map_forge/tests/suite/rock_boxes.rs`, `scene_build/src/{battlefield,clutter}.rs`, `client/src/app/minimap_build.rs`, `game_core/src/hull_plan.rs` | done |
| ~~X6~~ | ~~**Cover is a veto, not a body.**~~ **CLOSED (2026-09-07, `feat/x6-cover-as-a-body`).** Every standing solid within reach of any hull is an immovable body in the roster solve: `ContactBody::solid` (planted in the ground — its band from below every support up to its top — and met by the hull's STEP band, `TankObstacle::climbing`: the one predicate the cover blocker reads, so a low solid is ground to the solver as it is to the SAT; two solids never pair), built on both sides of the wire by `physics::solid_bodies_near` (the movement list, cover order, each box once, id = `SOLID_ID_BASE | index` so the impulse cache keys agree between the authority and the predictor). The sim's roster is hulls ++ solids (`exchange_contact_momentum`), the predictor's neighbours gain the solids within the local hull's reach (`LocalPredictor::contact_body`). So a wall hit is a CONTACT: the hull spends its momentum on the wall's impulse before the settle reads the tick's acceleration — the nose dives through the same spring a brake dives it, an off-centre hit turns the hull, and the pair is BILLED (`apply_ramming_damage`: a pair with a solid bills the hull alone, its own mass as the reduced mass, the face it hit with; the wall takes nothing — what a hull does TO cover stays the crush and breach path's, Z12). The veto in the settle is a BACKSTOP now (a spawn inside a wall, the single-hull physics API): in a solved tick it has nothing to refuse — locked. Locks: `a_fourteen_metre_per_second_wall_hit_is_a_contact_that_dives_the_nose` (physics: one pair carrying the charge's momentum, the nose at the face, dive < −0.003 rad, the backstop refused nothing on any tick), `the_solids_within_reach_are_the_covers_own_and_no_more`, `a_wall_hit_bills_the_hull_through_the_contact_pair` (sim: a T-54 charge into a tenement — a ram event on the hull, HP down, the nose at the face), `a_hull_pinned_against_a_wall_holds` (sim: a pusher grinding a parked hull into a wall — the pinned hull moves < 0.10 m over ten seconds, never enters the wall, no shake); hulls_touch / queue_holds / tank_collision / cover_footprint / yaw_collision hold as they are. Replays: the recordings in which a hull leans on a wall re-pinned (the wall now decelerates through the solver) — see the PR. NOT DONE: the wall's own damage from a ram (a breach by momentum is Z12's crush path, unchanged) | `physics/src/{contact_impulse,world}.rs`, `physics/tests/suite/wall_as_a_body.rs`, `sim/src/{state,ramming}.rs`, `sim/tests/suite/wall_contact.rs`, `client/src/{predict.rs,app/prediction.rs}` | done |
| ~~X7~~ | ~~**The ram bill is a hidden die.**~~ **CLOSED (2026-09-07, `feat/x7-ram-bill-is-the-charge`).** The solver reports a COLLISION as a whole (`physics::ContactImpact { a, b, peak_closing_mps }`): per pair and `ContactFeature` the cache carries the fastest the pair closed along the contact normal during the current approach (the closing speed the pair BROUGHT into the tick, before any impulse — `Constraint::closing_mps` at gather), and reports it ONCE when the approach is over — the pair has PRESSED (carried a normal impulse; a pair gathered within the speculative margin that never touched is no collision) and closes no faster than the drive's push (`IMPACT_RESET_MPS`, 0.5 m/s: a hull that keeps its throttle on after the hit brings a hand of fresh closing into every tick, so "no longer closing" cannot mean zero) — or when a pair that pressed separates still closing (a glancing pass); a reported pair that closes again faster than 0.5 m/s is a new collision. Billed at the end of the closing rather than at separation, on the record: a rammer that keeps pushing never separates and would never pay, and a queue would be billed minutes later — the collision is over when the pair stops approaching. `apply_ramming_damage` reads impacts: the base from the reduced mass and the PEAK closing speed on the same curve as before; both hulls pay weighted by mass — `2·m_other/(m_a+m_b)` of the base each (equal masses pay exactly the base, so every T-54-on-T-54 number holds) times the face they met it with; a wall (X6's solid) is the infinite-mass limit, the hull alone against its own mass. Locks: `a_centimetre_of_spawn_distance_moves_the_ram_bill_by_a_centimetre_not_a_die` (sim: the 1 cm sweep 30.00…30.23 m, charger and target each ≤ 5 % spread — the row's number; as run: charger 66–67 HP, target 154–155 HP on every step, where the impulse gave ~268 or ~55), `a_collision_is_billed_once_however_long_the_push_lasts`; `tbone_ram_registers_only_when_hulls_actually_touch`, `a_ram_charges_the_same_bill_whichever_hull_spawned_first`, `a_broadsided_hull_pays_more_than_the_bow_that_charged_it`, `high_speed_ramming_damages_both_tanks_and_running_gear`, `a_gentle_bump_and_a_friendly_ram_deal_nothing` and the wall bill hold. The tolerance rows of the second pass (`tbone_ram_registers_only_when_hulls_actually_touch` +0.5 m) stay as they are — the bill's spread is the thing this row retired | `physics/src/contact_impulse.rs`, `sim/src/{ramming,state}.rs`, `sim/tests/suite/ram_bill_is_the_charge.rs` | done |
| ~~X8~~ | ~~**H2: a hull sinks 0.44 m into a parked neighbour.**~~ **CLOSED (2026-09-07, `feat/x8-contact-manifold`).** The contact acts WHERE the plates meet: `FootprintContact::points` — the incident hull's FACE (the edge from its deepest corner, the SAT's `ContactFeature`, to the neighbour corner lying most nearly along the reference face) CLIPPED to the reference face's span along the tangent, the way two boxes meet; the clipped ends that are pressed (no shallower than the deepest by `MANIFOLD_DEPTH_SLACK_M` 0.05 m) are the points: two for a face flat on a face, one for a corner digging in. Clipping is what keeps a hull nose-on into a ten-metre wall honest: the SAT names the hull's face the reference there, so the WALL is the incident body and its own corners stand ten metres to the side — unclipped, the point sat there, the lever clamped to the hull's reach, the hull spun and the contact took 42 % of the charge. One constraint per point, each with its own warm-start entry (`remembered_feature`: pair + corner), each hull's lever its own tangential offset to the point (the midpoint of the centres is gone: a charger no longer slews as much as its victim; a face on a face is held at two points and cannot turn about one). THE TWO POINTS SOLVED AS ONE BLOCK (`solve_normal_block`): a pair's two points share a normal and couple through the same masses and their levers (`coupling`: the 2×2 effective-mass matrix), so both normal impulses are solved at once as the complementarity problem they are — both press, only the first, only the second, neither, tried in that order (the Box2D block solver) — with full impulses and the friction cone per point; a one-point pair keeps the point solve. This is what the first manifold (2026-08) lacked: solved one point at a time in a Jacobi pass, the front point's impulse freed the rear's and back again, and a body handed both full impulses at once over-corrected — a queue of three sank 0.11 m. Two cheaper schemes were measured on the way and rejected on the record: splitting each body's mass across ALL its contacts (Jacobi mass splitting) oscillates for two symmetric points (each fully stops the body, together they reverse it — the 14 m/s wall charge kept 42 % of its momentum); applying HALF of each point's alone-computed impulse holds the locks but is not exact when the points load unequally, and reshuffled the Bystra bot soak from 2 to 4 losing seeds (H1's chaotic ratchet, which the block solve leaves at 2). Four passes suffice (eight changed no lock). The report merges a pair's points into one `ContactPair` of the APPLIED impulse; the collision's memory (X7) is the pair's, whatever point carries it. THE BACKSTOP YIELDS A HAND (`cover.rs::BACKSTOP_SLOP_M` 0.01): the speculative contact brings a hull to a wall touching it to rounding, and a blocker refusing any overlap past 1e-5 m refused that very move and deleted the momentum the solver left to close it (measured on the 14 m/s wall lock: 3.3 of 10.7 m/s went to the veto) — the blocker now reads every box a centimetre smaller in plan, inside the nose-at-the-face lock's tolerance. H2 RETIRED: `steering_into_a_neighbour.rs` holds every lean under 0.07 m (`TOLERATED_M`: measured 0.060–0.065 m on the three manoeuvres, the same at four and eight passes — the equilibrium of the drive's full push against the recovery velocity, over the 0.02 m slop; was 0.13 and 0.47 m recorded): `steering_into_a_neighbour_never_gets_inside_it`, `leaning_on_a_parked_neighbour_never_gets_inside_it`, `pivoting_against_a_neighbour_never_gets_inside_it`; `queue_holds` (no shake, no sink past the slop), `hulls_touch`, `separation_travels`, the solver's unit locks (a centred hit does not spin, order independence, momentum), the ram locks (X7's sweep, the roster-order lock) and the wall locks (X6's 14 m/s charge now owed the momentum the hull BRINGS to the contact — a coasting hull sheds speed on its own) hold. NOT DONE: the manifold is two points at most (a corner-on-corner pair is one point); the lean's 0.06 m equilibrium is the drive's push against `RECOVERY_RATE`, a knob a later row may tighten with the queue lock as the judge | `physics/src/{collision,contact_impulse,cover}.rs`, `physics/tests/suite/wall_as_a_body.rs`, `sim/tests/suite/steering_into_a_neighbour.rs` | done |
| ~~X9~~ | ~~**`parry3d` compiles for a function with zero callers**~~ **CLOSED (2026-09-07, `chore/x9-parry-out`).** `parry_query.rs` and the dependency deleted; `no_physics_engine_rules.rs` keeps rapier AND parry out of the manifest; `math_backend_parity` no longer lists a crate that is not there | `physics/src/lib.rs`, root `Cargo.toml` | done |
| ~~X10~~ | ~~**Trees are ghosts but the oak.**~~ **CLOSED (2026-09-07, `feat/x10-every-tree-a-trunk`).** Every tree the ladder draws earns a `TreeTrunk` box sized off the very wood mesh it draws: `world_forge::tree::authored::bole_metrics(species, variant)` (cached per species × variant) measures the BUTT radius (the widest wood in the lowest 0.6 m of the mesh), the FIRST LIMB (the lowest wood reaching past the butt's cylinder by `LIMB_LEAVES_M` 0.09 m) and the tip; `map_forge::tree_trunk_box_of` makes the box: the butt radius × the instance scale + a 3 cm skin in plan, from the ground up to the first limb × scale less the ladder's sink (`TRUNK_SINK_M` 0.35, locked equal to `scene_build`'s), yaw 0 — the oak's two literals (0.6 × 5 m from a generator that no longer ships) are gone, and `trunk_species_for` names the oak, the poplar and the fruit tree (a bush is dressing; locked equal to the ladder's `ladder_species` for every tree). ONE SEED on every route: `instance_tree_seed(position)` — the position bits — read by the statics bake, the ladder and the compiler alike, so the bole the compiler boxes is the bole the ladder draws. FAIR TWINS WITHOUT RE-ROLLING A FOREST: a mirrored pair draws two individuals (A2.1's own plant each side) and the map report holds every box to its twin, so a pair's two boxes are ONE box — the wider bole's radius and the lower first limb of the two (found by the pair's `seed`, X5's) — which still holds each bole and ends under each first limb. Measured and rejected on the way: growing both twins from the pair's seed (as X5 does for a stone) re-rolled the variant of nearly every scattered tree on both sides and moved 30 % of Ostrogorsk's pixels in the review frames the owner has looked at. THE ENVELOPE: Ostrogorsk's box count went 115 → 177 with its 62 trunks, past the `urban_150` bench envelope's 160; raised to 180 with a measurement — the sim bench ran the 150-box grid in 17.2 ms and a 180-box grid (`urban_180`, added) in 15.6 ms per 128-tick 14-tank battle, inside the run's noise (the broadphase prefilters were built for this). Census: 308 trees on five maps (oak 134, poplar 114, fruit tree 60; Prokhorovka 60, Bystra 68, Orliny 66, Ostrogorsk 62, Mazurski 52), 308 boxes. All five map goldens re-blessed; the 7v7 deployment hashes re-pinned (a cover object rides the snapshot as its phase byte and wall segments); one review frame re-recorded (`prokhorovka_sniper_contact`, 0.01 % of pixels — the camera boom meets a trunk box). Locks: `every_shipped_tree_stands_in_a_trunk_box_sized_off_its_own_bole` (map_forge: one box per tree, no box without a tree; every bole vertex under the box's top — the chest-height ones the row names among them — inside the box ± 5 cm; the first limb above 0.9 of the box; the box from the ground; ≥ 300 trees checked), `the_trunk_box_and_the_ladder_agree_on_every_tree` (scene_build), `dressed_by_an_oak` generalised to every authored tree in the two bake-honesty walks. NOT DONE: a tree hosted inside a `TreeLine` box keeps its own box at its own scale (the ladder fits a hosted tree under the wall's top — the wall towers over both, so nothing lies, but the hosted bole's box may stand a hand taller than its fitted mesh) | `world_forge/src/tree/authored.rs`, `map_forge/src/compile.rs`, `map_forge/tests/suite/tree_trunk_boxes.rs`, `scene_build/src/{foliage,tree_lod,battlefield}.rs` | done |
| ~~X11~~ | ~~**Rubble has three shapes.**~~ **CLOSED (2026-09-07, `feat/x11-rubble-one-shape`).** ONE shape: the 38° truncated pyramid the hull climbs (`terrain::RubbleMound`) is what the shell stops on and the eye is stopped by, and what the picture draws. `terrain::rubble_segment_impact` / `RubbleMound::segment_impact` walk a segment every 0.125 m inside the footprint and return the first sample under `height_at`, lifted onto the surface (a 900 m/s shell steps ~15 m a tick; the walk is what keeps a flank from being skipped); `sim::line_of_sight(heightmap, cover, rubble, from, to)` and the shell trace (`ShellTraceWorld.rubble`, `first_cover_impact`, the HE splash's `cover_blocks_splash`) read it, the sight-and-shells slice carries NO box for a mound any more (`CoverPurpose::{SightAndShells, Movement, Camera}`; only the camera boom keeps the lowered box — presentation, not honesty), `SimulationState::cached_rubble()` hands the piles to spotting and the shell step, the bots' thread and the client's reticle/sweep/prediction read the same `rubble` (`LiveCoverCache::rubble`). The picture: `scene_build::append_rubble_mound` draws the pyramid from `RubbleMound::from_cover` — the four flanks and the crown at the sim's own surface — with the broken slabs sunk into the talus so no corner stands more than a brick's edge (`RUBBLE_CHUNK_LIP_M` 0.15) proud of it; the old slab at 0.55 of the crest is gone. The Z9 hollow of slabs now also reaches the camera slice (it was lost when the camera stopped riding the sight list). Measured on the way: the felled Orliny tower's pile is 2.03 m (the repose angle over the 2.6 m half-footprint, not the fraction's 2.2 m — the hull always climbed the 2.03 m pyramid; the 2.2 m was the old box) and pointed, so the cross-bench hull line 22 m each side grazed its apex by 3 mm — the hull-down promise is re-measured where a hull-down tank sits, 6 m behind the apex, and the height is locked to the number. Locks: `rubble_one_shape` (sim: a 19 × 11 grid across a tenement's pile, unturned and turned 0.6 rad — a hand under the surface the eye and the shell stop, the shell ON the surface within 0.2 m; a hand over, where no higher part of the pile crosses the run, both pass; ≥ 150 points), `a_building_collapses_to_a_mound_that_is_a_pyramid_and_not_a_box` (the sight slice empty, the flank decides both ways, the camera keeps a box), the scene bake's `a_collapsed_building_slumps_below_its_intact_height` (every vertex ≤ `height_at` + the lip, the talus drawn up to the crest) and `a_turned_ruin_piles_up_in_its_own_frame`; the map locks (Bystra hull-down, Ostrogorsk tenement row, Orliny stump) read the mounds. No wire change (the phase byte is the mound). | `terrain/src/rubble.rs`, `sim/src/{cover_damage.rs,spotting.rs,shell_trace/,shell_step.rs,shell_splash.rs,state.rs}`, `battle_host/src/{bots.rs,bot_aim.rs,bot_combat.rs,local.rs}`, `client/src/{aim.rs,app/{live_cover.rs,reticle.rs},hud/reticle*.rs}`, `scene_build/src/battlefield.rs` | done |
| X12 | **Wrecks are infinite mass**; a dead ally in a chokepoint is a wall forever | `sim/src/state.rs:709` (`movable: hit_points > 0`) | wrecks movable with parking friction; lock: a 36 t hull pushes a 36 t wreck at ≤ 1 m/s |

### J — driving (jazda)

| ID | Defect | Evidence | Closes when |
|---|---|---|---|
| ~~J1~~ | ~~**S is not the brake.**~~ **CLOSED (2026-09-07, `feat/j1-j2-s-brakes-traction`).** An opposing throttle on a rolling hull goes through the brake channel; the speed cap follows the direction of motion while momentum is opposed (no more overspeed governor toward 4.2 m/s); locks: S from 50 km/h stands in < 1.9 s inside 13 m and never snaps across zero; S and Ctrl brake identically | `physics/src/forces.rs`, `tests/suite/movement_model.rs` | done |
| ~~J2~~ | ~~**The brake is a constant, not traction.**~~ **CLOSED (2026-09-07, with J1).** `brake_deceleration_mps2` 4.8 → 7.2 = the grass grip cap, and the demand is `min(brake, mu·g·traction·cosθ)`; the mobility table's `brake_m` column re-recorded (T-54 grass 16.9 → 11.8 m, Tiger II 9.9 → 6.9 m); lock: quarter traction brakes at ≤ 1.8 m/s²; the bot planner's `braking_deceleration_mps2()` is truthful by construction | `controller_settings.rs:209`, `mobility_baseline.rs` | done |
| ~~J3~~ | ~~**Releasing W coasts 45 m.**~~ **CLOSED (2026-09-07, `feat/j3-j5-engine-brake-steer-release`).** `idle_drag_mps2` 1.3 → 2.6 and engine braking applies only when the driver commands nothing (a steer with no throttle is a driven state, so a braked-belt pivot keeps its walk); coast lock band 25–90 → 15–35 m (T-54 ~22 m) | `controller_settings.rs`, `forces.rs`, `movement_model.rs` | done |
| ~~J4~~ | ~~**The gun rides the dive.**~~ **CLOSED (2026-09-07, `feat/j4-dive-share-stabilizer`, wire v53).** The pitch spring's response to weight transfer is integrated as its own state (`dive_pitch_rad`, superposed on the same spring), carried through the sim, the wire and the predictor; `step_aiming` holds the dive share on every gun (`historical·Δpitch + (1−historical)·Δdive`), the Centurion still holds the terrain share; locks: every playable hull dips ≥ 0.003 rad under the brakes and its gun keeps its world elevation within 5e-4; on a 10 % grade the T-54's gun rides the terrain pitch, the Centurion's holds. The presentation spring's pass-through is J8 | `physics/src/hull_attitude.rs`, `sim/src/aiming.rs`, `net/src/lib.rs`, `sim/tests/suite/brake_dip.rs` | done |
| ~~J8~~ | ~~**The drawn barrel lags the shooting barrel.**~~ **CLOSED (2026-09-07, `feat/j8-presentation-passthrough`).** The presented pitch and roll are the replicated attitude to the bit; the fire and hit kicks ride a decaying offset on the same tuned spring; the heave spring stays (G7b); lock: pass-through to the bit, a kick decays back onto the replicated pose | `engine/src/attitude.rs` | done |
| ~~J5~~ | ~~**The heading overshoots 6.7° on every steer release.**~~ **CLOSED (2026-09-07, with J3).** Spool clamp (0.25..0.7) → (0.12..0.35) s and the release ramp runs at twice the spool-up rate (both belts brake against the ground); lock: full lock at 8 m/s released → overshoot ≤ 2° and the rotation stops; the pivot table and `mobility_baseline` unchanged within tolerance | `controller_settings.rs:198`, `movement.rs` | done |
| ~~J6~~ | ~~**The engine is one number and the launch is a jolt.**~~ **CLOSED (2026-09-07, `feat/j6-gearbox-as-data`), narrower than planned and on the record.** `Gearbox { gears, first_gear_top_speed_fraction }` is DATA on every engine module (T-54/T-34-85/Centurion five-speed at 0.22, the Maybach and IS-3 boxes eight-speed at 0.18, the Panther II seven at 0.20); `physics::engine` reads the gear and the revs from the speed (stateless: no wire byte, no clutch timer a replay could drift on) and the engine voice sings them; the drive delivers the rating at every speed above the clutch floor (`the_box_decides_the_revs_and_never_the_rating`: bit-identical to P/v), so the mobility table, the climbing envelope and every replay hold. MEASURED AND REJECTED: a thrust beat at each shift — stateless it traps a hull cruising at a shift speed in the dip (turn radius 6.9 → 5.5 m), stateful it costs the wire; a governor roll-off moved vmax. The „wolno rusza, potem dociąga” of §4 is the grip cap and P/v the fleet already has; the box adds what a driver hears and a readout can show | `game_core/src/modules/engine.rs`, `physics/src/engine.rs`, `client/src/app/audio_link.rs` | done |
| ~~J7~~ | ~~**The tracks go into the wheels.**~~ **CLOSED (2026-09-07, `feat/g1-j7-running-gear-truth`), the seat and the ramp carried to J9.** The top run rides its carriers' LIVE height (`BeltPath::with_sag_and_travel`); the ground run's chord is clamped to the tyre arc near a lower wheel; a belt sample knows which half of the loop it is on (`lower`), so the top run is never travelled twice; the wheels read the physics' ground (`physics::station_ground`, rubble included) against the replicated attitude with the presentation heave subtracted; ONE symmetric ±0.20 m travel limit. Lock: a wheel lifted 0.15 m lifts the run over it ≥ 0.12 m; at ±0.15 m no shoe over the top run or along the ground run lies inside any wheel radius, and the ramps are never worse than at rest | `vehicle_geometry/src/{running_gear_place,running_gear_belt}.rs`, `client/src/vehicle/render_frame.rs`, `physics/src/track_contact.rs` | done |
| J9 | **The belt still grazes the wheels at rest.** The ramp past the outer road wheel is a tangent from a POINT 0.1 m past the wheel, not the common tangent of wheel and wrap: at rest a shoe on the T-54's rear ramp sits 9 mm inside the tyre; the OMSh horn tip sits 24 mm in the steel seat (56 mm for the Kgs dish); `LINK_SEAT` 0.02 puts the plate face 10 mm inside the tyre | `running_gear_belt.rs:413` (`end_geom`), `running_gear_geom.rs:137-140`, `LINK_SEAT` | the ramp as the external tangent of the outer wheel's seat circle and the wrap; `LINK_SEAT` = plate reach + 3 mm; the horn depth from the wheel's seat radius; the fleet re-blessed with `tools bless --vehicle`; lock: the J7 shoe-vs-wheel floor applied to the ramps too, with no rest allowance |

### G — tracks and the ground (inherited)

G1 (three ground samplers — the CLIENT half closed with J7: the wheels read `physics::station_ground`; still open: the drive's slope and the attitude's slope as one number, which moves the climb envelope and needs its re-measure on the record; before X4), G2 (belt speed on the wire), G3 (the FX gauge), G4 (one `TrackCondition`), G5 (per-belt ground; P2.2 closes as X4), G6 (fifteen track models; `parry_query` deleted = X9), G7b (heave on the spring). Full text: `docs/inny-poziom-program.md`.

### Z — destruction (Honest Steel)

Inherited: Z4 (the register replaces the roadmap's DONE); Z7 (shells ricochet off the ground) CLOSED below.

| ID | Defect | Evidence | Closes when |
|---|---|---|---|
| ~~Z8~~ | ~~**A tree vanishes and a log-box appears.**~~ **CLOSED (2026-09-07, `feat/z8-tree-topple`, wire v54).** The fall has a DIRECTION: `CoverState.fall` is the heading byte (256 steps, `terrain::fall_heading_byte`) written the tick a tree leaves Intact — the crusher's heading, or the shell's flight (its recorded direction; the shooter-to-impact line for an old record) — replicated as `Snapshot.cover_falls` beside the phases. The CHOREOGRAPHY: a tree line or bole stepping to gone keeps standing in the DRESSING (`dressing_phase_bytes`) while the ladder draws its trees tilting about the foot toward the heading over 1.5 s (`tree_lod::topple_rotation`, a pendulum's p^1.7, rest 5° off flat); the sim's box is cleared at once — honesty never waits. Then the wreckage bake: the trunk lies ALONG the heading from the foot it stood on (`felled_log_lie`, the bucket bake reads the falls); a born clearing keeps its hashed lie. The dust is the foliage's — the roots' heave now, the crown's leaf litter where it lands, staged — not a tenement's curtain. Locks: `a_felled_tree_remembers_which_way_it_fell` (sim), `a_toppled_tree_lays_its_crown_down_along_the_heading_and_then_leaves_the_frame` (crown within 1 m of the heading line, on the ground; over → no tree in the frame), `a_felled_trunk_lies_along_the_heading_it_fell_on` (the state after the fall equals the bake), `a_felled_tree_goes_down_along_its_heading_before_the_wreckage_bakes` (client), the wire roundtrip; fixtures re-pinned v54. OWED: the Topple as a `DestructionClass` is Z11's; a per-tree heading inside one hedge (today one heading per box) | `terrain/src/battlefield.rs`, `sim/src/cover_damage.rs`, `net/src/lib.rs`, `scene_build/src/{tree_lod.rs,battlefield.rs}`, `client/src/app/{ingest.rs,render.rs}`, `client/src/fx/collapse.rs` | done |
| ~~Z9~~ | ~~**A wall breach is a state swap under dust.**~~ **CLOSED as Z9a (2026-09-07, `feat/z9-wall-segments`, wire v55); the carved wall is Z9b.** Segments as DATA on the box: each facade cut into ~4 m runs (`terrain::facade_segments`, B5's 3–5, at most 5), two bits each — whole / damaged / ruin (an opening from the sill) / rubble (a lip) — packed five bytes per object in `CoverState.segments` and `Snapshot.cover_segments`. The MATERIAL decides (`terrain::WallMaterial` by kind, a church or tower stone by id; thickness 0.25/0.45/0.90 m, segment budget 200/500/900): `sim::segment_damage` — timber takes every round, brick HE in full and a kinetic chip, stone only a charge ≥ 1.5 kg (§13.4); `strike_segment` puts the shell's cover damage on the segment under it, thirds of the budget step the state, only forward; the box's own ledger stands and its collapse fells every segment. HONEST: a standing building with an opening resolves to a HOLLOW of slabs for the eye and the shell (`terrain::opened_building_boxes`: four walls of the material's thickness, the ruined segment sill-high, the rubble one lip-high, one roof) — a line through two openings sees clean through, the far wall stops it, the hull never enters (§13.4, movement keeps the box). The bake draws every opening in the interior's dark (`append_wall_openings`), the client splits its blocking and camera obstacles from the wire's bytes and asks for the bucket's bake when a segment moves. Locks: `wall_segments_pack_two_bits_and_an_opened_building_is_a_hollow_of_slabs`, `a_57_and_a_152_fell_the_same_barn_segment_in_different_counts_and_stone_only_to_a_large_he`, `a_ruined_segment_opens_the_wall_and_the_far_wall_still_stops_the_eye`, `a_ruined_segment_bakes_an_opening_on_its_facade`, `a_breached_segment_arrives_alone_and_splits_the_blocking_and_asks_for_a_rebake`, the wire roundtrip; fixtures and the 7v7 hashes re-pinned v55. OWED (Z9b): the kit CARVES the wall — the bays of a ruined segment become a broken opening part, a rubble one its lip, the roof over it sags; and AP through a timber barn with penetration loss (the S lane's thickness table) | `terrain/src/battlefield.rs`, `sim/src/cover_damage.rs`, `net/src/lib.rs`, `scene_build/src/battlefield.rs`, `client/src/app/{live_cover.rs,ingest.rs,render.rs}` | done (a) |
| ~~Z10~~ | ~~**A collapse is a swap: the walls vanish inside the dust and a slab with 4–6 chunks stands there.**~~ **CLOSED (2026-09-07, `feat/z10-walls-come-down`).** The walls come DOWN: `world_forge::building_kit::collapse_placements(plan, seed, half, progress)` — every standing piece of the kit plan drops to the floor on a gravity curve and a WALL piece (bay, plinth, eave, gable, downpipe, footing) leans about its own foot INTO the box (0.84–1.2 rad at the end, the lean from the seed); the roof's pieces drop straight (a slope swung about its eave rises before it falls, a swung ridge leaves the box). One second (`COLLAPSE_DURATION_S`), deterministic per seed, inside Z1's theatre (curtain, chunks, the settle wave at 0.35 s, the haze at 0.9 s). The client keeps the fall (`ActiveCollapse`) while the sim's box is rubble at once; the kit draws the ruin (B5) under the falling pieces from the first frame, and from progress 1 the ruin alone. Locks: `a_collapse_lays_the_pieces_down_inside_the_box_and_ends_with_nothing_standing` (at 0 the plan, every piece lower each step, no vertex over the top, nothing past the plan's own reach, one fall per seed); `the_kit_dresses_the_towns_dwellings…` gained: at progress 1 the frame equals the plain phase-1 frame BYTE FOR BYTE; `a_kit_dwelling_coming_down_lays_its_walls_into_the_ruin_over_a_second` (client). OWED: the talus drawn (X11) under the ruin; the landmarks (church, windmill, factory) still swap on the bake | `world_forge/src/building_kit.rs`, `scene_build/src/{building_kit.rs,tree_lod.rs}`, `client/src/app/{ingest.rs,render.rs}` | done |
| ~~Z11~~ | ~~**Destruction is one axis.**~~ **CLOSED (2026-09-07, `feat/z11-destruction-class`).** `terrain::DestructionClass` — Immovable / Stateful / Breach / Topple / Crush / Prop / Terrain — as DATA per kind: `StaticCoverKind::destruction_class` (buildings, towers and the wreck Stateful; crag and rail Immovable; tree line and bole Topple; fence Crush; garden wall Breach) and `SceneryKind::destruction_class` (trees and lampposts Topple, bushes Crush, rocks and debris Immovable); Prop stands in the table for the kinds to come. The class DECIDES: `is_crushable` = the class yields to a hull, `leaves_rubble` = Stateful, the client's fall (Z8) and the bake's wreckage pick by Topple, the collapse (Z10) by the kit; the look, the health and the rubble fraction stay the kind's. Lock: `every_kind_names_a_destruction_class_and_the_class_decides_the_phases` (both enums walked; only the immovable has no health; a class no kind wears yet is in the table) | `terrain/src/{battlefield.rs,scenery.rs}` | done |
| ~~Z12~~ | ~~**Ramming a wall does nothing to the wall.**~~ **CLOSED (2026-09-07, `feat/z11-destruction-class`).** The threshold is the class's and the MASS's: `StaticCoverKind::crush_speed_mps(mass_kg)` — a fence or a hedge at 2.5 m/s, a mature bole at 4, a brick garden wall under MOMENTUM (200 000 kg·m/s: the T-54's 36 t past 5.6 m/s; a 15 t light never under the 12 m/s cap); and the price is the class's (`crush_self_hp`: 8 a fence, 25 a bole, 60 a wall). Lock: `a_t54_at_speed_breaches_a_stone_wall_and_pays_but_at_a_crawl_it_stops` (past 8 m/s the wall is a door and the hull pays 60 and drives through; at a crawl it stops against the brick and pays nothing) | `terrain/src/battlefield.rs`, `sim/src/state.rs`, `sim/tests/suite/cover_destruction.rs` | done |
| ~~Z13~~ | ~~**The detached turret is a picture.**~~ **CLOSED (2026-09-07, `feat/z13-landed-turret`, wire v56).** The throw lives below both sides now — `game_core::turret_launch(tank_id, ring, ground)`: 6.5–10 m/s up, a lean of 1.2–3.5 m/s some way round the compass, all from the id — so the AUTHORITY computes where the casting rests the tick it flies (`TankState.turret_rest`, once, from the id and the ring) and replicates it (`Snapshot.turret_rests`); the client's pop-off bends its lean to land its casting exactly there (`TurretPopoff::launch_to`). From then on the rest is a LOW SOLID (`sim::turret_rest_box`: 1.8 × 0.9 × 1.8 m, steel) in the sight list on both sides — the shell stops in it, the eye stops at it, the camera treats it as an obstacle — and never in the movement list: the hull crosses it (X4's model will tilt it). Locks: `a_turret_rests_where_its_id_and_ring_say_and_nowhere_else` (game_core), `the_eye_and_the_shell_stop_in_a_landed_turret_and_a_hull_does_not` and `a_decapitated_wreck_lands_its_turret_and_the_authority_blocks_on_it` (sim), `the_casting_lands_on_the_rest_the_authority_replicated` and the client's detonation lock (the blocking carries the solid, the casting lands on it), the wire roundtrip; fixtures and the 7v7 hashes re-pinned v56. OWED: the hull's crossing with a tilt (X4) | `game_core/src/turret_rest.rs`, `sim/src/{state.rs,cover_damage.rs,tank_state.rs}`, `net/src/lib.rs`, `client/src/vehicle/turret_popoff.rs`, `client/src/app/{live_cover.rs,ingest.rs}` | done |
| ~~Z7~~ | ~~**Shells never ricochet off the ground.**~~ **CLOSED (2026-09-07, `feat/v0-one-march`).** A kinetic round meeting the ground within `GROUND_RICOCHET_MAX_DEG` (12°) of its plane skips off it ONCE — mirrored about the ground's normal (`terrain::ground_normal_at`), 0.75 of its speed, 0.6 of its penetration, `ricocheted_once` set, the same `deflect_shell` a plate's glance uses — and the furrow the impact records is its mark; a charge never skips, a plunge never does, a round that skipped already dies on the next ground. Lock: `a_kinetic_round_skips_off_flat_ground_at_a_graze_and_dies_on_a_facing_slope` (a 4° graze skips and climbs, slower, then dies on the next ground; a one-in-one bank at 45° kills the round; HE goes off where it lands) | `sim/src/shell_step.rs`, `sim/tests/suite/ground_ricochet.rs` | done |

### T — terrain

Inherited: ~~T1~~ **CLOSED (2026-09-08, `feat/t1-grid-2p5m`) — the grid is 2.5 m map-wide, measured; the chunk LOD and the geomorph are NOT built, on the measurement:** the strokes are analytic, so every blueprint's `cell_m` went 5.0 → 2.5 (401 × 401 samples) with no re-authoring. Measured (MX330, the warm box; the owner's cold-sandwich condition still owes a cold number): the ground mesh 56 277 → 176 677 vertices (331 k → 1 051 k indices); the perf probe's full scene p50 on Orliny 28.0 ms (5 m) → 32.4 ms (2.5 m) → 31.6 ms (5 m again) — the +0.8 ms against the rerun is inside the box's thermal drift and the frame is FILL-bound (×1.9–2.2 GPU time at ×2.25 pixels), so the far-chunk LOD the decision allowed is not bought yet (it stays the answer if a cold sandwich shows the +4 ms); Bystra likewise (numbers in the row's PR); map compile + report 60–150 → 170–520 ms per map (`grid_creases` budget 1.5 s); the map swap's bake +12 ms. The one rule that was CELLS became METRES: the reachability leash a start or a target walks to passable ground (`START_LEASH_M` 10 m — two cells of the old grid; two cells of the new one stopped inside the windmill's own box and reported Windmill Hill boxed in). The crease count (G7/C4): along six straight drive lines per map the largest single slope kink fell 22–50 % (Prokhorovka 0.369 → 0.275, Bystra 0.355 → 0.277, Orliny 0.266 → 0.134, Ostrogorsk 0.603 → 0.344, Mazurski 0.189 → 0.133); lock `every_shipped_map_is_sampled_at_two_and_a_half_metres_and_its_creases_shrank` (≤ 0.8×). Five map goldens re-blessed, the 7v7 hashes re-pinned (the deployment's spawn heights sit on the finer ground), the battlefield look frames re-recorded. Tank-scale relief stays analytic through `sample_height` (craters, ruts); the far-morph is a measured follow-up, not a debt. T2 (erosion in the editor), T4 (cliffs triplanar), T5 (road edges and kerbs — its city half CLOSED as B7 on 2026-09-07: kerb, pavement and setts; the country road's edge still open).

| ID | Defect | Evidence | Closes when |
|---|---|---|---|
| ~~T7~~ | ~~**Destroyed terrain is „beznadziejny”**~~ **CLOSED (2026-09-07, `feat/t7-crater-material`).** The crater's material is in the GROUND MESH itself (`scene_build::crater_material`): the crater patch's vertices carry the scorched bowl (darkest at the centre, browning to the lip), the fresh subsoil over the rim out to one and a half radii (the deformation's own spoil reach) and torn turf mottling the ring's outer half — in the vertex lane the ground pipeline lets win over the splat (`tint_weight`, the riverbed's lane), so it stays as long as the hole does; the FX stamps still drape a fresh crater and fade in minutes. CLODS (`crater_clods`): frost-shard stones scaled to lumps of earth, half sunk, six and more on a crater past 1.5 m and a couple on a small one, hashed from the record, baked into the patch (the patch re-uploads with every crater anyway; the instanced clutter path is T9's business). Frame: `destruction_showcase` → `_craters.png` (three HE rounds beside the farm), looked at before merge. Lock: `a_crater_wears_scorch_and_spoil_in_the_mesh_and_spills_clods_on_its_rim` (the ring ≥ 0.5 r past the lip, the bowl scorched and winning, the rim outshining it, the ground beyond untouched, ≥ 6 clods on the rim of a 2.2 m crater, the same scatter per record, the clods in the mesh); virgin ground byte for byte as before | `scene_build/src/battlefield.rs`, `client/examples/probe/destruction_showcase.rs` | done |
| ~~T8~~ | ~~**Ruts and furrows are stickers.**~~ **CLOSED (2026-09-07, `feat/t8-ruts-with-memory`); furrows owed.** Ruts with MEMORY: `terrain::RutField` — the client's own ledger of pressed soft ground (no wire, no gameplay, never folded into `sample_height`), a press per track every 1.5 m of travel (`press(from, to, rut_depth_m)`) as deep as the surface remembers: 3 cm a pass, summed across the track's 0.6 m width, capped at the ground's `rut_depth_m` (grass 3, straw 5, dirt 9 cm; rock takes nothing and spends no budget), 2048 presses per battle, the oldest recycled. The ground mesh reads it (`battlefield_ground_mesh_with_ruts`): the cells any press touches are cut into the crater patch and there the surface is the heightmap LESS the field — heights and normals, so the trough shades as a trough — with the bared earth tinted in the vertex lane; a bake at most every 2 s (`RUT_REBUILD_INTERVAL_S`) on the crater worker. Locks: `passes_add_up_to_the_grounds_memory_and_no_further` (terrain), `a_column_presses_a_rut_the_ground_mesh_keeps` (scene_build: 5–9 cm after five passes, < 5 after one, untouched two metres off, tinted, shaded, no ruts = the same ground), `a_column_of_five_leaves_a_rut_in_dirt_that_outlives_the_fade` (client: five T-54s down a dirt lane press ≥ 5 cm, one hull less, the marks fade and the rut stays). OWED: the AP furrow as displacement in the same patch (today a decal); T9 makes the bake a patch buffer | `terrain/src/ruts.rs`, `scene_build/src/battlefield.rs`, `client/src/app/{motion_fx.rs,render.rs}` | done |
| ~~T9~~ | ~~**Every HE re-uploads the whole ground mesh**~~ **CLOSED (2026-09-07, `feat/t9-ground-patch-buffer`) — Q1's terrain half.** The ground binds ONCE as its BASE (`GroundMesh::base`: every cell of the grid, the cut ones too, plus the apron; its index buffer writable in place) and a crater re-bakes and uploads only the PATCH (`GroundMesh::patch`: the cut cells at crater resolution, the clods, the ruts) as its own chunked buffers (`set_ground_patch`), after CUTTING the base's two triangles per cut cell in place (`cut_ground_triangles`: twelve bytes a triangle through `chunk_scene_indices_with_slots`, idempotent). `GroundMesh::merged()` is the one mesh every other consumer (probes, the harness, the editor) still draws. Measured (the renderer's own byte count, a 100-cell steppe, headless): a bind 974 472 B; the old path re-uploaded 974 472 B per crater in 2.43 ms; the patch path 96 B of cuts + 29 472 B of patch in 0.13 ms — a thirty-third of the bytes, a nineteenth of the time; on a 1000 m map the base is ~4 MB and the ratio wider. Locks: `a_crater_uploads_the_patch_and_the_cuts_never_the_whole_ground` (renderer_wgpu, GPU: ≤ the patch, under a twentieth of the bind, a cut is 12 B and idempotent, the frame draws, empty slices clear), `the_ground_parts_merge_to_the_whole_mesh_and_the_cut_is_two_triangles_a_cell` (scene_build). OWED: a cold MX330 frame delta on the real map (the owner's cool-machine condition) | `renderer_api/src/culling.rs`, `renderer_wgpu/src/scene_renderer/ground.rs`, `scene_build/src/battlefield.rs`, `client/src/app/{mod.rs,render.rs,garage_render.rs}` | done |
| T10 | **Steep faces exist only where a diagonal run-up reaches them.** The Prokhorovka embankment (grade 0.78) and Ostrogorsk's (0.71) are the only places to fall from; nothing measures or locks the climb envelope on the shipped maps; bots never climb | `blueprints/*.map.ron`, `physics/tests/suite/climb_envelope.rs` (synthetic face only) | the map report lists every face between 0.68 and 1.36 grade as a CLIMB (position, height, approach angle) so the author sees them (decision 3: never removed); lock: the count per map is a golden the author blesses |

### S — the shot (ballistics and hit)

Inherited: S2 (sniper feel), S6 (roadmap wording).

| ID | Defect | Evidence | Closes when |
|---|---|---|---|
| ~~S14~~ | ~~**Design and code disagree on air drag with no row.**~~ **CLOSED (2026-09-07, `feat/s14-flight-lock`).** The code stands (GDD row 31): linear drag `c = 0.0130/SD`, pen = `pen100·(v(d)/v(100))^1.5`. The flight is a NUMBER now: `the_br412_flies_one_thousand_metres_by_the_numbers` fires a T-54's BR-412 level from a shelf through the real sim at 60 Hz and reads the kilometre crossing — time of flight 1.173 s (± 0.01), drop 6.64 m under the departure line (± 0.1; the closed form's 6.54 m plus semi-implicit Euler's half tick of gravity per tick), the flown speed 812 m/s within 2 m/s of `speed_mps_at_distance(1000)` (812.2), the muzzle velocity read back within 0.1 m/s. The lying lock `shell_falls_under_gravity_after_firing` (only `v.y < launch`) is replaced by it. Found on the way: from a flat map the round meets the ground at ~520 m and Z7 skips it — the lock fires from a 20 m shelf | `sim/tests/suite/ballistics.rs`, `game_core/src/weapon.rs:255-302` | done |
| ~~S15~~ | ~~**HE direct non-penetration is a flat 18 % of alpha whatever the plate.**~~ **CLOSED (2026-09-07, `feat/s15-he-thickness`).** `game_core::he_surface_damage_hp(alpha, los_mm)` = `max(0, 0.5·alpha − 1.3·LOS at the point)` (`HE_SURFACE_ALPHA_SHARE`, `HE_SURFACE_HP_PER_MM`), read by every resolver path (facet, screens' outer plate, open channel) — §3.1 „obrażenia w funkcji grubości w punkcie" implemented; the flat 18 % is gone. An OF-412 (430 HP) puts 157 HP into a 45 mm side, 20 HP into a 150 mm glacis, 98 HP into 45 mm met at 60°, nothing into 166 mm and up (165 mm is the last HP). Locks: `he_surface_damage_is_a_function_of_the_steel_at_the_point` (game_core), the sim's `high_explosive_on_a_thin_side_hurts_without_penetrating_and_a_thick_glacis_shrugs_it` (an 88 mm HE on a T-34-85 side hurts, on the T-54's 200 mm-through glacis does nothing), `a_non_pen_he_burst_splashes_the_tank_beside_the_impact` (the Tiger II hit takes 0, the bystander takes the blast). Replays and 7v7 hashes held (no fixture carries an HE surface burst) | `game_core/src/armor/resolve.rs` | done |
| ~~S16~~ | ~~**The dispersion seed is predictable**~~ **CLOSED (2026-09-08, `feat/s16-dispersion-salt`).** `SimulationState::dispersion_salt` (`serde(default)` 0) is XOR-folded into the shot draw (`deterministic_pair(id, tick, shot, salt)`); the server installs it per battle from the battle seed (`setup.rs`, `DISPERSION_SALT` lane — random, dealt and AI battles; the practice duel stays at 0 as the client's deterministic test bed); `Replay.dispersion_salt` rides the replay header (`serde(default)`), so older recordings replay under the draw they were made with. No wire change: the client never draws a shot. Lock: `the_dispersion_draw_is_salted_per_battle_and_a_zero_salt_is_the_old_draw`; the three replay fixtures pass unchanged. N9 (WSL) remains the other half of "before the first public match" | `sim/src/{state.rs,aim_dispersion.rs,combat.rs,replay.rs}`, `battle_host/src/setup.rs` | done |
| ~~S17~~ | ~~**A casemate cannot traverse its gun.**~~ **CLOSED (2026-09-07, `feat/s17-limited-traverse`).** `TurretTraverse::Limited { half_arc_rad, rate_rad_s }` (append-only) → `TankSpec::turret_arc_half_rad` (`serde(default)`; `None` = a turret's full circle) → `effective_turret_yaw_rad` clamps to the arc, `is_casemate()` names the superstructure that never turns, `has_fixed_casemate()` is now only a zero arc (no vehicle in the fleet). The Jagdtiger: ±10° (Panzerworld) at 0.20 rad/s (a decision: a hand-cranked ball mount on 75 t). The sim lays inside the arc and reports no velocity on the stop; the client's `firing_solution` takes the nearest bearing inside the arc and reports `Traverse` only past it; the pose holds the superstructure and yaws the GUN in its mount (`VehiclePose.gun_yaw`); the forge's part graph keeps "does not traverse" for a casemate; the bots pivot the hull only for the part of the lay past the arc (before this a casemate bot never laid at all — its command had no steer and the sim zeroed its turret). Locks: `a_casemate_lays_nine_degrees_without_hull_motion_and_refuses_eleven` (sim; replaces `fixed_casemate_ignores_turret_yaw_commands`), `jagdtiger_turret_is_a_casemate_with_a_ten_degree_arc`, the reticle's casemate sight (9° off: clear; 30° off: `Traverse`), the pose's `casemate_pose_holds_the_superstructure_and_lays_the_gun_inside_the_arc`, the recipes' `the_casemate_never_traverses` re-aimed at the arc. No wire change (the yaw byte was always there) | `game_core/src/{modules/turret.rs,tank.rs,modules/loadout.rs,modules/catalog_misc.rs}`, `sim/src/aiming.rs`, `client/src/{aim.rs,vehicle/pose.rs}`, `battle_host/src/bot_combat.rs` | done |
| ~~S18~~ | ~~**The non-penetrating path lumps raw module damage.**~~ **CLOSED (2026-09-08, `feat/s18-s23-wound-scale-glance`).** The non-penetrating module path goes through `MODULE_WOUND_SCALE` (0.45) like the interior path: an OF-412's 280 HP module base lands as 126 on the T-54's 150-HP suspension — wounded, never destroyed in one lump; a computed zero is no module event. Lock: `a_non_penetrating_slap_wounds_a_component_and_never_destroys_it` (combat.rs unit test on a point inside the suspension capsule — from the outside the capsule is not reached by any hull-surface hit, so the path is exercised directly). | `sim/src/combat.rs` | done |
| ~~S19~~ | ~~**The reticle says Clear when the server buries the shell at the muzzle.**~~ **CLOSED (2026-09-07, `fix/s19-buried-muzzle-terrain`).** `trace_shell` returns `Obstacle { Terrain }` from the ground-contact branch, as `shell_step` does; lock: a muzzle inside a 3 m step → the server's first-step `Terrain` impact and the reticle trace name the same surface | `sim/src/shell_trace/mod.rs`, `sim/tests/suite/shell_trace.rs` | done |
| S20 | **The reticle collides with a turret that is not there.** The server sets `turret_detached`; the client's `trace_sets` never does, though the wire carries `detached_turrets` | `sim/src/shell_step.rs:232`, `shell_trace/types.rs:67`, `client/src/hud/reticle_sweep.rs:66-72` | thread `detached_turrets` into `trace_sets`; lock: a headless wreck, shell lane at turret height — both traces pass |
| ~~S21~~ | ~~**Two eyes.**~~ **CLOSED with V0 (2026-09-07): one march for the eye and the shell, the hull-down contracts re-run green on both kernels (Bystra, Orliny, Ostrogorsk); the bot's switch to the shell's reading owed to H1 (the river soak).** | `terrain/src/ground_march.rs` | done |
| ~~S22~~ | ~~**A dead kernel with a real bug in the hot file.**~~ **CLOSED (2026-09-07, `chore/s22-legacy-boxes-out`).** `legacy_boxes.rs` and the `armor_volumes: None` branch deleted; `TraceTank.armor_volumes` is non-optional and `armor_coverage.rs` is the lock that every playable kind owns volumes | `sim/src/shell_trace/{tank,types}.rs` | done |
| ~~S23~~ | ~~**The glance band is invisible and the aim time is undocumented.**~~ **CLOSED (2026-09-08, `feat/s18-s23-wound-scale-glance`).** On the record (GDD row 32, `docs/aiming-model-policy.md`): the aim time is THREE e-folds and the bloom is additive; locks `the_aim_time_is_three_e_folds_and_full_speed_settles_at_the_recorded_bloom` (sim: after one aim time 5.0 % of a shot bloom is left; the T-54 at full speed on the flat settles at 7.07 mrad = base + bloom·aim/3, 2.44× its base) and `the_glance_band_costs_fifteen_percent_at_sixty_five_degrees_and_the_hint_reads_it` (game_core: 0.3·(angle−60)/(bounce−60) — 11.5 % at 65° for the blunt BR-412 against its 73° bounce, 15 % for a sharp AP bouncing at 70°; the reticle's `shell_pen_mm` sum IS the post-bite figure — it always was; now it is said and locked). Splash occluded by wrecks: `wreck_blocks_splash` (a positive interior crossing of a dead hull's slab, never the wreck the shell burst on), lock `a_wreck_between_the_burst_and_a_bystander_shields_it`. | `sim/src/{aim_dispersion.rs,shell_splash.rs}`, `armor/resolve.rs`, `client/src/hud/reticle.rs` | done |

### V — visibility as a resource (as amended 2026-09-07)

| ID | Defect | Evidence | Closes when |
|---|---|---|---|
| ~~V0~~ | ~~the eye and the shell march the terrain differently (= S21)~~ **CLOSED (2026-09-07, `feat/v0-one-march`).** ONE MARCH: `terrain::ground_march` — the sampled ground is piecewise planar (every cell split on the diagonal the mesh draws), so along a segment the clearance is piecewise LINEAR between the cell lines and the diagonals; the kernel walks exactly those breakpoints and the first crossing is exact, no step to choose (a crater's bowl is walked at 0.25 m). The eye (`ground_blocks_segment`, its 0.3 m slack) and the shell (`first_ground_impact`, its radius) are two readings of it; `tank_shell_line_clear` is the bot's reading of the shell — NOT yet switched on: on it the Bystra river soak (`bot_water`) lost a third seed's hull, and that lock is not a threshold to raise; the switch waits on H1's escape (owed). Measured on every shipped map (400 random lines each): the eye sees by its slack alone what kills the shell on Prokhorovka 15, Bystra 8, Orliny 10, Ostrogorsk 3, Mazurski 12 lines — under 4 %; without the slack the verdicts are identical line for line. Locks: `the_march_is_exact_on_the_planar_surface` (300 lines vs a centimetre march: the verdict identical, the impact within 2 cm), `the_march_follows_a_crater_into_its_bowl`, `the_eye_and_the_shell_march_one_surface_on_every_map` (the parity lock, ≤ an eighth by the slack) | `terrain/src/ground_march.rs`, `sim/src/{spotting.rs,shell_step.rs}`, `sim/src/shell_trace/terrain.rs`, `battle_host/src/bot_combat.rs` | done |
| V1 | **A bush hides nothing and a tree line hides everything** — DECIDED IN (the owner: „krzaki mają chować”) | `sim` spotting, `terrain::scenery` | foliage kinds carry an optical density per metre; the spotting ray integrates density along its length (the shell's own march); a target is spotted when the integral stays under the observer's budget; scenery still never blocks a shell; locks on a bush at 5 m vs 50 m and on shell/LOS parity |
| ~~V2~~ | ~~camouflage per vehicle~~ — **OUT** (the owner, 2026-09-07; GDD row 30) | | — |
| V3 | the budget is invisible | HUD | a HUD readout of "seen from N m" that reads view range and density (no camo term) — routed to the interface session |
| V4 | no sixth sense; auto-spot at 50 m unverified | `sim` | sixth sense for everyone from the first battle; the 50 m lock |
| R1 (amended) | ~~no concealment stat~~ — the concealment half is OUT; the view-range half stays | `tank.rs:132` | an authored view range per vehicle from the dossier; lock: the scout sees farther, stationary and moving alike |

### W — the world as gameplay topology (new lane)

| ID | Defect | Evidence | Closes when |
|---|---|---|---|
| ~~W1~~ | ~~**Lanes, crossfire, sniper perches, rotation paths and fallback positions exist only as prose.**~~ **CLOSED (2026-09-08; W1a `feat/w1-topology-as-data` #864 — the data and the report, Prokhorovka; W1b `feat/w1b-four-maps` — the other four maps, the fleet-wide lock).** Every shipped map declares three lanes, one rotation path, one crossfire, one perch and two fallbacks a side, on its own road skeleton and dossier; the report proves each (no topology error on any map) and says on the record where the geometry carries no covered rotation: Orliny's shoulder walk is seen whole from the summits (0 % masked — the massif's own eyes count for both sides), Mazurski's peat defiles 19 % (a lakeland is open by design). Lock: `every_shipped_map_declares_every_topology_class_and_the_report_proves_it` (classes, the two warnings and nothing else, the W2 census bands per lane, the twin's equal census). Found on the way: the passable mask was computed twice per report (0.4 s on Orliny at 2.5 m) — once now, and the report is faster than before W1. Roles today: Observation 16, HullDown 14, Crossing 14, FlankRoute 13, HighGround 11; the report checks the passable graph, the hull-down floor, spawns, symmetry, the destructible floor; exposure lives only in the atlas tool. **W1a DONE (2026-09-08, `feat/w1-topology-as-data`): the DATA and the REPORT, Prokhorovka authored.** `terrain::{Lane, RotationPath, Crossfire}` on `BattlefieldMap` (serde default), `StrategicRole::{SniperPerch, Fallback}` appended (identity order pinned); blueprint `gameplay.{lanes, rotation_paths, crossfires}` in the road's two forms (`Lane` / `MirroredPair` — the twin point by point). `check_topology`: a lane starts within 80 m of a spawn, ends within 80 m of a named place, and walks as a CORRIDOR (every 5 m, any cell across its width passable, every step under the climb grade); a rotation path walks and its masked share is measured against the other side's Observation/HighGround/SniperPerch eyes (hull centre OR turret top, the spotting rule's two samples; a warning under 50 %); a crossfire's two points both see one lane sample from bearings ≥ 60° apart; a perch sees ≥ 2 lane samples at 250–500 m; a fallback stands on a lane's band 50 m behind its far end; a map that declares lanes carries every class per side, one that declares none gets a warning naming the debt. `lane_census` (W2) and `rotation_masking` are public data. Prokhorovka: three lanes a side on the dossier's three characters (field lane 0.2 boxes/100 m, farm corridor 4.7, hill lane 11 hull-down spots), the balka as the rotation (masked 59–69 %), the Psel crossfire (knoll + farm rise over the field lane: 25 of 86 samples seen by both from > 60°), two perches, four fallbacks; the geometry REFUSED the first crossfire (the hill's hull-down shelf and its crest see nothing of the farm corridor: the top ridge at z 368 stands between — a hull-down looks where its crest lets it). Locks: `prokhorovka_declares_its_topology_and_the_report_proves_it`, `the_report_refuses_topology_that_the_geometry_does_not_carry`. W1b (same day): the four other maps authored from the probe's own ranking of point pairs and rotation candidates — the geometry chose (Bystra: the knoll and the quarry ridge over the field lane, the plank track as the rotation 62 %; Orliny: both shoulders' shelves over the pass road; Ostrogorsk: the outskirts rise and the far side's street-end fallback over the high street, the depot row street as the rotation 62 %; Mazurski: the own esker and the far shelf over the moraine lane) | `map_forge/src/report.rs:136, 1685`, `tools/src/atlas.rs:592-681`, the blueprints' `role:` census | roles appended: `Lane` (polyline), `Crossfire` (a pair), `SniperPerch`, `RotationPath` (polyline), `Fallback`; the report computes each from geometry (a crossfire = two points with LOS to the same lane segment from > 60° apart; a rotation path's masking share from the exposure field; a fallback's distance behind its lane); lock: every shipped map declares every role class and the report's numbers are goldens |
| ~~W2~~ | ~~Cover density along lanes is unmeasured.~~ **CLOSED (2026-09-08, with W1).** `map_forge::lane_census` — length, cover boxes within the lane's band (half width + 15 m) per 100 m, hull-down census spots in the band — locked per lane on every map (bands) and cited in every dossier. What it says: the bare flanks are bare (Prokhorovka's field 0.2, Mazurski's shore 0.6 boxes/100 m), the brawls are dense (Prokhorovka's farm 4.7, Orliny's pass 4.1, Ostrogorsk's mill flank 4.0), and the hull-down census finds spots only in Prokhorovka's hill lane (11), Bystra's ford lane (5) and Mazurski's causeway (4) — Orliny's and Ostrogorsk's lanes carry NONE by the crest rule: the mountain and the city fight from walls and shoulders, not crests (a finding for the Orliny rebuild, T2/T4) | `report.rs` | a per-lane cover census (boxes per 100 m, hull-down spots per lane) in the report and the dossier |
| W3 | Bots do not read the topology; they steer straight with an unstuck routine and never path around cover | `battle_host/src/bots.rs:18-30` | bots route on the lane/rotation graph and use cover boxes as waypoints; every X and Z feature the bots cannot use is a human-only advantage in the AI battle |
| W4 | The representative fragment (decision 19) has no acceptance frame | — | Kamienna's route authored (parapet → bluff → market lane), the ~10 s sequence recorded from the player camera with the real HUD, the owner's five questions answered before the next block |

### B — buildings

Inherited: ~~B1 (eaves and ridges)~~, ~~B2 (ground connection)~~ (its dirt apron deferred), ~~B3 (kit of parts, instanced)~~, ~~B4 (coursing and streaks)~~, ~~B5 (a ruin with form)~~, ~~B6 (variety: L-shapes, annexes, terraces)~~ (terraces owed). B3 first. Plus:

| Row | Defect | Evidence | Fix |
|---|---|---|---|
| ~~B3~~ | ~~**Every building is unique merged triangles.**~~ **CLOSED (2026-09-07, `feat/b3-building-kit`), the first family.** `world_forge::building_kit`: a catalogue of parts in canonical metres (plain bay, window/door/shop/portal bays per family and bay width, plinth, eave, roof slope and gable per pitch, ridge cap, chimney) and a split grammar that plans a dwelling from its collision box and its id's seed — storeys, pitch (16–52°), ridge orientation, bay rhythm, ground-floor kind, age band, a knee wall that takes the leftover height; pierced bays are never stretched along the wall (a longer wall earns more bays, never wider ones). `scene_build::building_kit` places the plan as `RenderObject`s (wall/roof tints per building × age), caches per map, and submits the standing, seen ones in the dressing pass beside the trees (`dressing_meshes()` is the one door every renderer registers the pass through); the static bake draws nothing for a kit dwelling (the landmarks — church, windmill, factory hall — stay authored). Measured: Ostrogorsk statics 124 420 tris baked, 98 728 taken out of the buffer (70 dwellings); 4 558 kit instances with nothing culled; the scene instance buffer 512 KiB → 2 MiB with the number (it clipped the street in half at 6.5k). The STREET PLANNER: a grid house whose whole signature a neighbour wears is re-rolled. Locks: every placed vertex inside its box (eave/plinth ≤ 0.4 m past a face, scenery-class), `grid_neighbours_never_share_a_whole_signature` (Bystra + Ostrogorsk grids: no two neighbours the same house, no signature > 34 %, ≥ 3 roof forms, ≥ 2 storey counts — B6's number, started), the town's static-buffer ceiling, the kit instance ceiling, the dressing frame under the instance budget, doors/windows/portals and surface roles on the kit. DEFERRED on the record: the cold MX330 A→B→A frame delta (the owner's cool-machine condition); hip and mansard roofs, dormers, downpipes (B1); the plinth apron and ground connection (B2); coursing and streaks as a surface term with per-building stone (B4); the ruin from the same parts (B5); L-shapes and terraces as blueprint data (B6) | `world_forge/src/building_kit.rs`, `scene_build/src/building_kit.rs`, `scene_build/src/battlefield.rs`, `renderer_wgpu/src/scene_renderer/buffers.rs`, `client/tests/suite/dressing_budget.rs` | done |
| ~~B1~~ | ~~**The silhouette is flush-cut.**~~ **CLOSED (2026-09-07, `feat/b1-b2-roofs-and-ground`).** On the kit: three roof forms — gable, hip (the ridge shortened by the depth at each end, corner triangles and hip ends closing it), pyramid on a near-square box; dormers on an attic slope ≥ 36° with room on the slope (a front with a window, cheeks and a flat roof running back to the slope at the pitch); downpipes at the corners of the eaves facades; the chimney straddles a slope just off the ridge (foot 0.9 m under the ridge line, so it stands on knee-walled fits). Every sloped piece keeps the uniform-scale rule. The signature grows `roof` and `dormers`; the grid lock asks for ≥ 2 forms per grid. Lock: `roofs_take_more_than_one_form_and_every_dwelling_wears_eaves_and_a_footing` (60 seeds: ≥ 2 forms, dormers on ≥ 10, chimneys on ≥ 30, eaves and downpipes on all). Ostrogorsk 5 389 kit parts uncull, the dressing frame 9 263 of 21 845 | `world_forge/src/building_kit.rs` | done |
| ~~B2~~ | ~~**No ground connection.**~~ **CLOSED (2026-09-07, with B1), the dirt apron deferred.** A footing course under every plinth (0.12 m, 0.12 m past the face, scenery class). DEFERRED: the dirt apron and the grass at the wall are a ground splat in the terrain crate whose split the sim's `material_at` rides — a physics change, not a look change (with D43's straw patchwork) | `world_forge/src/building_kit.rs` | done |
| ~~B4~~ | ~~**Walls are one flat albedo.**~~ **CLOSED (2026-09-07, `feat/b4-coursing`).** A BRICK surface role (15, appended): running-bond courses world-anchored on the wall's plane — 0.075 m rows of 0.24 m bricks, joints staggered by half a brick, one tone per brick, a lighter lime mortar; plaster and brick take a rain-streak term drawn down the wall. The kit's signature gains a CLADDING (a third of the town, a sixth of the village in brick, three brick tones for the tint); the catalogue registers twice over with the role baked into the wall vertices. The REVEALS: every pierced bay lines its opening with four shaded faces (jambs, head, sill; `REVEAL_SHADE` 0.62 baked into the vertex colour), so a window is a hole with depth at forty metres. Locks: `windows_carry_shaded_reveals_and_both_claddings_appear`; the grid lock asks for a brick share between an eighth and a half; the kit meshes carry BRICK; the scene shader carries the courses after a bounded glass arm; on the record `the_canyon_facades_carry_coursing_reveals_and_streaks_on_the_record` (the canyon's two facade crops: local contrast 0.0110/0.0127 before the kit → 0.0163/0.0203, floors 0.014/0.017). Not done: AO in the reveals from the SSAO pass (the baked shade stands in), per-building stone tones (one kit stone) | `renderer_api/src/scene.rs`, `renderer_wgpu/src/shaders/scene.wgsl`, `world_forge/src/building_kit.rs`, `scene_build/src/building_kit.rs`, `client/tests/look_goldens.rs` | done |
| ~~B5~~ | ~~**A ruin is a heap of 7–10 slabs.**~~ **CLOSED (2026-09-07, `feat/b5-ruin-with-form`).** `plan_ruin`: two ADJACENT facades keep standing wall — the run cut into 3–5 segments with broken tops, the two segments meeting at the kept corner at least four fifths of the ceiling — the other two keep stubs, every facade its footing and plinth, a floor slab hangs in the kept corner at half the ceiling; the heap inside stays the static bake's mound. Everything under the sim's rubble height for the kind (`rubble_height_frac` × the box: a tenement's 2.0 m, a farm building's 40 %) and inside the box — what a shell stops against is what the eye sees. Phase 1 submits the ruin objects in the dressing pass; phase 2 nothing. Locks: `a_ruin_keeps_two_wall_planes_and_a_floor_under_its_ceiling` (deterministic, ≥ 2 standing facades, a slab, under the ceiling, inside the box), the Ostrogorsk budget lock checks a collapsed house's ruin against its mound top. Owed: a frame of a ruin — the review set has no phase-1 view; it comes with Z10's choreography, whose final state must equal this ruin byte for byte | `world_forge/src/building_kit.rs`, `scene_build/src/building_kit.rs` | done |
| ~~B6~~ | ~~**Repetition.**~~ **CLOSED (2026-09-07, `feat/b6-annexes-and-terraces`), terraces owed.** The grid is DATA: `TownGrid.annex_share` grows an annex behind a share of the houses — a smaller, lower box sharing the house's rear face, shifted toward one end (an L, not a T), seeded from the canonical cell so both twins carry it (Bystra 0.5, Ostrogorsk 0.35; `town_grid_cells` is the one place the cells are decided, the editor counts through it). The kit's PARTY WALLS: a facade that touches another dwelling's box (`party_walls`: the planes within 6 cm, a metre of overlap) is blind — plain wall per storey, no bays, no eave, no downpipes — and the street moves to the facade across; an annex derives a cottage or a townhouse wing, never a tenement or a barn. The signature gains `attached`. With B3's street planner (no neighbour the same house), B1's roof forms, B4's claddings and the age band, the axes promised on 2026-09-07 stand: bryła, dach, kondygnacje, rytm, parter, wiek — a street is not one house repeated. Locks: `a_party_wall_is_blind_and_the_street_moves_across`; the grid lock asks for ≥ 20 % attached houses; the map goldens re-blessed (Bystra, Ostrogorsk) with the annex boxes; the sim's cross-block lock fells every block the line runs through (an annex is a box of its own). OWED: terrace rows (shared walls along a street) as grid data, and the REGION per map — the Prochorovka izba, the Galician podcienia, the Orliny stone — as families the blueprint names | `map_forge/src/{blueprint.rs,compile.rs}`, `map_forge/blueprints/{bystra-valley,ostrogorsk}.map.ron`, `world_forge/src/building_kit.rs`, `scene_build/src/building_kit.rs`, `editor/src/objects.rs` | done |

| ID | Defect | Evidence | Closes when |
|---|---|---|---|
| ~~B7~~ | ~~**No kerb, no pavement, no setts.**~~ **CLOSED (2026-09-07, `feat/b7-kerb-pavement-setts`).** SETTS: `surface_role::SETTS` (16, appended) rides the ground vertex's surface lane wherever a `Cobble` road's paint is more than half; the terrain shader reads the lane (location 10, flat) and draws granite setts there — 0.14 m stones in a lattice turned 30° off the axes, one tone per stone, a 12 mm dark joint — in the road's own tone, over the rock lane's crack tile (`setts_shade`). KERB AND PAVEMENT: `street_kerb_runs` — a granite kerb (0.25 × 0.15 m) and a flagstone strip (1.5 × 0.10 m) on both edges of every paved road, runs ≤ 8 m following the ground, breaking at the junctions, baked into the statics by bucket (the row asked for the instanced path; the bake is one draw cheaper and a street never moves); under the belly line by a const assertion. Locks: `a_paved_road_wears_setts_not_cliff_cracks` (scene_build), `the_paved_street_wears_setts_in_the_terrain_shader` (renderer_wgpu); the Ostrogorsk goldens re-recorded. OWED: the kerb's mitre at a bend and the dropped kerb at a gateway; a setts tile in the splat's own bake for the far field | `renderer_api/src/scene.rs`, `renderer_wgpu/src/shaders/terrain.wgsl`, `scene_build/src/battlefield.rs` | done |

### D — the picture (the register's home moved here from `docs/art-direction-program.md`)

Inherited and open: D4 (no dark mass — CLOSES WITH D34–D37), D8 (no edge wear), D9 (dirt lane never populated), D15 (nothing to look at up close beyond the T-54), D18 (Orliny borrows Bystra's light), D33 (the hall brightened; the turntable 1.9× the floor after K24-1). D17 CLOSES (the lineup paints since K24). Full text: `docs/art-direction-program.md`.

| ID | Defect | Evidence | Closes when |
|---|---|---|---|
| ~~D34~~ | ~~**The low sun does not own the field.**~~ **CLOSED (2026-09-07, `look/d34-d36-low-sun-owns-the-field`).** Measured on LEVEL ground (the CPU mirror of `light_radiance` for n = +Y, replacing `KEY_LIT_FRACTION 0.75`) the golden evening was a dusk: the 14.9° sun at 1.32 landed the field at 0.11 luma with the blue indirect delivering 55 %. Now: indirect small and warm-grey (ambient (0.10, 0.10, 0.13), fill (0.06, 0.06, 0.07), rim halved), the sun at 20° (still inside the long-shadow band, 2.7:1) and 1.3× (1.72, 1.24, 0.72), the look exposed at 1.4; on level ground the key's share is 0.72, the incident light R/B 1.69, the field 0.31 luma, a sunlit wall 0.65, the sun-side haze 0.965 (under unit energy). The same honest measurement found the dawn fog's field at 0.215: its ambient lifted to (0.31, 0.33, 0.38) and its sun to (1.20, 1.05, 0.88). Locks: `the_low_sun_owns_the_field_and_the_lid_casts_a_shade` (share ≥ 0.6, R/B ≥ 1.5), the value bands now measured on n = +Y, look goldens re-recorded | `renderer_api/src/lighting.rs`, `tests/suite/look_locks.rs`, `client/tests/goldens/look/*.png` | done |
| ~~D35~~ | ~~**The reference frame looks away from the sun.**~~ **CLOSED (2026-09-07, `look/d35-into-the-sun`).** A second canonical Prokhorovka evening view, `prokhorovka_evening_into_sun`, from the player's chase seat looking INTO the 20° sun with the T-54 ahead: its cast shadow comes toward the eye, the field's shade masses face the camera, the disc and its haze sit in the frame. Measured on the record: 27.0 % dark, 15.8 % DEEP (< 0.07 linear) against 1.8 % on the antisolar contact frame and 0.1 % on the reference frame — the number behind „every shadow hides behind its caster”. Locks: `the_sunward_frame_carries_rule_ones_shade_mass` — `OUTDOOR_DARK_TARGET` 0.08 ASSERTED on the sunward frame (not reported as a debt), deep shade floor 0.10 / target 0.15, near-white ≤ 1.5 %, the antisolar frame always carries less deep shade than the sunward one; `FrameStats.deep` added. The sun-side haze grades at 0.876 on the CPU mirror and the frame passes D3's white ceiling — no scatter change was needed after D34's key | `scene_build/src/review_views.rs`, `client/tests/look_goldens.rs`, `goldens/look/prokhorovka_evening_into_sun.png` | done |
| ~~D36~~ | ~~**Cloud shade cannot be seen and the lids cast hard shadows.**~~ **CLOSED (2026-09-07, with D34), one part rejected.** `SceneLighting::shadow_strength` is profile data blended like the rest: 1.0 under a clear sky, 0.3 (overcast), 0.35 (rain), 0.7 (dawn fog); `shader_params(near, look_strength)` folds it into the sun-shadow term, so the lid's shadows are a shade and the evening's a cut. Cloud shade deepened on every clear look (battlefield 0.25 → 0.6, Bystra afternoon 0.3 → 0.7, the evening 0.3 → 0.7; the foggy dawn keeps 0.1). REJECTED: 2.5× finer shade blobs — `ground_cloud_shade_scale_matches_the_dome` refused them, rightly: 1.35/400 is the dome's projection at a 400 m cloud base and the shade a player stands in must belong to the bank overhead; the visibility is the depth. Locks: lids ≤ 0.4 / suns ≥ 0.6 (`look_locks.rs`), `a_patch_of_cloud_shade_is_visible_on_every_clear_look` on the baked tile (≥ 5 % of the field ≥ 30 % darker) — the golden-pixel neighbourhood lock waits for D35's sunward frame | `renderer_api/src/lighting.rs`, `renderer_wgpu/src/scene_renderer/{shadow.rs,draw.rs,cloud_map.rs}` | done |
| ~~D37~~ | ~~**The sky is lavender.**~~ **CLOSED (2026-09-07, `look/d37-sky-gradient`).** The dome has a THIRD stop: `SceneLighting::sky_band_rgb`, the sky at the elevation a chase camera looks at (dir.y ≈ 0.12), profile data blended like the rest and carried in the camera uniform (1008 → 1024 B). The golden evening writes straw-gold (0.78, 0.64, 0.47) where the two-stop mix fell into (0.57, 0.48, 0.46); every other profile carries the old dome's colour at that elevation, so their pictures did not move. The dome and the reflections' `env_sky` mix horizon → band → zenith; the shaded side of a cloud takes the band's HUE at the ambient's luminance (clamped ±30 %), so antisolar evening clouds are warm-grey, not blue. Locks: `the_played_sky_band_is_never_lavender` (evening R > G > B, R/B ≥ 1.5; every band between its horizon and zenith in luma, no invented chroma) and, on the record, `the_golden_evening_sky_is_straw_not_lavender_on_the_record` (`FrameStats.sky_warmth` = top-15 %-rows R/B ≥ 1.05 on every frame lit by the evening profile, selected by data). The chroma ceiling moved 0.60 → 0.63 with the number (the evening contact frame 0.563 → 0.603 — sky and light, not dirt). N6's elevation ladder stays open; this is the played band | `renderer_api/src/lighting.rs`, `renderer_wgpu/src/{gpu_layout.rs,shaders/sky.wgsl,shaders/lighting_common.wgsl,shaders/camera_common.wgsl}`, `client/tests/look_goldens.rs` | done |
| ~~D38~~ | ~~**The leaf sprites are the darkest, most saturated object in every frame.**~~ **CLOSED (2026-09-07, `look/d38-leaf-sprites`), one part decided against the row.** The cluster colour page is now the leaf's albedo, FLAT: `bake_clusters.py` puts an emission of the material colour at the output (the lit Principled+translucent tree stays for previews), so no Cycles occlusion, cosine or translucency is baked in — the engine lights the card live, ONCE. Albedos re-tuned to the world's scale and all four species re-baked in Blender 5.2 (≈106 s each): oak 0.226 / 0.43, poplar 0.236 / 0.45, fruit 0.233 / 0.42, bush 0.212 / 0.44 (mean linear luma / saturation; texels under 0.07: 1–2 %, was 23–33 %). The impostors were re-baked the same way: the cards are an emission of the sprite × the ENGINE's shade lane (`CORE_SHADE` 0.68 → 1.0 by distance from the crown centroid, the same law `authored.rs` deals), so the far rung is the near deck's picture before the live light — 0.17–0.19 luma against the near deck's ≈ 0.19 (the first re-bake left the white-world occlusion in and measured 0.57 of the near deck: a LOD pop by brightness). DECIDED AGAINST: `CORE_SHADE` ≥ 0.85 — with no occlusion in the page the 0.68 lane is the crown's ONE occlusion; raising it would flatten the crown. Locks: `cluster_sprite_albedo_sits_in_the_world_window` (each species' embedded page: luma 0.15–0.25, sat ≤ 0.50, ≤ 5 % texels under 0.07); `SPECIES_GOLDENS` blessed (clusters + impostors; trees and bark untouched); look goldens re-recorded | `scripts/flora/bake_clusters.py`, `scripts/flora/bake_impostor.py`, `assets/flora/{oak,poplar,fruit,bush}/`, `world_forge/src/tree/authored.rs` | done |
| ~~D39~~ | ~~**The vehicle is not PBR and three of four roles have no highlight.**~~ **CLOSED (2026-09-07, `look/d39-vehicle-lobes`).** The roughness lane ADDS to the role (`role + (G − 0.5) · 0.30`, `ROUGHNESS_LANE_SPAN`) instead of multiplying it by (0.55 + G), which saturated cast armour, track metal and rubber at 1.0 — no lobe, no sky. The exterior roles are an ordered ladder: glass 0.10 < barrel 0.45 < track 0.50 < rolled 0.55 < cast 0.62 < rubber 0.80 < timber 0.88 < canvas 0.95; the cube-of-smoothness lobe and the metalness-driven F0 (track 0.50 on the lane) are unchanged and now REACHED. CPU mirror `renderer_api::vehicle_lobes` (`surface_roughness`, `specular_amplitude`, `environment_energy`). Locks: every steel/rubber/glass role keeps a non-zero lobe at the lane's roughest texel and the ladder is strictly ordered at the finish (canvas and timber may go matte by authorship); track metal carries > 5× the sky energy of rubber (28×); the shader source carries the same span, ladder and lobe law (`the_vehicle_shader_carries_the_cpu_mirrors_roughness_ladder`). Studio tiles and look goldens re-recorded. Not done here: a real GGX/PBR BRDF — the row's 3/10 was the saturation, and this is the half that costs zero milliseconds; the material octaves are D40/D41 | `renderer_wgpu/src/shaders/vehicle.wgsl`, `renderer_api/src/vehicle_lobes.rs`, `tests/suite/{vehicle_lobes.rs,wgsl_layout.rs}` | done |
| ~~D40~~ | ~~**The vehicle textures shimmer by construction.**~~ **CLOSED (2026-09-07, `look/d40-vehicle-mips`).** Every vehicle map layer uploads its COMPLETE box-filtered mip chain (`Rgba8MipChain::build`, the ground maps' walk — nine levels for the 256-texel defaults, `vehicle_mip_level_count`) and the sampler filters between levels (`Linear`, was `Nearest` on one level). The synthesis' per-texel hash is gone: the grain is two smooth value-noise octaves at 40 and 72 texels of the 2 m triplanar tile (0.31 m and 0.56 m), the finer carrying 0.6 — the material floors (normal tilt, grain, cavity span, roughness span) hold at the same amplitudes. Locks: `vehicle_maps_upload_a_complete_mip_chain` (9 for 256², 1 for 1×1), `no_material_map_carries_texel_noise` (rule 5 for vehicles: the neighbour step ≤ 0.35 × the 8-texel step on every map and family; the hash measured ≈ 1.0, the octaves ≈ 0.13). Not done: a hull LOD in battle (Lod0 always) — a triangle question, outside block 2's „zero triangles” | `renderer_wgpu/src/scene_renderer/vehicle_materials.rs`, `vehicle_forge/src/artifact/material_synthesis.rs`, `vehicle_forge/tests/suite/material_floor.rs` | done |
| ~~D41~~ | ~~**Paint is one tone.**~~ **CLOSED (2026-09-07, `look/d41-paint-octaves`).** `vehicle.wgsl`: a 3 m object-space octave over albedo (±6 %) and roughness (`MACRO_OCTAVE_PER_M` 0.33); bare steel where the surface turns within a pixel (`fwidth` of the normal, armour roles — albedo toward steel, the metalness lane up 0.6, roughness down 0.25); a mud band from the ground up on every exterior role (0.15–0.9 m, broken by the broad noise, matte, rain thins it). The vehicle's ground rides the instance tint's w lane (`scene_resources.rs`): the lowest origin among the objects sharing a tank and a material — the hull's, placed at the ground by the client; a turret's own origin (the ring) would have put mud on its skirt. Locks: the shader source carries all three terms; on the record the evening contact frame's lower-hull crop reads darker and ≥ 1.30× warmer than its deck crop (1.21 before, 1.40 after). Not done: hub-centred oil, the 2 m-kernel contrast lock (the crop was the number measurable on the goldens), and D8/D9 as separate rows are closed by this one | `renderer_wgpu/src/shaders/vehicle.wgsl`, `renderer_wgpu/src/scene_resources.rs`, `client/tests/look_goldens.rs` | done |
| ~~D42~~ | ~~**Garage and field disagree on the same hull with no lock.**~~ **CLOSED (2026-09-07, `look/d42-hangar-field-parity`), with a debt on the record.** Measured for the first time: the T-54's turret roof read saturation 0.89 in the field (linear (0.31, 0.24, 0.04) — no blue left) against 0.53 in the hangar: the amber key × olive × the evening grade's saturation 1.25. Two data fixes on the evening: grade saturation 1.25 → 1.10 (the key carries the chroma, the grade must not push the paint's blue to zero on top) and the sky ambient takes the sky's blue at the same luminance ((0.10, 0.10, 0.13) → (0.09, 0.10, 0.16); the deck sees the dome; `the_low_sun_owns_the_field` still holds, share 0.72). Field turret now 0.787: gap 0.255. Locks: `the_hull_wears_the_same_paint_in_the_hangar_and_on_the_field` — the TURRET ROOF crops (the subject boxes hold grass and hall floor) within a 0.30 chroma ceiling and a 1.8× luma ratio, the distance to the 0.15 target printed as LOOK DEBT; `the_presentation_bible_quotes_the_hangar_and_the_field_from_the_code` — the bible quotes the profiles' ambient, key and grade numbers verbatim (it asserted parity with numbers that were not the code's). The garage look goldens re-recorded for the first time since D39–D41 (they had drifted on master; the Jagdtiger hero's form floor re-derived 0.0095 → 0.0082 because the filtered mips no longer count texel noise as form). Open on the record: the 0.255 gap is the light — closing it toward 0.15 is a question for the vehicle material's own chroma under a warm key (K6) | `renderer_api/src/lighting.rs`, `client/tests/look_goldens.rs`, `renderer_api/tests/suite/look_locks.rs`, `docs/vehicle-presentation-bible.md` | done |
| ~~D43~~ | ~~**The ground is a marbled mat.**~~ **CLOSED (2026-09-07, `look/d43-ground-form`), one part deferred on the record.** DATA: worn dirt reads at most 0.85× lush grass on every shipped map (Prokhorovka 0.307 → 0.256 against grass 0.310; Bystra, Orliny, Ostrogorsk re-set; Mazurski already 0.82) — the rule lives in the map report (`DIRT_TO_GRASS_LUMA_CEILING`, Severity::Error, every shipped map answers to it). The tone fields halved: `MACRO_TONE_AMP` 0.12 → 0.06, the quilt's light drift ±17 % → ±8.5 % (locked in the shader source). The clumps: 0–4 centres per 8 m cell from a 60 m occupancy noise over the cell's world origin (stretched 1.6× about its middle) instead of the 2–3 lattice; `pull_toward_clump` leaves an open cell's tufts where the candidate stream put them; lock `clump_counts_follow_the_occupancy_noise_not_a_lattice` (over 2 500 cells: ≥ 8 % open, ≥ 8 % with four, no count owns 45 %, {2, 3} ≤ 70 %). DEFERRED: the straw patchwork (+29 %, 65/19 m) lives in the terrain crate's ground bake and the SIM's `material_at` rides the same split (traction) — halving it is a physics change, not a look change; and plots as authored polygons are map-editor work (T-lane). The map compile hashes did not move (materials are not in the hash); look goldens re-recorded | `map_forge/blueprints/*.map.ron`, `map_forge/src/report.rs`, `shaders/terrain.wgsl`, `scene_build/src/grass.rs` | done |
| ~~D44~~ | ~~**A distant tree line is a green rectangle.**~~ **CLOSED (2026-09-07, `look/d44-hedge-body`).** The hedge body is FOLIAGE with a BROKEN TOP: cut into ≤ 10 segments of ~4 m along the run (12 triangles each, ≤ 120 per box), each segment's top a hash of the cover id and its index — the body height down to 1.2 m below it, never under 3.2 m (the Tiger II's roof stays hidden behind the deepest dip) — and every vertex in the FOLIAGE role, so the scene shader lights the mass as leaves (wrapped key, back-lit transmission; the atlas' white slot at uv 0 keeps it opaque) instead of as a cliff with strata on its faces. Lock: the F3/F7b test rewritten — ≤ 120 triangles, every vertex FOLIAGE (`no legacy`), the lowest dip ≥ 3.1 m, the top breaks ≥ 0.5 m on at least three of Bystra's lines; look goldens re-recorded | `scene_build/src/battlefield.rs` (`append_tree_line`, `TREE_LINE_SEGMENT_M`, `TREE_LINE_TOP_BREAK`, `TREE_LINE_BODY_FLOOR_M`) | done |
| ~~D45~~ | ~~**The garage hides its hero.**~~ **CLOSED (2026-09-07, `look/d45-garage-pivot`), the crew plate left to the interface session.** The hero pivot sits ahead of the turntable centre along the parked hull by 0.16 of the vehicle's silhouette span (`hero_pivot_for`, 1.9 m on the T-54; a share, not metres, because the boom scales with the span and a Jagdtiger's barrel overhangs twice a T-54's) — the live camera and every hero review view share the one function. `TURNTABLE` 0.34 → 0.30 (rim/seam/hub with it; the row's 0.22 was measured and refused — it put the hero room's dark plane at 0.811 against its 0.78 ceiling and the running-gear close-up at 99.8 % void, and 0.28 still left the hero 1.49× its room against the 1.5× floor: the disc was carrying the room's mid plane, and the room may not get darker). Lock: `the_hero_muzzle_and_stern_sit_inside_the_panel_free_band_on_every_vehicle` — the muzzle (turret front + stock barrel, the silhouette card's own geometry) and the stern project inside the band no plate covers (`HERO_PANEL_FREE_BAND_NDC` −0.625..0.5625, i.e. the crew column's right edge and the stats column's left edge of the 1920 u screen, locked against the screen's plate constants by `the_hero_panel_free_band_is_the_screens_plates`); the T-54's muzzle sits at 0.52, the Tiger II's at 0.56. NOT DONE: the crew plate as a 62 u strip (VR-06) — the interface session's file; the gate gap's HDR sky is the interior background policy, unchanged. Garage look goldens re-recorded | `scene_build/src/hangar.rs`, `scene_build/src/review_views.rs`, `client/src/app/garage/{camera.rs,screen.rs}` | done |

### U — interface (routed to the interface session; `docs/interface-policy.md` owns these files)

Inherited U1–U11 stay with that program. New, from the graphics review verified in code:

| ID | Defect | Evidence | Closes when |
|---|---|---|---|
| ~~U12~~ | **CLOSED 2026-09-08** (`feat/u12-stencil-floor`): `Style::STENCIL_FLOOR_U = 24` — the stencil faces are for headings at 24 u and above; every label under it across the garage and the HUD is `VALUE_STRONG` now (the sweep by size); locks `no_garage_label_wears_the_stencil_below_its_floor` (every garage list) and `no_hud_label_wears_the_stencil_below_its_floor` (every HUD state); the HUD and garage goldens re-recorded with the picture change stated. ~~Labels are stencil at 1.0 px stems.~~ `Style::LABEL` = Big Shoulders Stencil Medium 16 u → em 13.3 px, stem 1.0 px at 900p; Plex SemiBold 20 u = 2.1 px; the goldens are 960 × 540 (stem 0.6 px) and could not show it; 20 u overprints the value in compare mode, 18 u fits | `garage/screen.rs:697-704, 605-612, 389-390`, `chrome.rs:55-62`, `ui_kit/src/font/manifest.rs:55`, `look_goldens.rs:29-30` | a dated policy row: stencil for headings ≥ 24 u only; labels → `VALUE_STRONG` 18 u; locks: no stencil below 24 u, a garage frame at 1080p under the image lock |
| ~~U13~~ | **CLOSED 2026-09-08** (`feat/u14-u13-reticle-readouts`): an enamel plate under the readout column (`RETICLE_READOUT_PLATE`, alpha 0.86, sized for the widest row so it never resizes), drawn first; lock `the_reticle_readouts_stand_on_a_plate_that_keeps_them_above_three_to_one` — every readout tone (range, unit, pen, no-pen, armour, blocked) ≥ 3:1 over the plate over noon straw, and the plate adds contrast against bare straw; the reticle_readouts ratchet moved 5 → 6 on purpose. ~~The reticle numbers fail the policy's own 3:1.~~ Pen "201" at 1.07:1 on straw, armour "120" 1.62:1, range "214" 1.89:1 — drop shadow only, no plate; the 3:1 lock skips text without a plate (the reticle is a legacy payload) | `hud/reticle_readouts.rs:91-121, 193-221`, `font/layout.rs:61-73`, `look_goldens.rs:1099-1107` | an enamel plate under the two readout rows (a dated H25 exception); the 3:1 lock extended to the legacy numbers on the golden |
| ~~U14~~ | **CLOSED 2026-09-08** (same PR): the marker's range row dropped (the sight prints the range once; lock: no marker text ends in the unit), the zoom label burned (readouts ratchet 6 → 5; `zoom_factor` left the model). ~~Range printed twice; the zoom label answers no question~~ | `hud/marker.rs:250`, `reticle_readouts.rs:193`, `hud/readouts.rs:25-39`, `reticle_overlay.rs:11-35` (H27's table) | the marker's range dropped, the zoom label burned (ratchet ceiling 6 → 5), both in the audit table |
| ~~U15~~ | **CLOSED 2026-09-08** (`feat/u15-short-hit-log`): the rows are SHORT by default — outcome · damage · the other hull, 300 u wide on the same centre line — and N prints the DETAIL the rows always carried (round, mm at the angle, zone, module, crew, range); Standard = N toggles detail, Full = detail always, Minimal = one short row; lock `rows_are_short_by_default_and_n_prints_the_detail`; the N-key edge lock renamed to the new meaning. ~~No compact hit-log form.~~ Six 620 px rows for 8 s in Standard; one formatter | `hud/damage_log.rs:20-21, 81-139, 320` | a short row by default (outcome · damage · target), detail on N — a new decision row on the closed H8 |
| U16 | **Texture competes with text.** After #801: brushed ±44 %, painted up to ±52 % (`TILE_NEUTRAL_GAIN 2.0`), ~35 plates in one olive, no glass in the garage | `ui_kit/src/sheet.rs:174-230`, `hud.wgsl:28`, `theme.rs:140-183` | DECIDED: #801's plate reading stays on bars; any plate carrying text ≤ 12 u takes the enamel tile; amplitude behind letters ≤ ±10 %; the `SHEET_HASH` re-pinned once |
| U17 | **The crew plate is 330 × 400 u for five role words** | `garage/screen.rs:581-615` | a 62 u strip (with D45) |
| U18 | **Equipment is missing.** GDD §3.5: „Wyposażenie: 3 sloty z jednego zestawu, swap za darmo" — the garage has six MODULE slots (turret, gun, hull, engine, suspension, radio) and no equipment at all; the owner's „moduły… wygląda i działa tragicznie" (2026-09-08) is, half of it, a feature that does not exist | `garage/draft.rs` (`FitSlot`), `garage/screen.rs:764` | the OWNER'S DECISION: the set (which three-of-N pieces, what each does in the sim — GDD: no ±25 %, honest numbers), then a lane row; until then the six module slots carry their NAMES (U21) |
| U19 | **Consumables are missing.** GDD §3.5: „Materiały eksploatacyjne darmowe" — nothing in code or docs models a repair kit, a first-aid kit or an extinguisher; the damage panel's repair clocks (W-4) are OWED too | `docs/game-design.md:187`, no code | the OWNER'S DECISION: which three, what they do (repair a module / heal a crewman / put out a fire — the sim has all three states), free per GDD; then a lane row with the garage slot and the battle key |
| U20 | **The tech tree is eight hulls on a grid.** G12 draws columns by tier, rows by nation·class; with an eight-hull roster it reads as a scatter, and GDD §3.5 wants class bands (1939–40 … 45) — an R-lane row the policy already names | `garage/tree.rs`, `target/garage_techview.png` (2026-09-08) | the R-lane class bands (the OWNER'S call on the four bands and what sits in each), then the tree redrawn by band; more hulls (K lane) before any redraw is worth a frame |
| ~~U22~~ | **CLOSED 2026-09-08** (`feat/u22-standard-preset`): the STANDARD preset showed the visibility budget line („SEEN FROM 440 M · MOVING”) and the RTT lamp („RTT 48 MS · SNAP 32 MS”) the policy reserves for FULL (`docs/interface-policy.md`, „Three presets”: standard = everything but the budget line and the RTT lamp) — two lines of text over the sight in the preset every player starts in, the owner's „najebane” of 2026-09-08. `HudLayout::shows` and `shows_budget` say the policy's word now; lock: the preset test asserts all three presets; the HUD goldens re-recorded (the standard frames lose both lines). ~~The standard preset shows what the policy gives FULL~~ |
| ~~U21~~ | **CLOSED 2026-09-08** (`feat/u21-module-slot-names`): each slot prints its NAME (TURRET · GUN · HULL · ENGINE · SUSPENSION · RADIO, `SLOT_*` words, `VALUE_STRONG` 16 u, the garage floor) above its value (`VALUE`, tabular), the rack line reads „RACK 34 / 34"; lock in the garage's legibility test (every slot names itself, six different words, a value under each); the garage goldens re-recorded with the picture change stated. ~~The module slots say a number and no name.~~ „200mm · 100mm · 1550 · 390kW · 45d/s · 100kg" under six icons at 16 u stencil — the player cannot tell the turret's front from the gun's calibre from the hull's HP without hovering (U12's stencil defect on top) | `garage/screen.rs:764-818`, `garage/draft.rs:239` | each slot prints its NAME (`MODULE_*` words) on a legible label and the value under it; the ammo slots print their designation, pen · damage and the count as today; the rack line reads „RACK 34 / 34" |

### K, P, C, Q, N, H, R, F, O, A, L, M — inherited, unchanged

N12 (CLOSED 2026-09-08, `fix/n-input-backlog-catch-up`): the host applied ONE input per tick and never caught up after a dry tick, so under loss the player's control lag grew by a tick per dry tick for the whole battle — `RemoteInputQueue::command_for_tick` now skips a stale edge-free command when a newer one already waits, a fire or ammo switch is never skipped; lock `a_dry_tick_is_caught_up_on_the_next_one_and_no_edge_is_skipped`, and the client's seeded-loss lock is green again (W1b's bots had shifted the loss pattern and exposed it). K0, K3–K6, K9, K11–K19, K21–K23 (K10 CLOSED with J7; K24 CLOSED: `Nation::paint` merged as #780/#781 — one RGB per nation; the materials are K6), P1–P3, C1–C4, Q1 (CLOSED 2026-09-07: its terrain half = T9, the cover half = PR-04's dirty buckets), Q2 (CLOSED 2026-09-08, `feat/q2-live-frame-log`: `client::frame_log` — `WOT_FRAME_LOG=<path>` times every presented frame by phase (fixed ticks, bookkeeping, camera, scene assembly, HUD, upload, render), keeps a 3600-frame ring and every hitch over 25 ms with its worst phase, reads the GPU pass table every 60th frame (the window's device is built with `TIMESTAMP_QUERY` only then), and writes the report at exit; `WOT_AUTOBATTLE=1` deploys straight into the AI battle, `WOT_AUTODRIVE=1` drives full throttle in a weave, `WOT_EXIT_AFTER_S=<s>` ends the run; `scripts/perf/cold-run.ps1` waits for the GPU to cool, samples temperature, SM clock, throttle mask and the client's working set every 5 s. FIRST READING, Bystra, AI 15v15, 90 s autodrive, MX330 from 66 °C: p50 26.7 ms, p95 101 ms, 85 % of frames over 60 Hz; mean CPU phases fixed_ticks 16.0 ms, render 17.0 ms, hud 2.9 ms; hitches by worst phase: fixed_ticks 560, render 431; GPU frame 31.8 ms with scene_pass 22.7 ms at a throttled clock (74 °C, mask 0x20 = SW thermal slowdown, SM 227–1404 MHz of 1911); working set 1.08 GB, private 1.83 GB, GPU memory 873 MiB. Two rows open from it — Q8 **the AI battle's authoritative tick costs 5 ms inside the client** (2026-09-08, `feat/q8-fixed-tick-split`: the frame log splits the fixed tick into sight / predict / host / sync and counts the loop's sleep as `wait`; the means are taken over the steady frames — a frame over 1 s is a bake, not a frame — and the header carries the viewport. Steady reading, Bystra AI 15v15, 60 s autodrive, 1920×991: fixed_ticks 10.7 ms per frame of which host 10.1 at 1.91 ticks per frame = 5.3 ms per authoritative tick; sight 0.3, predict 0.1, sync 0.2; render 15.2, hud 2.7, upload 1.6, wait 0.2, unattributed 2.9; GPU frame 27.0 ms, scene_pass 19.8 ms. The bench `battle_tick::ai_15v15_tick` (new — `WOT_MAP` picks the map, as in the game) says the same tick costs 2.2 ms headless on Bystra: the other 3 ms are the render thread and the driver sharing the cores, and the spiral — a GPU-bound 31 ms frame runs 1.9 ticks, so every millisecond of tick is paid twice. SECTIONED (2026-09-08, `feat/q8b-tick-sections`, `battle_host::TickSections` + the `tick_sections` example, off in the game): Bystra AI 15v15, 144 cover boxes, 600 warmed ticks headless — 3.16 ms per tick of which spotting_log 1.10 (35 %) and view 1.07 (34 %): the observer masks (30 × 29 lines of sight, each a terrain march plus the cover boxes) were walked TWICE per emitting tick, once for the spotting log and once for the viewer's cut, and the dedicated host walked them a second time too. Now once per emitting tick, cached on the server for the tick (`emitted_masks`; `observer_masks()` hands them back), read by the spotting log, the local cut and every remote client's cut: 2.03 ms per tick (−36 %), view 0.002. What remains: spotting_log 1.06 (52 %, one walk), sim 0.52 (its own spotting walk every 6th tick is inside), bots 0.44. NEXT cut = the walk itself: a range gate exists; the terrain march per line is the cost — march the pair once for both target points, or a coarser step past the first clear metre) and Q9 **the frame runs at a thermal-throttled clock within a minute** (the GPU load has to fall for the clock to stay — every scene_pass cut counts twice). THE COLD TRUTH (2026-09-08, `perf/q7-ab-quick`, `scripts/perf/ab.ps1`): Bystra 1080p, GPU cooled under 68 °C, each row the first and only row of its own process, three rounds, repeatable to 0.03 ms — full scene GPU frame 16.5 ms, scene_pass 11.5; no card meadow 11.0 (−0.5); no near ring 10.7 (−0.9); no shadows 10.8 (frame 13.4: the two cascades 2.3 + their taps 0.7); no SSAO 11.5 (frame 14.8: the prepass and the passes 1.6); scope 18° 8.7; the 15v15 fleet 14.3 (+2.8, frame 20.8). So the card is at the 60 Hz line COLD with the fleet in view, and the long twelve-row table (scene_pass 21.1 / 19.3 / 23.2 ms in three runs of the same code) measured the heat. The grass and the meadow together cost 1.4 ms cold, not the 10 ms the warm table said; the 10 ms that remain are the lit opaque world itself — terrain, statics, trees, water — at 2 M pixels through one fragment shader (PCF 6–9 taps, procedural detail and puddles, SSAO, fog, the specular lobe). THE FRAGMENT PRICED (2026-09-08, `perf/q9b-shader-ablation`): eight ablation bits in `ShaderDetailMask` (8–15, never in a profile, `WOT_GPU_DETAIL=<mask>` may set them, `ab.ps1 -Masks`) each remove one part of the shipped fragment — cold on Bystra, canonical 11.36 ms: material noise −0.61, the PCF cross and far cascade −0.59, local light pools −0.61, specular + env −0.42, fog −0.36, macro tone −0.16, cloud shadow −0.13, puddles 0.00; everything off 8.53 — the floor is the taps and the geometry. TWO CUTS, bit for bit the same picture (the look goldens' drift report identical to master's): the puddle noise skipped while the ground is dry (`fill > 0.001`; the scene pass ran it for every pixel of every dry frame), and the pool loop stopping at the first dark slot instead of `continue`-ing through six (`local_lights_are_a_prefix`, asserted in debug, locked on the hangar rig, the empty rig, a hole, and every blend step). A/B cold, three rounds: scene_pass 11.56 → 10.66 ms (−0.90), GPU frame 16.51 → 15.62. THE FLEET, cold (2026-09-08, after Q9b, base 10.66 / frame 15.61): full + 7v7 12.10 / 17.94; 7v7 with every hull forced to the NEAR gear 14.27 / 21.57; forced FAR 12.11 / 17.95; full + 15v15 13.42 / 19.92. The near gear costs 2.5× the far per hull — the vehicle's near LOD (its shader and its shell, drawn twice for the interior) is the next non-look cut; with 30 hulls the cold frame is 19.9 ms, over 60 Hz before the card warms. NEXT (look changes, the owner's call, each priced above): the material noise on statics and trees (0.6), the shadow kernel (0.6). Q9a REJECTED the same day: the card meadow drawn last + the tufts sorted front-to-back = +0.3 ms (B ≥ A in every pair) and four Orliny golden frames drifting at depth ties; the ground drawn first +2–4 ms (the meadow stands on it — ground shaded twice)), Q4 (CLOSED 2026-09-08, `fix/q4-audio-mailbox`: the audio engine used to sit behind a `Mutex` the main thread held for the whole control update every frame while the callback, on its real-time thread, waited — the docstring's "never blocks the cadence" was enforced by nothing. The callback OWNS the engine now; the main thread posts one `ControlFrame` per presented frame (listener, beds, engines, events with their occlusion, a gain change) into a bounded `sync_channel` mailbox with `try_send` — a full mailbox drops the frame's continuous state and carries its events to the next frame that fits, capped — and the callback drains it with `try_recv` before rendering. Lock: `the_audio_callback_path_holds_no_lock` (`audio_out.rs` names no Mutex/RwLock/lock() outside comments, the mailbox is bounded and non-blocking both ways, no shared engine handle), plus the mailbox's own unit test), Q7 (CLOSED 2026-09-08, `perf/q7-ab-quick`: the cold sandwich the MX330 needs — `WOT_PERF_QUICK=1` (3 rows, 2 × 30 frames, 12 s), `WOT_PERF_ONLY=<row>` (one row alone), `scripts/perf/ab.ps1 -A -B -Rounds` (two binaries alternated A B B A A B, the GPU cooled under a threshold before every run) and `-A -Rows` (attribution: each row first and cold in its own process). A number taken warm, or after another row in the same process, is not a number: the rows after the first read 2–5 ms heavier for heat alone), N1–N8, N9 (the owner's WSL), N11, H1–H6, R2, R3 (R1 amended above, R4 decided), F8–F11, O1, O2 (every D34–D45 PR rewrites the lock it names), A2, L1–L3, M5b/M7b/M8 (`docs/game-modes.md`). Full text in the second pass.

## 4. Lying and blind locks (rewritten in the PR that closes their row)

| Lock | Measures | Must measure | Row |
|---|---|---|---|
| ~~`look_locks.rs:48` `KEY_LIT_FRACTION 0.75`~~ | ~~a slope tilted 26° toward the sun~~ | light delivered on n = +Y — REWRITTEN with D34 (`level_field_light`) | D34 |
| ~~`look_goldens.rs:398` `OUTDOOR_DARK_FLOOR 0.0055` vs `TARGET 0.08`~~ | ~~never worse than the worst frame~~ | the target on the sunward frame — ASSERTED with D35 | D35 |
| ~~`every_sunlit_pass_takes_the_cloud_shade`~~ | ~~that the multiply exists~~ | that a patch is visible — ADDED with D36 (`a_patch_of_cloud_shade_is_visible_on_every_clear_look`); the golden-pixel form waits for D35 | D36 |
| ~~`tree_line.rs:526-586`, `battlefield.rs:2387`~~ | ~~the box contains the trees~~ | the body is not a box — REWRITTEN with D44 (≤ 120 tris, FOLIAGE, a broken top) | D44 |
| ~~`grass.rs:799` Clark–Evans~~ | ~~randomness of tufts~~ | a clump-count histogram — ADDED with D43 (`clump_counts_follow_the_occupancy_noise_not_a_lattice`) | D43 |
| `material_floor.rs` | a minimum material amplitude | also a maximum frequency (mips) | D40 |
| `every_battle_readout_sits_on_glass_at_three_to_one` | text on a plate only | also the legacy reticle numbers | U13 |
| `no_garage_string_renders_below_the_legibility_floor` | `size_u ≥ 16` | the face and the stem in px | U12 |
| `HERO_OVER_ROOM 1.5×` | median hero vs room, no UI | the silhouette inside the panel-free band | D45 |
| `t54_top_track_links_ride_the_wheels_with_only_the_horn_in_the_slot` (`− 0.075`) and `running_gear_dynamics.rs:110` | a plate 75 mm inside the tyre "rides on it"; "the top run does not move" | a clearance lock over a driven crest and ditch | J7 |
| `the_result_does_not_depend_on_roster_order` (2 bodies) | one constraint | ≥ 3 bodies, client vs server summation order | X8 |
| `tbone_ram_registers_only_when_hulls_actually_touch` (+0.5 m) | that a ram registers | the bill's spread over the sub-tick phase | X7 |
| `windmill_shelf_masks_a_hull_from_the_bridge_but_fires_over_the_crest` | the eye | the shell | S21 |
| ~~`shell_falls_under_gravity_after_firing`~~ (rewritten with S14: drop and TOF at 1000 m), ~~`…uses_deterministic_dispersion`~~ (S16: two salts, two draws; a zero salt is the old draw), ~~`fixed_casemate_ignores_turret_yaw_commands`~~ (replaced with S17: the arc) | — | — | closed |
| `cover_visuals_never_leave_the_collision_box` (one direction) | mesh ⊆ box | also box ⊇ mesh (fill) per kind | X1 |

### Q8c / Q10 — memory and the remaining sight march (2026-09-08)

These were follow-ups from block 8 on 2026-09-08. Their implementation does not close
the sustained-performance defect added on 2026-09-09 below; N9 still needs the owner's
second OS. Scope and evidence: [memory and Q8c](perf-memory-q8c.md).

| Row | Work | Closure evidence |
| --- | --- | --- |
| Q8c | Conservative height-maximum pyramid implemented before the unchanged exact LOS march; cache excluded from map identity, crater rims fall back to the exact kernel | Exact-march parity and replay gates green. Owner waived additional A/B runs before merge; no measured ms reduction claimed |
| Q10 | First attribution and cut implemented. The renderer retained 298.667 MiB of decoded leaf/bark mip chains on CPU and uploaded the leaf atlas again when setting bark. GPU views now retained instead. Source-image caches remain 128 MiB; texture GPU payload remains 298.667 MiB | GPU ownership lock and PR gate green; owner waived process A/B. The rest of process memory remains to attribute; see linked report |

Q9's near running gear remains its own follow-up. Neither this change nor Q8c changes the
appearance policy or installs WSL.

### Q11–Q16 — sustained performance and one look (2026-09-09)

**All rows OPEN.** GDD row 36 and [one-look policy](one-look-policy.md) set the contract.
The [execution and acceptance plan](sustained-performance-plan.md) carries historical
evidence, hypotheses, the experiment matrix and numerical targets. It does not certify
60 FPS today or replace this queue. Q14 continues Q9; Q13 builds on Q8c and Q10 without
claiming their unmeasured FPS gains. The owner's aiming complaint is not assigned to a
single cause before Q11 isolates it.

| Row | Scope | Evidence required to close |
| --- | --- | --- |
| Q11 | Reproduce late-battle/aiming degradation; separate heat, world age, view, CPU/GPU and waits; fill gaps in counters and repeatable camera/input scenarios | Identified release binary, raw data, controlled warm fresh/late-state comparison, attributed stalls and explicit remaining uncertainty |
| Q12 | Persistent GPU statics by fragment; bounded integration/upload after destruction | Before/after hit and salvo capture, upload bytes proportional to changed work, p99/max improvement, correct visible destruction and regression locks |
| Q13 | Measured growth in sight/reticle, terrain queries, destruction and FX; bounded local work without changing physics | Late-state cost and aiming response within the plan's targets; exact query/replay parity, resource/candidate counts and appearance checks |
| Q14 | Reduce sustained scene cost and thermal pressure, including near gear (Q9); evaluate further representations only with one-look evidence | Cold A/B and warmed full-load comparison, pass timings, clocks, appearance in motion/scope, no quality/fairness regression |
| Q15 | Full-battle 60 FPS on the named MX330 configuration | Plan's complete map/mode/duration matrix and repeatability, explicit viewport, input response, p99/hitches, late-state and consecutive-battle checks; no missing cell presented as PASS |
| Q16 | Higher FPS on stronger PCs with the same picture; review current 120 Hz cap | Named CPU/GPU/display/resolution, 120+ pacing and input evidence, unchanged simulation results, common-look comparison |

Q11 is first; it may reorder Q12–Q14 with measured justification. No timing promise is
closed by docs or unit tests alone. Use the [capture template](performance-capture-template.md).

## 5. Verification

The merge gate is `scripts/verify.ps1` (see `CLAUDE.md`); `scripts/preflight.ps1` first. In addition, for this program:

- Any renderer or profile change (D34–D45, N-, T-, F-rows) lands with an MX330 A→B→A cold
  sandwich; the budget moves per item, never fleet-wide. The picture rows land with a frame
  sent to the owner, and the frame is taken from the player's eye, once INTO the sun.
- Q11–Q16 also require the warmed full-battle evidence in the linked plan. Cold A/B isolates
  code cost; warm acceptance measures the player's sustained experience. Neither substitutes
  for the other; historical claims that a warm measurement is never useful apply only to
  uncontrolled A/B attribution. Missing target-hardware evidence leaves the relevant row open.
- Any sim number that moves (J1–J6, X-, S-, Z-rows) re-pins the replay fixtures in the same
  PR and says why; `mobility_baseline.rs` is re-recorded as a set, never a column at a time.
- Any wire change (a `StaticCoverKind` tier, a phase byte per segment, a fall-direction byte,
  the dispersion salt, the gearbox state if it rides the wire) is append-only and bumps
  `PROTOCOL_VERSION`; the yaw = 0 / bands-overlap path stays bit-identical on every replay.
- Goldens are blessed deliberately, in the PR that moves them, with the before/after in the
  message; HUD and garage goldens only from the interface session.
- Every row that names a lying lock (section 4) rewrites it in the same PR.
- Each vehicle migration (K3) ends with the close-up review under the model-logic bar and the
  K0 overlay; each destruction class (Z8–Z13) ends with a `destruction_showcase` frame looked
  at before merge; the representative fragment (W4) ends with the owner playing it.
