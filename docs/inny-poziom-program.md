# Inny Poziom — The Second Pass

Approved 2026-09-01. The owner named ten things that read as unfinished — the geometry kernels and
the fleet, the picture, the physics, Honest Steel, the armour, the tracks, the flora, the shot, the
fleet's identity, the HUD — and asked for all of them to be taken "to a completely different level".
Eight read-only audits of the code and a look at the review frames in `target/` answered with one
diagnosis, and this document is that diagnosis turned into a register and a wave plan.
[art-direction-program.md](art-direction-program.md) is the model: the register below names the
evidence, the wave, and the lock that closes each row. When the register is empty this document
becomes history and the policies it edits stand alone.

The same day the owner added a second list — the driving camera, shots refused or landing for zero,
the enemy hard to read in the scope, the trajectory, a hit that is only a number, frame drops, the
garage, the T-54 itself, the hull over a small hill, buildings and terrain, sky and weather, water —
and granted this program full design authority: "if something needs redesign, go ahead; you take
responsibility." Eleven more audits answered. The rows they produced are in the register below, and
the redesigns they justified are in the decisions, stated as taken.

## The diagnosis

**The simulation is mature and test-locked in nearly every area named. What is thin is the layer
the player sees, and the rollout.** The same disease repeats in nine of the ten areas: a good thing
was built once — for one vehicle, one map, two species, two turrets — and never made a rule.

| Built once | Rolled out to | Evidence |
|---|---|---|
| Hybrid geometry (27 565 triangles, 69 parts) | 1 of 8 vehicles; the next best is 3 734, the Centurion 1 960 | `cargo test -p vehicle_forge --test shipped_cost`; `vehicle_forge/src/mesh_source.rs` hard-codes `T54_1951 → Hybrid, _ → Procedural` |
| Drzewa 3.0 species (6 grown) | 2 placed (Oak, Bush); Poplar, Willow, FruitTree, Pine stand nowhere | all five `map_forge/blueprints/*.map.ron`; Orliny's view named `pine_belt` contains no pine |
| Armour↔metal parity lock (≤ 10 mm) | 2 turrets of 8 (T-54, Tiger I); hulls, sides and decks unmeasured on 6 vehicles | `vehicle_build/tests/t54_turret_armor_lock.rs`, `game_core/tests/tiger_i_benchmark.rs` |
| Destructible cover authored in anger | 1 map of 5 — Ostrogorsk 39 objects; Prokhorovka 14, Mazurski 9 | map blueprints, `terrain/src/battlefield.rs` HP table |
| Baked contact AO | 1 vehicle of 8 | art-direction D8 |
| Weather timeline | 1 map of 5 — `static_program = map != BystraValley` | `weather_timeline.rs:56` |

The second list added a third pattern: **one global constant standing in for a property of the
thing.** One rate limit (1.4 rad/s) is every hull's suspension; one drowning depth (1.5 m) is every
hull's fording depth; one dispersion factor is three; one rain wind is hard-coded while grass reads
the storm heading; one "0" is drawn for five different outcomes.

Two mechanisms keep it invisible:

1. **"DONE" in the roadmap blinds the register.** `docs/ROADMAP.md` lists fire feel, destruction and
   the flora stack as done, so none of them has a defect register; the only register in the repo is
   the light's (`art-direction-program.md`). A system without a register cannot accumulate debt on
   paper, so its debt accumulates on screen.
2. **Documents lie in the details.** `docs/vehicles/t-54.md` quotes a 22 000-triangle cap against
   29 000 in `t54.rs` and 27 565 measured; `vehicle-forge-policy.md` marks UVs and normal/AO bakes
   "done" when one kernel authors UVs; `procedural-kernel-program.md` M8 "migrate other vehicles"
   has read "in progress" since 2026-08-03 with zero migrated; `hybrid.rs` still describes an SDF
   composition deleted 2026-08-02; `scene_build/src/backdrop.rs` calls its ring "FAR bakes that at
   kilometres read identically" while the ring starts 40 m past the border; the roadmap's creed
   promised "dispersion ~0.1–0.3 mrad" while every gun ships 1.9–3.4; the roadmap's Movement line
   claims "per-wheel suspension with sprung attitude" while the authoritative hull is a rigid beam
   and the springs live in the presentation layer.

## The three rules this program is built on

1. **Measure the eye, not the model.** Every register row names a frame or a number, and the lock
   that closes it. No row closes on a description. Where no metric exists yet, the row says so and
   the first PR of its wave derives one from a frame judged good — never from the broken frame
   (the lesson of art-direction D31).
2. **Rollout before invention.** A capability counts as landed when it stands on every vehicle,
   map or species it applies to. No wave starts a new capability while the previous one is on
   one-of-N. This is the fix-as-rule rule at program scale.
3. **One truth per system.** Where the audit found N models of one thing — fifteen track models,
   three ground samplers, damage modelled three times, twenty-two sets of HUD floats, two
   penetration resolvers, two cloud fields, three wind constants — the wave that touches it leaves
   one owner and every consumer reading from it.

## Decisions taken with this program

- **No physics engine.** `crates/runtime/physics` is ~2 800 lines of deliberate custom code
  (SAT footprints, a Jacobi impulse solver, heightfield contact); `parry3d` has one call site with
  no production caller. rapier3d would replace the integrator, contacts and friction — the parts
  the repo already rebuilt on purpose (`docs/physics-policy.md`) — and would not touch a single
  item the owner complains about: belt geometry, sag, link-to-wheel wrap, ruts, per-wheel travel
  and belt scroll are presentation and stay hand-built. The replay-exact locks need cross-platform
  bit determinism; rapier's `enhanced-determinism` promises same-binary determinism only.
- **The authoritative hull becomes a sprung mass on its road-wheel stations (G7).** The rigid beam
  plus a global 1.4 rad/s attitude ramp is replaced by per-station spring/damper forces with travel
  limits, integrated semi-implicitly at the fixed 60 Hz tick in f32 — the same arithmetic the
  presentation spring already uses, so determinism and replay-exactness survive. The force model,
  belt-drive steering, SAT contacts and the two-phase tick order stay verbatim; the beam's convex
  envelope survives as the springs' rest-length source (its trench bridging and crest overhang are
  correct). Weight transfer, crest compliance, landing rebound and per-vehicle wallow fall out of
  mass and station layout with no new knob. Spring state enters the snapshot append-only.
- **The shell falls at 9.81 m/s².** The old 12.0 was undocumented in every policy file; an honest
  tank drops its shell at the real rate (`SHELL_GRAVITY_MPS2`). The aim solver integrates the same
  step, so it adapts. The HULL keeps the 12.0 its contracts were measured under (climbing envelope,
  mobility table, trench bridging, drive replay) until G7 rebuilds the hull model and re-measures
  them as a set — one gravity then, not two constants forever.
- **The dispersion creed is rewritten, the data stays.** The guns' 1.9–3.4 mrad is a hard maximum
  radius with a √ centre-biased draw — already tighter than WoT's 3.2–4.2 and without its tails.
  Bringing guns to 0.1–0.3 mrad would give a 12 cm cone at 400 m and delete aim time, bloom and the
  aiming circle. The roadmap line now says what the envelope is.
- **Fire is never refused for geometry.** A shell into a crest or over a wall is a legitimate play
  (HE over cover). What changes is that BLOCKED is drawn identically in both camera modes, the
  elevation-limit case gets its own glyph, and a casemate's yaw limit enters the reticle's arc.
- **Trees never cut the camera boom.** Trunks and shelterbelts leave `camera_obstacles`; the eye
  passes through canopy with a fade, as it does in every shipped tank game. Buildings, walls and
  terrain still cut, with a rate-limited inbound and a velocity look-ahead so the eye never clips.
- **No rigid debris.** Collapse is client theatre (particles, cards, dust, sound) over the
  replicated phase swap. Debris that could block a hull would have to be replicated, and the
  honesty doctrine says scenery never blocks gameplay.
- **Buildings become a grammar-driven, instanced kit of parts (B3).** Today every mullion is unique
  triangles merged into a 1 000 m static buffer. A kit (wall bay, pierced bay, door bay, roof
  segment with overhang, ridge/eave caps, chimney, dormer, downpipe, plinth, dirt apron) placed by a
  split grammar and drawn through the existing instanced path costs less than today and reads more.
  The blocking volume stays the wall-and-roof mass; overhangs, chimneys and downpipes are
  scenery-class (never block gameplay, ≤ 0.4 m past the footprint, and a spotting lock proves a
  cornice cannot hide a hull). SDF/CSG per building is refused: CPU-heavy at map swap and hostile to
  the golden-hash gate.
- **The sky is a precomputed scattering LUT, not a two-stop gradient (N6)**, which also yields the
  sun's colour ladder by elevation instead of hand-typed keys. Clouds keep their 2D FBM but gain a
  lighting model (Beer–Lambert toward the sun, powder, forward lobe); true volumetrics are refused
  on the MX330. **One cloud field** feeds the dome and the shadow map. Rain becomes screen-space
  streaks plus a real wet response. Weather evolves on every map. One wind field.
- **Fording depth is a vehicle property and drowning is a replicated countdown (H1, H2).** The
  fleet-wide 1.5 m and the silent engine death are replaced by `ford_depth_m` from each dossier and
  a `submerged_s` lane mirrored by the rack-countdown widget. Bots derive their deep-water line
  from it.
- **Water reflects the world at half resolution (H5).** Screen-space reflection for the water pass
  only, measured before it ships; a planar pass (the scene twice) is refused on the budget.
- **The damage number is demoted, the world promoted (S7).** A literal "0" is never drawn; the
  outcome word is. The number's ink may not exceed twice the lit area of the penetration FX.
- **GPU uploads are dirty-range writes into persistent buffers (Q1).** Three whole-mesh
  `create_buffer_init` paths (ground, meadow, statics) are the shape of every remaining hitch.
- **No external UI crate for the client.** egui brings its own look and font stack against the
  one-look policy and a second pipeline against the single HUD draw call the MX330 budget is built
  on. It is allowed in `apps/editor` and `apps/tools`, where no look policy applies. The client's
  layout layer is written inside `ui_kit`.
- **The T-54 goes to the bar before the fleet copies it (K0).** Seven migrations reproduce the
  benchmark's parts; the benchmark's own defects (sprocket, wheels, links, dome length) would be
  copied seven times. K0 and K9–K15 land before the first migration.
- **Terrain and buildings are in scope now (W5)**, by the owner's second list. The earlier "recorded,
  not rebuilt" (K8) is retired; K8's inventory became the B and T rows.
- **Crew proficiency stays pinned at 1.0** (R4) — progression is proof, never power.
- **Night, snow and lightning stay absent.** No shipped map is a night or winter map; adding one is
  a map decision, not a renderer row.

### The owner's design brief (2026-09-02)

The full design document is `docs/game-design.md` (adopted the same day, with its reconciliation
table); this brief is its mid-playtest precursor and the two agree.

Given mid-playtest, after A10–A12. Recorded as the owner's DECISIONS; each says what the repo
already has and which rows carry the rest.

- **Fire.** "A shell that penetrated does not vanish: it flies on through the interior as a ray,
  losing energy, damaging what it cuts — racks, engine, fuel, crew. The HP pool stays (this is
  half-arcade, not War Thunder), but through the interior not every 300 damage is equal." —
  **Standing** (Honest Steel): every vehicle carries a damage layout of components, a penetrating
  round continues through the plate, exits, spalls, wounds crew stations; the pool stays. Owed:
  nothing structural; the theatre of it is the S lane.
- **Spotting.** "Keep the idea, throw out the table. A bush is a VOLUME with an optical density;
  the line of sight from the observer to the target's spotting points integrates occlusion the
  way a shell integrates armour. One raymarch, two questions. View range and camouflage are two
  scalars that move the threshold. Deterministic and explicit in the UI: I see my visibility
  budget and what eats it. Sixth sense for everyone from the first battle. Auto-spot at 50 m
  stays — a tank invisible behind a bush at the muzzle is absurd." — **Not built.** Today:
  per-vehicle view range (400/440), LOS through cover boxes (a TreeLine blocks, scenery never
  does), no camouflage stat, no density, no budget on the HUD. The **V lane** below.
- **Movement.** "A tank must weigh. Multi-raycast suspension gives the hull's sway on braking and
  over bumps — what tells the player '45 tons' without a HUD. Terrain resistance from the ground
  material." — **Planned as G7** (W2, first): the sprung hull in the sim; the ground's grip per
  material partly stands (the mud belt). G7 owes the sway.
- **Aiming and camera.** "Third person plus sniper — exactly WoT, because that is why WoT is
  more approachable than WT. The reticle blooms with hull and turret motion and converges with
  aim time. One reticle, the server's — with an authoritative server and rewind there are not
  two truths. Dispersion concentrated in the centre, not uniform." — **Standing**: server-owned
  dispersion with bloom and aim-time recovery, replicated to the reticle; the centre-biased draw
  (`r·u²`, floor 0.15); one penetration resolver shared by reticle and server (A1); the gun
  lands on the crosshair without controller lag (A10).
- **Progression.** "Horizontal, in the WWII band: class 1–4 (roughly 1939–40, 41–42, 43–44, 45),
  matchmaking ±1. The tree stays — a collector's hook veterans love — but the whole band is
  ~100 hours, not a thousand. Crew: one counter per tank, capped at ~10 hours, small bonuses.
  Equipment: 3 slots from one set, free swaps. Consumables free. No credits — they existed to
  sell premium. There is XP and there are tanks." — **Product decisions**: the repo has
  nations/lines/tiers, the tech tree and the loadout editor, a crew proficiency pinned at 1.0
  (R4), no economy (as decided). Owed: the class bands and the crew counter — the R lane,
  after W2.
- **Honesty as a feature.** "After every hit the game says what happened: pen 150 vs effective
  162, the angle, the point. In the garage an armour inspector with the same raymarch — point,
  see thickness. A battle log with every shot. Replays from the server's input log. WoT players
  use three external tools for this; here it is a screen in the game." — **Partly standing**:
  the scope's verdict with the millimetres (sniper mode), replays as the sim's own fixtures.
  Owed: the inspector's thickness-at-a-point through the shared resolver, the per-shot battle
  log screen, the in-game replay viewer — the **L lane** (the ledger) below.

### The two bars (2026-09-02)

Set by the owner the same afternoon, above every lane: **"the best-shooting tank in history, and
the best physics and destruction in history."** Neither is a row; each is a bar with an instrument,
and a row in its lanes closes only when its number moves. Both instruments are owed — the first
lands with S1, the second with the next Z row — and are named here so the rows build toward them
rather than past them.

- **Bar 1 — the shot.** Instrument `shot_probe` (a `client` example probe, then a test): every
  gun of the fleet fires once at rest and the probe reads the FIRST frame after the trigger. The
  bar: all channels are present in that frame — flash and its light (S1), smoke, the dust ring,
  the barrel stroke, the hull rock, the camera nudge, the report's onset — every one scaled by
  the round's recoil momentum (S3), monotonic across the fleet from the 75 mm to the 128 mm; the
  player's own shot leaves the muzzle with no network delay (standing); the reticle's verdict is
  the server's (A1); the turret arrives time-optimally (A10) and the gun holds through the pitch
  of the hull (A12). Rows: S1, S4, S5, S7–S12 owe their channel; a shot that is late, missing a
  channel, or the same for two calibres is the bar failing, not a polish item.
- **Bar 2 — the world after five minutes.** Instrument `world_marks` (a deterministic 7v7 bot
  battle of five minutes on each shipped map, replayed from its input log): the probe counts the
  marks the battle left — craters and their gameplay slopes, ruts, felled trees and stumps, walls
  and buildings brought down and their mounds, scorched ground, wrecks — and the physics the
  battle showed: hull settle and rock under fire and braking (G7, "45 tons"), track–ground
  contact, wreck and rubble as terrain. The bar: a per-map floor of marks that only ratchets up,
  and no mark that is a decal where it should be a slope. Rows: Z1–Z3 done; G7, the rest of Z,
  the B lane (buildings) and the P lane owe their number.


### The four-mind audit (2026-09-02)

A read-only audit of the whole workspace by four evaluation functions — Reed Richards (is it true),
Victor von Doom (is it mine, do I control it), Tony Stark (does it run, does it feel, can I demo it),
Mysterio (does the illusion hold) — lives outside the repository at `../wot-audit/AUDIT.md` (Polish;
the four sections plus a convergence). Its findings enter this register as rows: **Q7** (rewritten on
the cold rotation: no drift, a thermal budget), **S13** (the own shot presented from the snapshot),
**V0** (the eye and the shell march differently; scenery lies both ways), **O2** (the picture's metric
lies toward "fine"), **N9** (float determinism is per OS and the door hashes the map bit for bit),
**N10** (the local host filters differently from the remote one; seats by hash order). Its three next
things, in order: the frame at the sustained clock (Q7's diet and lock), the own shot in the frame
after the trigger (S13 with `shot_probe`), the binary answer on cross-OS determinism (N9) with the
local/remote parity (N10) before the first playtest with people. Its first frame: Prokhorovka's
golden evening, a T-54 from the player's seat, the camera into the sun across the balka.
### Order after the owner's directive (2026-09-02)

The owner's message of 2026-09-02 16:25 (game-design.md reconciliation row 9): terrain, trees and all flora are PART OF THE GAMEPLAY and must read well, naturally and at real quality — ground and grass included — with maps redesigned, rebuilt or improved where needed; and weather must neither spoil legibility nor change the game. Consequences for the queue: after W1, N9 and N10, the flora and terrain rows of W4 (F4 density, F5 variety, F7 near rungs, T3 tiling to the horizon, T4 cliffs and slopes, H5 the river, O1 the meadow moiré) and the map roots of W5 (T1 the 5 m grid — the root of C4 and T3 — T2 erosion, T5 roads) move AHEAD of W3 (Forge 2.0); every one of them closes with a frame looked at, not a number alone; the weather rule is N11 and lands first, as a gate rule, because it costs nothing and stops a class of PRs.

## The queue and the fundamentals (2026-09-02, evening)

The owner asked for two things written down, not said: what to do about the engine's fundamental problem, one sentence each, and everything that is still to do. Both live here; the register below carries the detail per row.

### The fundamental problem (the owner's special task)

**The engine has two worlds and one rule policed by tests, instead of one model of the world's surface.** The sim reasons in boxes, capsules and heightmap samples; the renderer draws procedural meshes built by other crates; both take the same blueprints, but nothing owns "the surface of the world" as one queryable thing (point → nearest surface, normal, material, thickness, owner). The doctrine "what blocks the shell blocks the eye" is a POLICY enforced by ~30 locks, not a PROPERTY of one geometry — so every presentation system keeps a private, approximate truth and private lighting conventions. One day's symptoms of that one absence: hit marks as FX stickers (Z5), wall wounds as squares 4 cm proud of the plaster (Z5), a hit seat 33 cm inside the hull snapping onto an interior horizontal (Z5), a single-skin hull whose hole showed the running gear and the ground (Z6), an aperture lighting its own rim with no cosine (Z6), the eye and the shell marching the terrain differently (V0), two springs and three gravities (G7), the client predicting the server's rule from its own copy (S13). One layer down, in the engine: materials are `if (role < 1.5)` chains and number tables in WGSL, so every change of look is a shader edit and a byte-exact golden re-bless per machine; the frame's cost is decided per fragment (uber-shaders, the MX330 fill-bound, diet step 2 measured as a loss), not per asset with tiers; the scene is a list of objects rebuilt every frame with whole-mesh re-uploads (Q1); network determinism is assumed and never proven on another OS (N9).

What to do about it, one sentence each:

1. **One model of the world's surface**, built once from the blueprints, from which the sim DERIVES its boxes and heightmap and the renderer its meshes, and which marks, apertures, sight and shells QUERY (point → nearest surface, normal, material, thickness, owner) instead of guessing.
2. **The vehicle as an armour solid with volume** (the breach contour and thickness already are), from which the skin, the interior and the cuts FOLLOW rather than the other way round — K3 (the seven hulls onto that model) is the largest gap right after this one.
3. **Materials as data with cost tiers** (a role table → parameters, LOD per asset) instead of if-chains and numbers in WGSL, so a change of look is not a shader edit with a golden re-bless, and the frame's cost is decided per asset, not per fragment.

This is a program, not a PR. It enters the queue above by the owner's decision, to be asked at the planning of Destruction 2.0 and Forge 2.0 (K3 and the surface model are their foundation). **Asked and answered 2026-09-05 — the owner delegated the decision ("masz pełną decyzyjność"), and the decision is: K3 proceeds on the current split, vehicle by vehicle, with every hull solid built FROM the blueprint's armour facets (as the T-54's tub and upper hull already are) so a later armour-solid model derives from the same data rather than from seven skins; remedy 2 (the vehicle as an armour solid with volume) is scheduled as its own program after K3 and before Destruction 2.0's B-rows, where the wall-segment armour needs the same surface model. Reason: K3's value — seven vehicles at the benchmark bar — does not depend on one surface model (the T-54 reached the bar without it), and the program would hold the fleet for weeks.** Until then every new presentation system QUERIES an existing source of truth (the mesh contact index, the aperture list, the heightmap) and never builds its own; and a colour-coded diagnostic render (material family, damage index, front facing, local position) comes before the second hypothesis.

### Everything still to do, in order (91 open rows)

Throughput today: 16 rows closed and 38 PRs merged in one day with the pipeline full; the ceiling is the gate on one laptop (10–15 min per PR gate, 25–30 min the full one, the box idle for every cold measurement), not the writing of code. Estimates assume continuous sessions.

| Order | Block | Rows | Estimate |
|---|---|---|---|
| 1 | **W1 remainder + the Q lane** | Q7 (diet steps: cards behind `discard`, Q1's per-bucket uploads, then the opt-in frame lock — the vehicle shader step was measured as a loss), Q1, Q2, Q4, O2 (with Q7's next golden cycle), S2, S6, Z4, A2 (the three docs rows in one PR), N11's picture half (fog fairness at max view range, per-profile legibility golden), N4 (weather frozen on four maps) | 2 days |
| 2 | **N9** cross-OS determinism | needs `wsl --install -d Ubuntu` on the owner's machine first; then `server` + `cargo test -p map_forge -p sim` under WSL, a `u64` golden of every `TankState` after 600 ticks on both systems; a deterministic libm behind `game_core::math` if they differ | 0.5–1.5 days |
| 3 | **Flora and terrain** (the owner's directive, ahead of Forge 2.0) | ~~F4, F5~~ (removed from the plan 2026-09-04, the owner), ~~F7~~ (F7b closed 2026-09-04), ~~T3~~ (closed 2026-09-04), T4, T1 (the 5 m grid — the root of C4 and T3), T2, T5, H5, O1; every row closes with a frame looked at | 4–6 days |
| 4 | **Destruction 2.0** | B1–B6 (the building kit: eaves and ridges, ground connection, a kit of parts, brick and plaster coursing, a ruin with form, variety) + destruction PER WALL SEGMENT with four states and the material deciding (game-design §13.4), walls as thickness in the armour table, rubble as low cover; with it V0 (one terrain march for eye and shell) and Z7 (shells ricochet off the ground); still owed inside closed rows: the ground's HE scorch as a splat not a stamp (Z5), a wall close-up in the showcase (Z5), `shot_probe` (S13) | 7–10 days |
| 5 | **W2** driving, track truth, water, spotting | G7b, K10, C1, C2, C3, C4 (after T1), G1 → G2, G3, G4, G5, G6, H1–H4, R1–R3, V1 → V2, V3, V4 | 5–7 days |
| 6 | **W3 Forge 2.0** | ~~K7~~ (closed 2026-09-05: the dossier, the forge policy, the kernel program and four docstrings say what the code measures; three anchors pinned) → K0 (instrument landed 2026-09-05; the same evening the side and front views were TRACED from a public-domain drawing — R6, the T-55 schematic — with `scripts/refs/trace_silhouette.py`: side 0.817 / front 0.865 / plan 0.951 against the drawing, floors 0.81 / 0.86 / 0.94; the drawing found K16–K18, so the 0.95 bar is now the K-block's to earn, not the outline's) → ~~K1~~ (closed 2026-09-05: every vehicle is a `VehicleDescription`; the description declares its `Fidelity` and `LodStrategy`, the forge reads them, and `seam_lock.rs` proves no seam file names a vehicle and no bake moved a byte)/~~K2~~ (closed 2026-09-05: the T-54's hull, plates and fittings left the `solid` kernel for the part library in `vehicle_build`, byte-exact; `kernel_purity` greps the kernels for vehicle-named files) → K3 → K9, K11–K15 → K4/K5/K6 → P1–P3 · **Added 2026-09-05 (the owner's night notes): K22 (the tracks bar) and K23 (the gun bar) ride with each vehicle's K3 pass, the Tiger I next; K24 (the presentation bible) precedes K6 and every material/texture row** | 10–15 days |
| 6a | **Forge 2.0 acceleration** (the owner, 2026-09-05 night: "zanim zrobimy wszystkie pojazdy … wieki miną i miliony na tokeny wydam" — accepted) | ~~step 1~~ `tools bless --vehicle` (one run re-records the bake-hash row in `vehicle_recipes/goldens/bake_hashes.txt`, the studio tiles, the asset) + `scripts/verify-vehicle.ps1` (the data-PR gate, minutes) — landed 2026-09-05; step 2: a vehicle is a DATA PACKAGE — ~~2a the Rust twins of the RON blueprints retire~~ (landed 2026-09-05: six `#[cfg(test)]` fixtures and the transitional `parsed_ron_equals_rust_fixture` lock deleted; the bake-hash goldens were always the outer judge); ~~2b `reference.ron` replaces the `packs_*.rs` literals~~ (landed 2026-09-05: `vehicle_forge/reference/<slug>.reference.ron` per vehicle, the five literal files gone, the registry carries no pack constructors; a data PR edits the file); 2c the per-vehicle benchmark test files become generic gates; step 3: `tools fit --vehicle` fits the blueprint to the traced outlines under the locked anchors + a scan-line edge reader (measurements as text); step 4: the Tiger's last recipe pieces as part classes (slab hull with shelf, welded box turret, gun group) for the whole German line; step 5: tracks and guns per FAMILY | 5–7 PRs, then ~2–3 h per vehicle instead of a day |
| 7 | **W4 remainder** | N2 → N3 → N7; N1, N5, N6, N8; H6; L1, L2 | 3 days |
| — | **U lane** → its own program since 2026-09-05: `docs/interface-program.md` (the owner: the whole interface is rebuilt, not repaired) | F0–F6 (foundation) → H (battle HUD) → P (shell) → G (garage); U1–U11, V3, L1 and L2 are absorbed there row by row (see each row's "Closes when") | ~38 PRs, 15–18 days |
| — | decided, no work | R4 (crew proficiency pinned at 1.0) | — |

**2026-09-02, late evening — the owner's call: block 3 (flora and terrain) starts NOW, ahead of blocks 1 and 2.** Nothing in blocks 1–2 gates it: N9 waits for WSL on the owner's machine either way, and Q7/O2's golden cycle simply follows the flora re-blesses instead of preceding them. Order inside the block, by dependency and by what frees the frame first: F7 (every species on the ladder — the far cascade stops paying for statics trees) → ~~F5 → F4~~ (both removed from the plan 2026-09-04, the owner's call, with the pine retired the same morning) → ~~T3~~ (closed 2026-09-04, the material fetch) → T4 (shader work, no grid dependency) → T1 measured → T2 → T5 → H5 → O1. Every PR ends with a frame sent to the owner. First PR landed: F7's free-standing half (see the row).

Dependencies that decide the order and prevent double work: Q7 with O2 (one golden cycle, not two); G7b before C3; V0 before V1; G1 before G2–G6 and K10; T1 before C4 and T3; K0 → K3 → the K0 block (K9, K11–K20) → K4–K6 → P1–P3; N2 before N3 and N7; U2 before the rest of U. After each merge a two-minute smoke (`cargo test -p client -p battle_host --no-run`) and once a day the full gate on master, because two green lanes can still sum to a red master (S10 + G7 did, 2026-09-02).

Machine facts that no agent changes: the cold measurement needs the box idle and rested to ≤ 60 °C (the MX330 throttles inside the probe's own 100 s); two worktrees on one target dir hand each other's binaries around (the sandwich runs copies built in a private target); the playtest with people and the owner's visual decisions (W4, W5, the K0 reference) sit on the critical path.

## Defect register

Columns: the defect, the evidence, the wave, and what closes it. IDs by area: K kernels and fleet,
G tracks and ground, C camera, A aim and sight, Z destruction, P armour, F flora, S the shot,
Q the frame, R roles, U interface, B buildings, T terrain, N sky and weather, H water, O picture.

### K — kernels and the fleet

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| K0 | **No reference-overlay gate.** `t54_silhouette.rs` rasterises the turret into its own bounds (proportion only); `review_images.rs` takes no reference input; the Blender overlay is a manual loop. The dome's +45 mm (K15) is exactly what such a gate catches | `vehicle_build/tests/t54_silhouette.rs:15-46`, `docs/vehicles/refs/t-54.md:48-52` | W3 (before migrations) | own-drawn outline polylines (side/front/plan) from the dossier drawing in RON; CPU raster of hull+turret+gear; IoU ≥ 0.95 per view, per vehicle, as a gate. **Instrument landed 2026-09-05 (PR #693):** `vehicle_forge::outline` (loops per view in RON, CPU raster of the bake plus its instanced running gear at 1 cm, IoU), `outlines/t54_1951.outline.ron`, `tests/outline_gate.rs` (Locked gates, Target reports as debt), `cargo run -p tools -- outline-overlay --vehicle t54_1951` (grey agreement, red bake proud of the drawing, blue drawing the bake misses, plus 10 cm slide sensitivity). The T-54 reads side 0.844 / front 0.889 / plan 0.951 against loops drawn from the dossier's tables, with the dome, the gear ends and the furniture placed from the blueprint — which is why all three views are `Target`. The row closes per view when the owner traces R1 over the overlay and the view flips to `Locked` at ≥ 0.95 |
| K3 | **Seven vehicles are unbuilt.** Bake sizes: T-54 27 565, Tiger I 3 734, Panther II 3 000, Jagdtiger 2 864, Tiger II 2 828, IS-3 2 206, T-34-85 2 064, Centurion 1 960. A Centurion turret is a 5-station ellipse ring from a scale table shared by four vehicles (`turret_fittings.rs:481-521`); a mantlet is a 3-number tuple; there are no fenders | `shipped_cost`, `centurion.rs` (54 lines) vs `vehicle_build` (4 566 lines) | W3 | each roster vehicle carries every part class of the fleet part library its dossier lists (hull solids with armour angles, lofted turret, revolved mantlet, fenders, hatches, grab handles, tow cable, vision blocks, stowage, exhaust, lights, weld seams) — an inventory gate per vehicle — and passes the T-54's close-up review set (`closeup_probe`) under the model-logic bar, and K0's overlay gate. **Scaffold landed 2026-09-05 (PR #697):** `vehicle_build::PartClass` (34 classes, `PartClass::of(key)`), `inventory/<slug>.inventory.ron` per vehicle citing the dossier section per class, `vehicle_forge/tests/part_inventory.rs` (the T-54 locked and complete at 30 classes; the Tiger I lists 23 from its dossier, the Tiger II 15, the Panther II 11, the Jagdtiger 10, the IS-3 and the Centurion 9 — all debt, every row citing its dossier section; the T-34-85 reports `Pending` because its dossier still reads `Needs dossier` — that research pass is its first owed step). **K3 begins with the Tiger I, data first (2026-09-05, the owner: "dane najpierw, potem części"):** the blueprint re-sourced to the STT 1944 sheet (K19/K20 data halves), the recipe golden and the studio tiles re-recorded, the K0 views up to 0.879 / 0.925 / 0.924. Then the parts, piece by piece: the Tiger's recipe is split into its five builders as named `Recipe` parts (slab, deck, details, turret, gun) that the description welds after the merge — byte-exact against the recipe golden (`seam_lock`), with the recipe's cavity bands scoped per submesh — so the part library replaces one piece at a time and every piece still standing is the recipe's own geometry. **The first library parts landed the same night (K3-2b):** the Tiger's `visual.ron` authors `fittings` from the sheet (bow hatches at z +1.69, x ±0.87 — the driver's and the radio operator's, a second bow hatch appended to `FittingsVisual`; the loader's hatch at x −0.96, z +0.98; the headlight at x −0.52, y 1.59; the bow shackles at x ±0.88), `vehicle_build::fitting_parts_for_blueprint` builds them for any vehicle whose visual file authors fittings, and the recipe deck leaves its own out (`DeckOmit`). The Tiger carries 4 of its 23 classes (Hatches, Headlights, TowHooks, Cupola — the lid; the drum is still the recipe's); a mixed sketch is pinned by its own golden in `seam_lock` and pays the class cost budget. Next: the guards and the upper-hull tier, then the welded turret as a part class. The manifest is fleet-wide (a sketch's is its three `Recipe` parts) |
| K4 | **No mesh boolean.** Every kernel is convex-only (`solid/convex.rs:5`, `sweep/lib.rs:77`, `panel/lib.rs:49`). Apertures are faked: the embrasure is a Gaussian dent (`t54_turret_loft.rs:49-56`), hatches are drums on a roof, the grille well is boxes. CSG exists in `sdf` and is unmeshed and dead (0 construction sites of `PartShape::Cast`) | `vehicle_build/src/part.rs:128,152` | W3 | a `cut` operator (convex tool subtracted from a solid or loft, watertight result) locked by a manifold + volume-delta test and used by at least one hatch and one embrasure on at least two vehicles; `sdf`/`sdf_mesh` either become that operator's path or are deleted |
| K5 | **No edge topology, so no fillets or rolled edges.** Chamfer only on axis-aligned boxes; the general pass was withdrawn (`solid/src/t54_fittings.rs:14-21`); `chamfered_prism` bevels 4 of 12 edges; loft caps are flat fans | `vehicle_geometry/src/builder.rs:24-58` | W3 | edge adjacency in the mesh contract; a fillet operator under the roundness law (segments from radius); locked by a facet-angle bound on filleted edges |
| K6 | **No per-part UV or bake.** One kernel authors `uv0` (`solid/convex.rs:161`); the rest are triplanar; `mesh.rs:78-80` claims otherwise. Normal/AO are per-role noise (`material_synthesis.rs`), and the synthesis is tuned to invisibility because it has nothing real to show. Cast grain on the T-54 is AO bands only (`t54.rs:340-344`) | `vehicle_forge/src/artifact/material_synthesis.rs` | W3 | every kernel output carries authored UVs (a test over kernel outputs); per-part normal + AO bake with a golden; cast grain as a baked normal on cast roles; `vehicle-forge-policy.md` row corrected |
| K9 | **The sprocket does not engage the track.** Teeth are 7.7 mm nubs proud of a ⌀463 drum, tip r 0.231 inside the belt (real tip r 0.341 passes through the shoe window); the ring is 44 mm wide against 120; the 40 ring bolts sit entirely inside the ring's metal (x 0.260–0.280 within 0.240–0.284, r 0.186–0.218 within 0.181–0.224) — 960 dead triangles no lock looks at; `gear_t.rs:776` pins the wrong OD (`reach ≤ 0.268`) | `vehicle_geometry/src/running_gear_end_wheels.rs:197,220,242-249` | W3 (K0 block) | teeth to r 0.341 through closed windows cut in the link plate (K4's `cut`), ring 120 mm, bolts proud; gate: tip ≥ plate outer r + 0.02 and a ray through the window; fleet rule "a fastener's AABB is not contained in what it fastens" |
| K10 | **The top run is rigid above travelling wheels.** Per-wheel travel is a client residual clamped −0.08..+0.20; a wheel at +0.20 puts its rim 0.18 m through the 0.90 run, at −0.08 the run floats 10 cm above the wheel it rests on; `running_gear_dynamics.rs:109` locks the defect ("the top run does not move") | `vehicle_geometry/src/running_gear_place.rs:255`, `client/src/vehicle/render_frame.rs:297-322` | W2 (with G7) | carrier height = wheel centre + lift; lock: a wheel lifted +0.15 raises the links over it ≥ 0.12 and no link lies inside any wheel radius |
| K11 | **Road wheel dimensions are wrong and the "holes" are sectors.** Assembly 360/157/45 mm against the drawing's 423/185/53; holes are the gaps between straight ribs, and the lock measures metal fraction only | `t54_1951.blueprint.ron:92`, `running_gear.rs:285`, `road_wheel_faces.rs:91-110` | W3 (K0 block) | `DimensionKind::RoadWheelWidth` 0.423 ± 0.01; 24 round punched holes under the roundness law with a hole-rim lock |
| K12 | **Links are seven box prisms** (plate, two 18 mm flat eye bars, two ribs, a 32×56 mm horn, backing; 84 tris) with no pin, no windows; the idler crank is a stub pointing straight down; the idler web ring is 90 % buried in the rim | `running_gear_belt.rs`, `running_gear_end_wheels.rs:41-47,123-150` | W3 (K0 block) | OMSh link: cylindrical eye barrels, windows beside a 50 mm horn, pin ends; idler crank pivoting aft; part-inventory rows and a through-opening ray test |
| K13 | **The world-space lock rule is partial.** The three 2026-08-09 P1s are fixed (`gear_t.rs:936,989,1037`, `handedness.rs`, `t54_nose_honesty.rs`), but fittings handedness enumerates 5 named parts, not every asymmetric part, and no rule says a fastener must show a face outside what it fastens (K9's bolts prove it) | `vehicle_build/tests/handedness.rs`, `fleet_running_gear.rs:115` | W3 (K0 block) | handedness over every part whose mesh is asymmetric in x; the fastener rule as a fleet walk |
| K14 | **Fuel tanks and bins float on the fenders** — no brackets or straps; barrel profile stations are unsourced; spare track links and hull grab rails absent | `vehicle_build/src/t54_*`, `gun_parts.rs:30-38` | W3 (K0 block) | brackets/straps as parts; barrel stations from the dossier; inventory rows |
| K15 | **The dome is 45 mm too long** (1.28 + 1.128 = 2.408 against the dossier's 2.363) and only a ratio lock (±0.06) watches it | `t54_hybrid_turret.rs:81-87` | W3 (K0 block) | dome length pinned to the dossier ± 10 mm; closed for real by K0 |
| K16 | **The dome is the wrong casting against the drawing (K0 against R6, 2026-09-05).** Side at y 1.70: the drawing's base runs −1.45 … +1.69 (3.14 m), the bake's −1.14 … +1.69 — 31 cm short at the REAR; front at y 1.70: 2.50 m wide against 2.28; at y 2.30 the drawing shows only the cupola while the bake's dome still spans 1.6 m — the drawing's bare roof lies at ≈2.25, the bake's at 2.40, and the cupola stands ≈0.28 proud, not 0.131. The dossier's 2.40 (low confidence) is most likely the OVERALL height | `docs/vehicles/refs/t-54.md` § What the traced drawing found; `tools outline-overlay` | W3 (K0 block) | a second source on the roof height resolves the dossier row; then the loft stations move: base 3.14 × 2.50 at the ring, roof ≈2.25, cupola ≈0.28 proud; K0 side/front IoU rise past 0.90 |
| K17 | **The fender stowage is too tall and runs the whole length; the upper hull is carried down over the tracks.** Behind the turret the bake's stowage top is 1.66 against the drawing's 1.56; at y 1.55 the bake's stowage spans −3.48 … +3.07 while the drawing's bins sit between −2.16 and +1.94; from the front at y 1.50 the bake is 3.20 wide against 2.59 — the drawing shows daylight between the fender and the track crest, the bake fills it (the front mudguard/upper hull carried down) | `docs/vehicles/refs/t-54.md` § What the traced drawing found | W3 (K0 block) | bins and tanks between the mudguards, lids at the hull roof line; the fender a sheet with daylight under it; front-view IoU past 0.90 |
| K18 | **The belt's end wraps are 26–34 cm short.** At y 0.30 the drawing's belt spans −2.74 … +2.66, the bake's −2.40 … +2.40: the belt leaves the ground run too early at both ends — the sprocket and idler radii the belt rides are too small (K9's sprocket, the idler's 0.51) | `docs/vehicles/refs/t-54.md` § What the traced drawing found | W3 (K0 block) | belt wrap radii from the dossier (sprocket tip 0.341, idler 0.255 + shoe); side-view IoU at the ends |
| K19 | **The Tiger I's turret is the wrong box against the 1944 STT drawing (K0, 2026-09-05).** Roof traced at ~2.55 against the blueprint's 2.885 (`roof_y`) with the cupola apex at 2.96 (documented 3.00) - the turret is ~30 cm too tall (C2 resolves toward tiger1.info's 2625); at y 1.95 the drawing's turret + bin spans -1.55 ... +1.60, the bake's -1.89 ... +1.20 (34 cm too far back, 40 cm short at the front); from the front the drawing's turret is 2.34-2.52 wide, the bake's 2.01 | `docs/vehicles/panzerkampfwagen-vi-tiger.md` § Reference drawings | W3 (K3, the first vehicle) | `roof_y`, `ring_z`, `plan_half_width/length` from the sheet; K0 side/front past 0.90. **Data half landed 2026-09-05 (K3 data-first PR):** `roof_y` 2.55, `cupola_height` 0.40, `ring_y`/`deck_y` 1.778, turret plan 1.17 × 1.42 at `ring_z` −0.12, the Walzenblende band 1.30–1.60 with `plan_front_pad` 0.30; K0 side 0.831 → 0.879, front 0.862 → 0.925. **Second data pass (2026-09-05, K3-2c):** the sheet's rear view carries its own heights, 8 ft 6.5 in = 2.60 to the roof and 9 ft 4.75 in = 2.86 to the drum's top, so `roof_y` 2.60 / `cupola_height` 0.26 (the side-view pixel trace's 2.55 / 2.96 read the roof 5 cm low and the lid hardware as the apex). What stays for the parts half: the cast cupola of the late E (the sheet shows the drum) |
| K20 | **The Tiger I's widths are swapped against the STT sheet.** 12 ft 3 in = 3.734 m is over the track GUARDS; the tracks are 2.100 + 2 x 0.724 = 3.548 m; the upper hull above the guards is 3.17-3.32 wide; the rear deck top is 1.96 (the sheet's hull side top 5 ft 10 in = 1.778). The blueprint has 3.705 over the tracks (`track.outer_x 1.8525`), 3.56 for the upper hull (`half_width 1.78`) and `deck_y 1.9` | same | W3 (K3) | `outer_x` 1.774, guards to 1.867, `half_width` ~1.62 with the sponson floor at the guard width, `deck_y` ~1.8; the dimension anchors re-sourced to the sheet. **Data half landed 2026-09-05:** `outer_x` 1.774 / `inner_x` 1.05 / `center_x` 1.412, `belly_y` 0.432, `deck_y` 1.778, end wheels 0.72 / r 0.40, anchors re-sourced (HullWidth 3.56 over the shelf, roof 2.55, apex 2.95, clearance 0.432). **Second data pass (2026-09-05, K3-2c):** the sheet read again from the front and the side — the upper hull box is 3.18 m (`half_width` 1.59, was the belts' 3.56) standing on a sponson underside at ~1.22 m (`sponson_y`, was 0.95); the belts run outside the box under the recipe's guards, so no third tier was needed: the tub, the box and the guard are the three. **Parts half, K3-2e (2026-09-05):** the 3.734 m track guards are the library's folded pressings at the sheet's 1.40 rail (`visual.fender`, `fender_parts_for_blueprint`; the recipe's flaps hang from that shelf), `HullWidth` 3.734. What stays: ~~the driver's plate set back from the nose behind the near-horizontal glacis~~ (landed 2026-09-05, K3-2d: `armor.hull_bow_shelf` 0.80 m / 1.35, the shelf as its own volume; K21 registers the shelf's zone debt), the sprocket ø0.914 / idler ø0.686 asymmetry (one `end_radius`) |
| K21 | **The Tiger I's glacis shelf resolves as `UpperGlacis` (100 mm) where the plate is 60 mm (K3-2d, 2026-09-05).** The near-horizontal glacis under the driver's plate is its own convex armour volume on the same planes the recipe draws (`ArmorShape::hull_bow_shelf`, the plate set 0.80 m back from the nose), but `ArmorZone` has no shelf zone and the zone rides the wire inside damage events. At ~80° from vertical every WWII AP shell ricochets off either thickness, so the lie is small — and registered, not hidden | `docs/vehicles/panzerkampfwagen-vi-tiger.md` § Reference anatomy | W3 (the armour-solid program) | an `ArmorZone::GlacisShelf` variant (append-only), its thickness in the zone table, the wire version bump; the Panther family and the T-34-85 bows will want the same field |
| K22 | **Every vehicle's tracks at the top bar** (the owner, 2026-09-05 night: „gąsienice każdego pojazdu powinny być naprawdę dobrze zrobione, mega dopracowane”). Only the T-54's belt is a construction (shoe pattern, guide horns, pins, tension v5, the moving belt); the Tiger I's Kgs belt is a recipe belt of 96 links, and the rest of the fleet falls back to the fleet's 0.22 m spacing and a generic shoe | `vehicle_geometry` running gear, `TrackShape`, the T-54 dossier § running gear; `docs/vehicles/*.md` | W3 (with each vehicle's K3 pass) | per vehicle: the shoe from the sheet (pitch, width, pattern, guide horn, end connectors), the sprocket's teeth engaging the links, the idler and both wraps, sag from tension v5, the moving belt, a close-up overlay of one belt against the drawing; the owner looks at every belt in the studio contact sheet. **Instrument landed 2026-09-05 (K22-0):** every vehicle's studio bundle carries `running_gear.png`, a close-up centred on the drive sprocket's axle at three times the silhouette's magnification (`ReviewFocus::DriveEnd`) |
| K23 | **The guns as constructions, reviewed** (the owner, 2026-09-05 night: „zastanówmy się czy działa można lepiej zrobić”). `GunShape` is a tube with a radius, a brake length and an evacuator flag; the recipes revolve it; only the T-54 authors a gun group (`GunVisual`) | `game_core::GunShape`, `vehicle_recipes` gun builders, `t54_hybrid.rs` | W3 (the T-54's D-10T as the bar, then each vehicle with its K3 pass) | a per-gun dossier row (calibre, barrel length in calibres, brake or evacuator type and its baffles/ports, mantlet form, recoil length); the barrel as its stepped sections (jacket, tube, brake with baffles), the mantlet's fit to the turret, recoil and run-out on firing; measured against a photo or drawing before the owner's look. **Instrument landed 2026-09-05 (K23-0):** every vehicle's studio bundle carries `turret_mantlet.png`, a close-up centred on the trunnion (`ReviewFocus::Trunnion`) |
| K24 | **No presentation bible: every tank is green or red.** The only colour on armour is the TEAM tint (`client/src/vehicle/pbr_mesh.rs` `material_tint_mask`); the albedo fallback is white; no nation paint, no scheme, no markings (the owner, 2026-09-05 night: „ustalić wygląd i kolor, prezentację każdego czołgu, każdej nacji i żeby w całej grze wyglądały tak samo”) | `pbr_mesh.rs`, `renderer_wgpu/src/scene_renderer/vehicle_materials.rs`; `docs/game-design.md` row 21 | W3 — gates K6 and every material/texture row | `docs/vehicle-presentation-bible.md`: per nation the base paint and its period, per vehicle the scheme, markings and wear; the team colour becomes a separate read (marker, outline, HUD — never a repaint of the tank; GDD „czołg na 400 m musi być do zobaczenia”); one material law across garage, battle and studio, locked by the studio contact sheets; the owner approves the fleet's palette on one contact sheet before any texture lands |

### G — tracks and the ground

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| G1 | **Three ground samplers per hull per frame.** The support envelope (`physics/src/track_contact.rs`, rubble-aware, convex hull of stations), the probe cross (`physics/src/contact.rs:67-118`, four samples around the hull centre), and the client's per-wheel residual (`client/src/vehicle/render_frame.rs:300`, no rubble). They disagree exactly at crests and ditches. The drive's slope term reads the 3 m probe (0.189 on the audit hill) while the attitude reads the beam (0.224) | the three files, `forces.rs:41-44`, `contact.rs:83` | W2 | one function owns ground contact; the client reads station heights from the physics result; `render_frame.rs` no longer samples the heightfield; drive slope and attitude slope are one number; `running_gear_dynamics.rs:233` extended to assert rendered station heights == physics station heights on a crest |
| G2 | **Belt speed is re-derived client-side from pose deltas** (`engine/src/components.rs:117`); slip, a thrown belt's asymmetry and forced yaw never reach the visuals | `engine/src/components.rs:100-135` | W2 | per-side belt speed on the wire (append-only, protocol bump); the scroll reads it; locks: a thrown belt scrolls zero while the hull is dragged, a slipping belt scrolls faster than ground speed |
| G3 | **The FX path invents the gauge.** Ruts and the shed ribbon use `hitbox.half_width_m * 0.86` as the track centreline — 1.505 m on a T-54 against the real `half_gauge_x` 1.32 m. The same class of bug P2.1 fixed for belt scroll | `client/src/app/motion_fx.rs:93`, `client/src/vehicle/track_ribbon.rs:88` | W2 | one source (`half_gauge_x` from the blueprint) under `single_source_constants`; lock: rut centreline within 2 cm of the belt centreline |
| G4 | **Damage is modelled three times**: the HP pool (`game_core/src/track.rs`), drive scalars (`sim/src/drive_modules.rs`, `DAMAGED_SPEED_FLOOR`, `BROKEN_ONE_*`), sag tiers (`render_frame.rs:338`) | the three files | W2 | one `TrackCondition` derived from HP, consumed by drive and render |
| G5 | **Admitted and open**: gradeability really 0.42 against a documented 0.60; per-belt ground unimplemented (`contact.rs:105` samples material at the hull centre); cm-scale velocity kinks from the 5 m heightfield snap (`docs/battle-camera-policy.md:101`); low obstacles-as-ground deferred (P2.2) | `docs/contact-and-tracks-program.md` register | W2 | per-belt ground material; gradeability locked at the documented value or the document corrected to the measured one; the kink bound is G7's continuity lock |
| G6 | **Fifteen track models** in total (damage pools, drive status, belt-drive steering, support envelope, probe cross, vertical follow, authoritative attitude, footprint SAT, a dead parry query, blueprint `TrackShape`, geometry running gear, client suspension, belt scroll, presentation spring, ribbon/ruts/audio/HUD). Only one cross-check exists (`running_gear_dynamics.rs:233`) | audit 2026-09-01 | W2 | the count is not the target; the cross-checks are — every pair that must agree (stations, gauge, condition, speed) has a lock, and `parry_query.rs` is deleted |
| ~~G7~~ | ~~**The hull is a rigid beam on a global ramp — the arcade moment is structural.** There is no sprung mass in the authoritative model (`hull_attitude.rs:2` "deliberately spring-free"); pitch is the slope of the convex hull under the stations, rate-limited at 1.4 rad/s (80 °/s), clamped ±0.6 rad; height is a hard snap (`vertical.rs:82-84`). On a 1.2 m × 12 m hill at 8 m/s the 5 m grid turns the hill into a tent with a 25° crease; when the apex crosses the 0.906 m gap between stations the pitch target swings +0.220 → −0.220 rad in 6.8 ticks (223 °/s demanded), the limiter takes 18.9 ticks, peak lag 16.2°, pitch rate a square wave; the beam rides 4 cm below the apex. No value of the one constant is both un-snappy and un-floaty. A T-54 and a Jagdtiger pitch identically; weight transfer is a 2.0°-capped client illusion the gun and armour cannot see; `mobility_baseline.rs:56` already admits "gradeability is a fleet constant, not a vehicle property"~~ **CLOSED (2026-09-02, `feat/sprung-hull-g7-state`, after step 1's locks in #665) — pitch and roll; heave stays a snap (G7b below).** `physics::HullSpring`: pitch and roll are a per-axis spring-damper on the support plane (the convex envelope survives as the rest-length source, as decided), semi-implicit f32 at the tick, the rest an exact fixed point (a hull within 1e-5 of its target is snapped to it, so the terrain-free fixtures stay bit-exact — no replay re-pinned), stable at the replay's 20 Hz. Nothing authored: `ω = √(g / sag)` with the sag the blueprint's arm rise (torsion bars 0.13 m → 1.5 Hz, Christie 0.22 m → 1.2 Hz — a T-54 and a Jagdtiger on the same bars share the frequency, mass cancels in steel sized for its load, which is what the physics says and what the lock now names), ζ by family (0.5 / 0.35 / 0.55), and weight transfer as the quasi-static tilt of the sprung mass, `θ = 12·a·h_cg / (ω²·L²)`, over the wheelbase and the gauge from the tick's own acceleration (braking dives the nose, a turn leans out, a ram is a jolt, capped 3.4°) — authoritative, so the gun and the armour see it and the Centurion's stabilizer (A12) cancels it. The spring state (`pitch_vel`, `roll_vel`) rides the kinematic state, the tank state, the snapshot and the owner's motion (wire v50, fixtures regenerated); the predictor resyncs it; the presentation spring lost its own dive and lean (the picture dived twice) and stays a 20 Hz smoother carrying the fire and hit impulses. Locks: (1) crest walk — the pitch rate changes per tick at most what the spring allows, no square wave; (2) wallow by family and travel, Christie over torsion by √(0.13/0.22), Jagdtiger = T-54, every hull 0.8–2 Hz; (3) a braking T-54 dips its nose in the sim and its gun loses elevation, the Centurion's stabilizer holds; (4) landing roll's peak/settle/no-tip survive on the spring; (5) `mobility_baseline`, `climb_envelope`, the drive replay and both combat replays byte-identical; `hull_rest` and the attitude parity lock from step 1 hold; the rate-limiter test replaced by the spring's own (one nod, exact rest, clamp without stored push, weight transfer by sign) | `physics/src/hull_attitude.rs`, `physics/src/controller_settings.rs`, `physics/src/world.rs`, `sim/src/tank_state.rs`, `sim/src/tank_drive.rs`, `net/src/lib.rs`, `client/src/predict/sync.rs`, `engine/src/attitude.rs` | W2 (first) | done |
| G7b | **Heave is still a hard snap.** `vertical.rs` sets `position.y = ground_y` the tick the running gear is grounded; the sprung hull pitches and rolls but does not sink on its springs over a step or rebound after a landing | `physics/src/vertical.rs:76-84` | W2 | the heave axis on the same spring law, with its home decided first (a separate authoritative field keeps the five height locks and the flight parity intact; folding it into `position.y` costs them) and per-station travel limits; the client's heave channel retires with it |

### C — the driving camera

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| C1 | **Trees cut the boom.** `TreeTrunk`, `TreeLine`, `Wreck`, `StoneWall` all enter `camera_obstacles`; only terrain gets bisection — cover is an exact entry `t` with an instant cut and no inbound smoothing, released at 14 m/s. Driving a shelterbelt slams 12 m → ~2 m per trunk | `client/src/app/live_cover.rs:36-40`, `camera/collision.rs:31-35`, `camera/present.rs:201` | W2 | trees and shelterbelts leave the obstacle list (the decision above) with a canopy fade; lock: a drive through the treeline never changes the boom |
| C2 | **The cut moves the gun, and there are two cameras.** The gun solves from the logical camera (no boom smoothing, a hard eye-over-terrain clamp, `collision.rs:95-105`), the view and reticle draw from the presented camera; every boom cut and clearance clamp pivots the sight ray about the target — the gun swings while the view eases | `app/reticle.rs:30`, `app/camera_link.rs:77-84`, `app/render.rs:506,589` | W2 | one eye: aiming reads the presented camera; inbound cuts against buildings/walls/terrain are rate-limited with a velocity look-ahead so the eye never clips; lock: no frame where the sight ray and the view ray diverge by more than the look-ahead bound |
| C3 | **The vertical leash is saturated on every slope.** A critically damped spring tracking a 1.76 m/s ramp needs 0.39 m of lag against a 0.05 m leash — it is a rigid offset that crawls 0.10 m at 0.225 m/s (0.44 s) at each slope reversal | `camera/smoothing.rs:24-57`, `camera_feel.rs` | W2 (after G7) | re-measured once the hull is sprung (its own motion becomes C¹); then rate-based tracking (match hull vertical velocity, capped) instead of a displacement leash. Refused and not re-proposed: ω7/0.35 m, 0.10 m, isotropic ω16, heave into the camera, 0.02 rad dive, 3 m/s kick, 29.7 Hz tremor, full-velocity anchor matching |
| C4 | **The terrain crease reaches the eye ~1:1.** `sample_height` is piecewise-linear over triangles with a data-dependent diagonal that flips cell to cell — 2–4 velocity corners per second at 10 m/s, each 0.2–0.6 m/s | `terrain/src/heightmap.rs:52,132-145` | W2 → T1 | the camera anchor samples a smoothed surface along the path; the count halves with T1's 2.5 m ring |

### A — aim and sight

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| ~~A1~~ | ~~**The reticle and the server resolve penetration by different code.** `penetration_from_outcome` dropped the thickness scale (the trace outcome never carried it) and resolved a bare track/skirt band while the server summed the spaced stack plus the hull side. A 20 mm track read green, the server charged band + belt → "0" + TRK. No test compared the two~~ — **CLOSED (2026-09-01, `fix/one-penetration-resolver`)**: one `game_core::resolve_traced_impact` over a `TracedImpact` (shell, armour, hull attitude, zone, angle, distance, thickness scale, shell direction, standing belts); the sim gathers it from `TankState`, the reticle from the snapshot; `TraceOutcome::Tank` carries `thickness_scale` and `direction`; `sim::verdict_for_traced_impact` is the seam's measuring point. Locks: `the_reticle_and_the_server_agree_on_ten_thousand_traced_impacts` (whole roster, every round, random attitudes, a third of belts thrown — penetration, effective armour and remaining penetration must all agree) and `a_track_hit_prices_the_belt_and_the_side_plate_behind_it` | `client/src/hud/reticle.rs`, `sim/src/combat.rs`, `game_core/src/armor/impact.rs` | W1 | one resolver in `game_core` called by both; a property test over 10 000 traced impacts asserting `hint.penetrates == server.penetrated` — done |
| A2 | **The creed contradicted the data by 10×.** ROADMAP promised 0.1–0.3 mrad; guns ship 1.9–3.4 (radius, hard max, √ draw, `aim_dispersion.rs:69,90,94-99`). At 200 m a fully aimed T-54 has a 58 cm cone; P(0.5 m cupola) = 65.6 %, at 400 m 46.4 % — that is what "hard to hit exact spots" is, and it is WoT-class by design | `docs/ROADMAP.md:11`, `catalog_soviet.rs:229` | W1 (first PR) | the creed line rewritten (done in this PR); a `roadmap_claims` anchor pins the fleet's rest-dispersion range to the catalogs |
| ~~A3~~ | ~~**A pitch limit looks like a wall.** A target below the gun's depression clamps `in_arc=false` → the same Blocked form, the shell into the dirt — indistinguishable from cover~~ — **CLOSED (2026-09-02, `fix/sight-feedback-a3-a5-a6`)**: the firing solution names which end of the arc bit (`ArcLimit::{Depression, Elevation}`), the feedback carries it, and the overlay draws the arc's own form in BOTH modes — a stop bar under (or over) the ring joined by a stub, and "DEPRESSION LIMIT" / "ELEVATION LIMIT" under the block distance; lock `an_arc_limit_wears_a_stop_bar_and_a_label_in_both_modes`. The audit's "nearly mute in third person" was half wrong: the block distance was already drawn in both modes; the impact X and the pen hint stay sniper-only by `docs/aiming-model-policy.md` (third person is situational awareness and speaks no armour), a policy this program keeps | `app/aim.rs`, `hud/reticle.rs`, `hud/reticle_overlay.rs`, `hud/reticle_readouts.rs` | W1 | a distinct pitch-limit form in both modes, locked — done |
| ~~A4~~ | ~~**Yaw reachability is never tested.** `in_arc` checked pitch only; a casemate's yaw is forced to 0 by the sim so a Jagdtiger's reticle was green off-axis while the round left down the hull line; the seam sweep skipped every out-of-arc placement as "a vehicle/map question"~~ — **CLOSED (2026-09-02, `fix/sight-yaw-reach-a4`)**: the firing solution flies the bearing the gun can take (a casemate's hull line), so the sight's trace judges the REAL shot — a sight point a hair off the line still arrives, 30° off it does not — and a blocked casemate names `ArcLimit::Traverse` (two stop bars bracketing the ring, "TRAVERSE LIMIT"); lock `a_casemates_sight_is_blocked_off_the_hull_line_and_clear_on_it`. The seam sweep counts out-of-arc placements (measured: Bystra 126, Prokhorovka 364 per ten thousand) under their own ceilings (190 / 550). The eye-vs-arc numbers are now a map instrument: Prokhorovka parks the eye where the gun cannot fire three times as often as Bystra | `app/aim.rs`, `hud/reticle.rs`, `hud/reticle/seam_tests.rs` | W1 | yaw folded into the fired bearing; the sweep's third counter with its own ceiling — done |
| ~~A5~~ | ~~**A silent refusal.** A fire input while the session is not ready is dropped before any feedback runs~~ — **CLOSED (2026-09-02, `fix/sight-feedback-a3-a5-a6`)**: the click into a session that cannot take it arms the red pulse and the UiReject knock before the edge is retired; lock `a_fire_click_into_a_session_that_cannot_take_it_is_refused_out_loud`. The second half of the audit's row was wrong: the 30-tick deploy shield IS set (`garage/actions.rs`) and swallowing the second half of a double-click on BATTLE is its purpose | `app/loop_step.rs` | W1 | every refusal reaches the UiReject knock — done |
| ~~A6~~ | ~~**A literal "0" is drawn for five outcomes** (ricochet, non-pen, near-pen, shatter, track stack) — `hit_indicator.rs` pushed `damage_hp` unconditionally~~ — **CLOSED (2026-09-02, `fix/sight-feedback-a3-a5-a6`)**: `hit_label` prints the damage when there was any and the OUTCOME in a word when there was none — RICOCHET / SHATTER / TRACKED / NO PEN — in the outcome colour, smaller than a damage number (a lesson, not a score); locks `a_zero_damage_hit_prints_its_outcome_in_a_word`, `a_zero_never_becomes_a_number_and_damage_never_becomes_a_word`, `the_word_is_drawn_where_the_zero_used_to_be`. ABSORBED belongs to S5 (our own hull) | `client/src/hit_indicator.rs`, `ui_strings.rs` | W1 | the outcome word replaces the number for zero-damage outcomes; "0" never rendered — done |
| ~~A7~~ | ~~**The scope hides the enemy.** The vignette window was 0.94 in y-units, so horizontally it reached 0.529 of clip-x — ~61 % of a 16:9 frame at α 0.87 near-black; and **no sniper frame was ever measured** — the D31 readability lock saw an 8 m third-person close-up~~ — **CLOSED (2026-09-02, `feat/sniper-review-view-a7`)**: `prokhorovka_sniper_contact` — an enemy T-54 in the enemy's paint, 300 m from the T-54's own sniper eye through the ladder's 8°, on the one bearing over Prokhorovka's rise that reaches the hull (the first bearing tried recorded a field with no tank in it; `the_sniper_frames_eye_sees_the_hull_over_the_ground` now walks the sight line) — recorded at first bless p50 0.344 / dark 27.9 % / form 0.0217 against the chase reference's 0.0167, under its own `SubjectBounds`; a `ReviewView` may bring its own lens; the housing is measured in the frame's aspect (0.94 of the WIDTH, corners only, < 20 % of a wide frame, locked); the frame's eye and paint are locked to `sniper_eye_from_base` and `ENEMY_HULL` from the client side. The battlefield goldens re-recorded deliberately (F1 had moved every horizon without re-blessing); every floor held. The spotted-enemy edge treatment moves to A9 | `scene_build/src/review_views.rs`, `client/tests/look_goldens.rs`, `hud/scope_overlay.rs` | W1 | the sniper view under the D31 floor; the window in the frame's aspect — done |
| ~~A9~~ | ~~**A spotted enemy is unmarked in the scope.** Spotting is cull-only (`app/aim.rs:335`): no outline, bracket or nameplate; at 8° a T-54 at 300 m is ~90 px wide against a field of the same value~~ **CLOSED (2026-09-02, `feat/spot-bracket-a9`).** `hud/spot_bracket.rs`: every spotted, live enemy wears four short corner arms on its projected hitbox (the eight corners of `HitboxProfile` posed by the hull yaw, projected, boxed; nothing when a corner is behind the camera), sniper mode only, in the reticle's neutral family one step quieter (`SPOT_BRACKET_COLOR`, its own bytes). Each arm is 22 % of the box's shorter side and never under 0.014 clip (5 px at 720p — the 300 m case), 3.6 px thick at 720p, standing 0.006 off the silhouette: corners, never a box, so the silhouette stays the eye's. The same visibility bit as the floating bar and the minimap blip; friendlies, wrecks, unspotted hulls and third person draw nothing. The `battle_hud` probe's sniper frame now lays the gun on the nearest enemy (hull-frame conversion, pitch included) and brackets every enemy in the scope as a review aid; at 34.6 m the arms read on both the sky and the field. Locks: the bracket draws for a spotted enemy and never for an unspotted one, a friendly, a wreck, or in third person; it straddles the hull's projected centre on both axes and is eight arms, not a box | `hud/spot_bracket.rs`, `app/render.rs`, `examples/probe/battle_hud.rs` | W1 | done |
| ~~A10~~ | ~~**The turret is steered by a proportional controller with a low gain.**~~ **CLOSED (2026-09-02, `feat/turret-aim-a10`, PR #652).** The P regulator (gain 5/6, τ 455–714 ms, a standing lag of `v/(R·G)` = 7 m behind the reticle at 54 km/h) is replaced by the time-optimal command `aim::rate_command_to_land`: full rate until the gun lands, an exact landing in ceil(err/(R·dt)) ticks. The audit's verdict stands in the row's history: the sim was innocent — a pure rate limiter, zero network lag on the own turret; the lag was the controller. (Row closed late in the register on 2026-09-02 — the merge preceded the bookkeeping.) | `game_core/src/aim.rs` (`rate_command_to_land`), PR #652 | W1 | `the_gun_runs_at_full_rate_until_it_lands` family in `aim.rs`; the drive_forward replay re-pinned at exactly ×2 (0.0315) |
| ~~A11~~ | ~~**Traverse rates are half the genre\x27s, and every hull out-turns its own turret.**~~ **CLOSED (2026-09-02, `feat/turret-aim-a10`, PR #652).** Fleet turret traverse raised from ~½ of the genre to the genre's class (T-54 24 → 48 °/s), every turret faster than its hull, and the fleet keeps its spread. (Row closed late in the register on 2026-09-02 — the merge preceded the bookkeeping.) | vehicle blueprints (`turret_traverse_deg_s`), PR #652 | W1 | fleet locks: every turret ≥ 22 °/s, ≥ 0.75 of its hull's yaw rate, fleet spread ≥ 2× |
| ~~A12~~ | ~~**Gun elevation is one fleet constant and nothing is stabilized.** `GUN_ELEVATION_RATE_RAD_S = 0.5` (28.7 deg/s) for all eight vehicles; the hull pitches at up to 80 deg/s and every degree lands 1:1 in the gun\x27s world elevation~~ **CLOSED (2026-09-02, `feat/gun-elevation-stabilizer-a12`).** `GunSpec::elevation_rate_rad_s` per gun (85 mm ZiS-S-53 and 7.5 cm KwK 42 1.0 rad/s = 57 deg/s; D-10 and 20-pounder and KwK 36 0.9; KwK 43 and Pak 43/3 0.8; 122 mm D-25T 0.7; 12.8 cm Pak 80 0.6 — a breech elevates as fast as its weight lets it), commanded through A10\x27s time-optimal controller, so a T-54 gunner now recovers 52 deg/s of the hull\x27s 80. `TurretModule::vertical_stabilizer` (0..1) → `TankSpec::vertical_stabilizer`: `step_aiming` cancels that share of the hull\x27s pitch change each tick, measured by the drive step around the attitude advance, before the gunner\x27s command and inside the arc clamp; shared by server and predictor, no wire change. Historical answer, locked by name: the Centurion Mk 3 carries one (the FVRDE Metadyne stabilizer under its 20-pounder), the wartime hulls none, and the T-54 obr. 1951 none — the STP-1 came with the T-54A in 1955, which the dossier says this model is not; the owner may choose the T-54A fit, it is one number. Locks: a stabilized gun holds its world elevation to 1e-6 through a 2.9° hull step, an unstabilized one rides 1:1, no stabilizer depresses through the roof; every gun\x27s rate in 0.5–1.5 with a 1.5× fleet spread; the prediction parity test elevates at the gun\x27s rate. Also: the vehicle asset snapshots regenerated (A11\x27s traverse rates had left them stale — the full gate caught it, the PR gate had no `tools`; `tools` is in the PR gate\x27s always-on set now) | `sim/src/aiming.rs`, `sim/src/tank_drive.rs`, `game_core/src/weapon.rs`, `modules/turret.rs`, `assets/vehicles/*.vehicle.json` | W1 | done |
| ~~A8~~ | ~~**The trajectory reads wrong for visual reasons.** Gravity 12.0 m/s² (22 % over Earth, undocumented); the tracer is a 22 ms streak that shows 5 % of a 400 m flight at any instant; no shell follow. The solver itself is right (WoT-style aim-at-point integrating the sim's own step)~~ — **CLOSED (2026-09-02, `feat/shell-flight-a8`)**: `SHELL_GRAVITY_MPS2 = 9.81` for the shell (lock `a_shell_falls_at_earths_gravity`; the sight's solver and the server's flight share the one step, so they moved together; the perforation replay and the Bystra seam ceiling re-pinned with their measurements); the hull keeps its tuned 12.0 until G7 re-measures its four contracts as a set; `ShellTrails` remembers every seen shell's path for 0.9 s keyed by shell id and draws it as a dimming line behind the live streak, so the arc reads AFTER the round has gone — the shot that fell short, the one that cleared the crest (locks `a_shells_path_stays_in_the_eye_after_it_has_passed`, `paths_are_kept_per_shell_and_a_still_shell_leaves_none`). No shell follow camera: a follow is a spectator feature, not a sight one | `game_core/src/math/mod.rs`, `fx/tracer.rs` | W1 | 9.81; the path visible for the whole flight; fixtures held — done |

### Z — Honest Steel

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| ~~Z1~~ | ~~**Collapse is a state swap.** `sync_cover_destruction` diffs phase bytes and fires one `impact_burst(Cover)` + `track_dust` at the object's centre — 5 sparks and 6 puffs — then swaps the baked mesh. An 18 m tenement dies in eleven particles~~ **CLOSED (2026-09-02, `feat/collapse-theatre-z1`).** `fx::collapse`: a staged sequence on the FX clock, sized by the box — the dust mass filling the box's volume (the walls vanish INSIDE the dust: the first probe frame had a roof-line curtain hanging eleven metres over an empty footprint) and masonry chunks off the roof line now, the settle wave along the perimeter at 0.35 s, the hanging haze at 0.9 s; counts scale with footprint area (the settle wave with the perimeter) inside clamps, so a fence is not silent and a tenement (~440 particles at peak) leaves the pool more than half free. `audio::AudioEvent::CoverCollapse` and the `Collapse` voice: the fracture, a rumble whose spectrum closes as the mass settles, the SECOND wave at 0.35 s (a second fracture, the sub taking the weight, the main rumble — the walls meeting the ground), and a rain of masonry that outlasts it, all scaled by footprint and height. Locks: FX count ≥ 4× and extent across the footprint for a tenement vs a shed, three beats on the clock and none early, the peak under half the pool, determinism; audio: deeper and longer than a shell thud, the second wave louder than the moment before it, a tenement rumbles longer than a shed. `tenement_probe` renders the mid-collapse frame (`tenement_collapse.png`) | `client/src/fx/collapse.rs`, `audio/src/voices/collapse.rs`, `app/ingest.rs` | W1 | done |
| ~~Z2~~ | ~~**Cover damage is two constants**: HE 300, everything else 80. A 57 mm and a 152 mm fell a barn in the same number of shells~~ **CLOSED (2026-09-02, `feat/destruction-theatre-z1`).** `sim::cover_damage_hp(&ShellSpec)`: a high-explosive round in proportion to its bursting charge (`300 × (filler / 2.16 kg)^0.75`, 120–600), a kinetic round in proportion to its muzzle energy (`80 × (energy / 6 288 kJ)^0.6`, 30–200) — one function, anchored on the D-10\x27s own rounds so the shipped balance is the reference (a 600 hp barn: two OF-412 or eight BR-412). The impact record is wire data and names the round by type only, so the sim reads the round from the shooter\x27s gun; an unnamed impact keeps the old constants. Lock: over every stock round the fleet fires, a bigger charge or energy never needs MORE shots, the fleet\x27s biggest and smallest differ, the D-10 anchors hold | `sim/src/state.rs` | W1 | done |
| ~~Z3~~ | ~~**Three of five maps are static battlefields.** Destructible objects: Ostrogorsk 39, Prokhorovka 14, Mazurski 9. `Wreck`, `RailCover`, `Crag` are indestructible forever; all 12 `SceneryKind` variants never change~~ **CLOSED (2026-09-02, `feat/destructible-maps-z3`).** Counted on the COMPILED maps (the town grids expand, the oak boles are derived): Ostrogorsk 125, Bystra 68, Prokhorovka 36, Orliny 36, Mazurski 30 destructible cover objects; `map_forge::destructible_count` and a per-map floor (`DESTRUCTIBLE_FLOOR`, today's counts) the report refuses a compile under (`destructible_floor`), so a map edit cannot quietly take the destruction out; each dossier carries its count and breakdown, and owns the target above the floor. `Wreck` is damageable (500 hp, shelled down to a hull-line mound at 0.45 of its height, then scrap) — steel, not scenery, on maps where wrecks were a third of the cover. `RailCover` and `Crag` stay indestructible by doctrine; the scenery kinds never changing is F5 (W4). Lock: `every_shipped_map_keeps_its_destructible_floor` | `terrain/src/battlefield.rs`, `map_forge/src/report.rs`, `docs/maps/*.md` | W1 | done |
| Z4 | **No destruction register; the roadmap says DONE** | `docs/ROADMAP.md` | W1 (first PR) | the roadmap line says "mechanics done, the theatre is this register" — done |
| ~~Z5~~ | ~~**Hit marks are stickers.** The owner, 2026-09-02 17:09, three screenshots after the first minute of a fresh build: "co to kurwa za ślady? jak to jest beznadziejnie zrobione". A shell mark on a wall was two or three flat-colour SQUARES 1.5–4 cm proud of the plaster (a pale square of 1.8–2.6 radii, one or two dark squares, the second rotated — the "teeth"), unlit by any cavity, sized by a rule that gave a 100 mm HE a 1.6 m bite; a bounce or ricochet on a hull was two unlit FX stamps that faded in 30 s. `destruction_showcase` had rendered exactly this frame for months and nobody looked; the battle goldens carry no hit at all~~ **CLOSED (2026-09-02, `feat/armor-wounds-marks` #677 + `feat/cover-wounds-marks` #678).** One rule, both halves: a mark is MATERIAL. Vehicle: scuffs and gouges are wound records in the aperture list the breaches ride (`ArmorMarkKind`, `thermal.w`), shaded by `armor_surface_wound` — a dish leaning the normal inward, scorched paint at the rim, bared dull steel at the centre, a gouge's bright torn scrape along the ricochet's departure (`shell_direction` reflected off `plate_normal`, carried as `HitDecal.local_tangent`); the FX stamp path deleted, marks kept for the battle (§13.1), the FX budget re-pinned at 12 288. Wall: a wound is a RECESS dug into the collision box — an irregular three- and five-lobed rim, a dark broken-masonry floor a real depth behind it, lit side walls leaning into the cavity, an irregular fan of exposed brick fading into the plaster's own tone, sizes from the calibre (a 100 mm HE bites ~0.9 m, not 1.6), spall in the brick's tone at the foot. Both showcase frames (`_surface_wounds.png`, `_cover_wounded.png`) looked at before merge | screenshots 2026-09-02 17:08–17:09; `camera_common.wgsl::armor_surface_wound`, `client/src/fx/decals.rs::hit_wound_records`, `scene_build/src/battlefield.rs::append_cover_scar`, `destruction_showcase.rs` | W1 (done) | GPU `a_scuff_and_a_gouge_mark_the_plate_without_opening_it` (colour distance, never the background), `a_gouge_runs_along_the_ricochets_departure`, `kinds_map_to_hole_scuff_and_gouge_and_every_mark_stays`, `turret_wounds_traverse_with_the_turret_while_hull_wounds_stay`, `a_bounce_and_a_ricochet_record_wounds_the_shader_shades`, `hit_decals_are_capped_and_every_mark_stays_for_the_battle`; wall: `a_wounded_wall_wears_its_scars_and_spills_rubble_at_its_foot` (every wound vertex inside the box, floor ≥ 9 cm behind the face, rim ≥ 12 vertices with radii varying > 15 %). Still owed: a close-up wall shot in the showcase, the ground's HE scorch as a splat instead of a stamp (Q1's lane) |
| ~~Z6~~ | ~~**The breach up close is four rusty flaps around a lit room.** The owner, 2026-09-02 18:53, two close-ups of a penetration on a hull: 4–6 large flat single-quad "petals" (`aperture_rim.rs::append_petals`, 40–70 % of the hole's radius long, standing straight off the plate), the whole rim and the petals in one rust-brown tone (`fracture_tone`: a heat band between two browns — but fresh torn steel is silver at the break and rust is weeks away), and through the hole a bright white-blue glossy surface: the hull's interior materials took the full `light_radiance` (hemispheric sky ambient, fill and rim light, none of them shadowed by geometry) plus a sky reflection, so the inside of a tank was lit like its outside~~ **CLOSED (2026-09-02, `feat/breach-close-z6`).** Tongues: a kinetic tear folds 8–14 narrow three-station strips that narrow to a torn tip and CURL (the lift grows with the square of the distance along the tongue), exit tongues flaring harder than entry ones, two-sided, deterministic from the fracture seed (`DAMAGE_BAKE_VERSION` 4). Tone by the tear lane the rim writes into `shade`: a near-black scorched root, dark warm steel along the section, bright bare metal at the tip (roughness 0.42, metal 0.6 there); the rim takes a flat contact term instead of reading the tear as AO. The interior is a CAVITY: materials 5–7 get 8 % of the sky ambient and the light through their own apertures, no key/fill/rim, spec ×0.15, env ×0.02. And there IS a far wall now: the hull was a single skin with its back faces culled, so through a hole the eye met the running gear and the ground beyond, lit as an exterior — the INTERIOR SHELL (`vs_interior`, `vehicle_interior_pipeline`: the same castings drawn once more with front faces culled; intact hulls collapse in the vertex stage and cost no fragment) draws the inside of the skin, which `fs_main` shades as primer in the cavity (`front_facing` false). And the hole goes THROUGH: the hybrid hull carries a second skin some tens of centimetres inside the part-aware plates, and a cut that stopped at the plate (thickness + 2.5 cm) showed that skin, lit as an exterior, filling the breach — the cut band is now at least 30 cm deep (never past 45). And the light through a hole now reaches a surface in proportion to how squarely it FACES the hole (a receiver cosine) and only past 6 cm behind the plate: the skin around a breach used to shine sky-blue through its own opening (`armor_interior_radiance` gave a fragment two centimetres from the hole the full solid angle and the sun with no cosine) — that was the "white-blue glossy" surface in the owner's close-ups. `_breach_close.png` in the showcase (a cold breach from under a metre), looked at before merge | screenshots 2026-09-02 18:53; `client/src/vehicle/aperture_rim.rs`, `vehicle.wgsl` (`fracture_tone`, `enclosed`), `destruction_showcase.rs` | W1 (done) | `tongues_curl_and_end_in_bright_torn_steel` (every tongue roots ≤ 0.31 and tips ≥ 0.95 on the tear lane; the tip stands more than twice the middle station off the plate), the ring/tongue counts and stand-off in `rim_has_an_open_center_and_real_plate_wall` and `only_kinetic_holes_grow_petals_and_they_stand_off_the_plate`, GPU `an_interior_material_is_a_cavity_lit_only_through_the_apertures` (an interior material under the same sun renders under 45 % of the plate's luma); GPU `a_breach_reveals_the_dark_inside_of_the_far_wall_not_the_background` (a closed box with a breach cut through its front: the centre pixel is neither the background nor the front face, and under 45 % of its luma) |
| Z7 | **Shells do not ricochet off the ground.** The shell march dies `radius_m` above the terrain (`sim/src/shell_trace/terrain.rs`) at any angle: a round arriving at 5° over a hard surface, which in reality skips and flies on, stops in the dirt — and the only ricochet the sim knows is off armour (`shell_step.rs`, once, a brittle core shatters past the bounce angle). game-design §4 lists the ground material table (μ, resistance) that the skip should key off | `sim/src/shell_trace/terrain.rs`, `sim/src/shell_step.rs:99-140`, game-design §4 | Destruction 2.0 (with V0) | a ground ricochet by impact angle and material (rock and hard-packed road skip below ~10°, soft soil never), continuing once like the armour bounce; replay fixture re-pin; a lock on a flat-angle shot over rock landing PAST the impact point |

### P — armour

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| P1 | **Parity is two spot checks.** Metal-outside-armour and armour-proud-of-metal are locked at ≤ 10 mm for the T-54 turret and ≤ 1 mm for the Tiger I turret; the other six vehicles have "≥ 4 vertices on the plane" tests; hulls, sides and decks are unmeasured everywhere. "What blocks the shell blocks the eye" is doctrine plus two numbers | `t54_turret_armor_lock.rs:35,84`, `tiger_i_benchmark.rs:115`, `panther_ii_benchmark.rs:44` | W3 (per vehicle) | a fleet-wide, hull-inclusive parity metric per zone as a gate for every roster vehicle — floor at today's value, target 10 mm — landing with each K3 migration |
| P2 | **Thickness is a six-bucket facet.** Mantlet, deck, track and skirt plates are formula-derived (`zone.rs:113-123`); only the T-54 authors its roof, lower front and taper | `game_core/src/armor/zone.rs`, `modules/catalog_*.rs` | W3 (per vehicle) | per-plate authored millimetres in the blueprint from the dossier, locked per vehicle |
| P3 | **No turret ring, no hatch zone, no interior or rack armour, no stowage or fuel as spaced** | `armor/vehicle_volumes.rs` | W3 | `ArmorZone` appended (ring, hatch) with real patches; external stowage as screens where the dossier says so |

### F — flora

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| ~~F1~~ | ~~**The cones.** The map-border backdrop ring — 450 trees per map, 40–380 m outside the red line, on all five maps — is built from the pre-Drzewa six-segment frustum kit (`push_scenery_tree_far`: three stacked hex frusta for Oak, a 0.95→0.12 spike for Poplar) at a 3.0–3.4× far scale, so the ring's trees stand 25–38 m tall — bigger than any tree inside the map — and read as hexagonal Christmas trees on every horizon. Drzewa 3.0 never touched this path~~ — **CLOSED (2026-09-01, `feat/backdrop-ring-drzewa`)**: the ring stands on every species' impostor — the same crossed quads over the same atlas sprite the ladder draws past 150 m (`foliage::push_impostor_quads`, one expansion for both routes); the frustum kit is deleted; the mix is `HorizonSpec::flora` per map (pine on Orliny and Mazurski, willow along Bystra, poplar around Ostrogorsk); scale 1.0–1.3 of a mature individual, locked on the vertices (`no_ring_tree_towers_over_its_species`), species locked per map (`the_ring_grows_the_species_its_horizon_names_and_never_one_alone`); the atlas grew 1024×512 → 2048×1024 (5.6 → 22.4 MB, re-locked to the byte) | `scene_build/src/backdrop.rs:43-75`, `scene_build/src/foliage.rs:38-46,197-257`; `target/orliny_pine_belt.png`, `target/bystra_treeline.png` | W1 | the ring is grown from Drzewa 3.0's impostor rung per species; the frustum kit has zero call sites and is deleted; lock: no backdrop tree taller than 1.3× its species' envelope; the ring's species mix follows the map's climate; perf measured on the MX330 (A→B→A) |
| ~~F2~~ | ~~**Monoculture.** Inside every map: Oak and Bush only. Poplar, Willow, FruitTree and Pine are fully grown (PRs #626–#628) and placed nowhere. Orliny's `pine_belt` view has no pine; Bystra's river has no willow~~ **CLOSED (2026-09-02, `feat/flora-species-f2-f3`).** Five blueprints plant their horizon's species inside the map: Bystra 26 oak / 12 poplar / 18 willow / 12 fruit (willows bank-loving within 22 m of the river, a poplar windbreak by the valley road, the orchard behind its screen); Prokhorovka 18 / 14 / 18 / 10 (a windbreak on the farm road, willows in both balkas, the Oktyabrskiy orchard); Orliny 16 oak / 44 pine / 6 fruit (pines on three slopes — the pine belt has pines); Ostrogorsk 26 oak / 16 poplar / 20 fruit (poplars down the promenade, two orchards); Mazurski 12 oak / 28 pine / 12 willow. The compile report gates it from here: `report::check_species_mix` — ≥ 3 species, none over 70 %, every horizon species at ≥ 0.10 planted inside the map (the ring continues the map, it does not invent one); fixtures under 12 trees warn instead of failing, and `goldens::every_shipped_map_is_dressed_enough_for_the_species_gate` keeps every shipped map above 12 so the rule judges all of them. Goldens re-blessed for all five. Found on the way: `check_scenery` warned on every oak on every map (each stands in its own `TreeTrunk` box — 104 warnings nobody read); the bole kind is exempt now and the goldens test turns "no dressing grows through cover" into a shipping assertion. Not done: the instanced LOD ladder is still oak-only — the other species bake as Mid-rung statics without wind (F5) | `map_forge/src/report.rs`, five blueprints, `docs/maps/*.md` | W1 | done |
| ~~F3~~ | ~~**The shelterbelt is boxes on sticks.** `TreeLine` is a szpaler of slabs on stick trunks~~ **CLOSED (2026-09-02, `feat/flora-species-f2-f3`).** `scene_build::tree_line`: every `TreeLine` box is planted at 3 m stations (windbreak density) with Mid-rung trees from the map's horizon mix (orchard trees and shrubs excluded), each fitted to ITS box — tip under 0.97 of the box height, crown within 2.5 m of the thin faces, crown inside the ends (a door between two boxes stays a door). A station grows the species that FILLS the wall (≥ 0.80 of the height at its fitted scale): a 16–22 m wall 6 m thick is a windbreak's shape, and the measured Mid crown reach (oak 6.8–9.3 m, willow 9–12.6, pine 4.2–4.8, poplar 2.2–2.8) fits an oak into it only at half size — so the lines stand poplar on the steppe, in the valley and in the town, pine on the pass and the isthmus, exactly what such walls are. The wall is OPAQUE because the box is: every tree carries a crown hull (a superellipsoid of shaded canopy under its cards, exponent 3, never narrower than 1.8 m), the cover-box bake keeps a hedge body to 0.40 of the height (6–9 m, over the tallest hull in the fleet), and `the_line_is_opaque_where_the_box_is` walks every run at half-metre steps from the body's top to 0.75 of the wall and proves a sight line meets a hull. Three pictures decided it: the first planted lines let the eye through to a tank the sim had hidden and `prokhorovka_clear_afternoon` lost its dark plane (0.005 → 0.004, the value-structure lock); an inset dark box under the crowns read from 300 m as the slab with sticks on top; the hulls read as crowns. Honesty debt left: the top quarter of the wall is card tips, not mass. A felled line leaves a stump at every station. The cover boxes are untouched (map data; the re-bless is F2's scenery). Locks: every vertex of every line on every shipped map inside its box, planted from the map's mix with every tree filling the wall, opaque to 0.75, per-map triangle cap 120 k (measured: Bystra 96 trees / 102 k tris over eight lines, Mazurski 58 / 106 k — pines are 1.8 k a tree, Ostrogorsk 38 / 41 k, Prokhorovka 37 / 40 k, Orliny 22 / 40 k; frame: A→B→A `perf_capture` sandwich within drift, Bystra statics +47 408 tris of which +36 388 in the far shadow cascade — PR #651), stumps per station, the slab gone (24 hedge-body vertices + bark/foliage roles only) | `scene_build/src/tree_line.rs`, `battlefield.rs` | W1 | done |
| ~~F4~~ | ~~**Density.** 12–22 oaks per square-kilometre map~~ — **REMOVED FROM THE PLAN (2026-09-04, the owner: "F5 and F4 can go")**. Not closed: the maps keep the F2 counts (Bystra 68, Prokhorovka 60, Orliny 66, Ostrogorsk 62, Mazurski 52 trees); no per-map floor is owed | map blueprints | — | — |
| ~~F5~~ | ~~**No variety.** No dead trees, saplings, stumps, logs or hedges as scenery; one canopy colour per species; no seasonal or climate tint; statics-baked species get no wind~~ — **REMOVED FROM THE PLAN (2026-09-04, the owner)**. What it asked for had partly landed by then and stays: wind on every placed species (F7), four size variants per species and per-leaf age (F8, F10); what it still asked for — new `SceneryKind`s (dead tree, stump, log, hedge) and a per-map canopy tint — is not owed | `world_forge/src/tree/mod.rs`, `foliage.rs` | — | — |
| ~~F7~~ | ~~**Four species have no near rung.**~~ The instanced LOD ladder (`tree_lod`) is oak-only; Poplar, Willow, FruitTree, Pine and every planted tree line bake as Mid-rung statics — so the willow ten metres from the eye on `bystra_clear_afternoon` is the thinned card deck, a wire drawing of a crown, and it never sways (F5). Seen closing F2: the rollout put the species in the maps and the maps show the rung they got. The cost side, measured on #651: statics-baked trees are paid every frame in the far shadow cascade (Bystra +36 388 cascade triangles for F2+F3), where an instanced far rung would be an impostor. **HALF CLOSED (2026-09-02 evening, `feat/flora-species-ladder-f7`): every free-standing tree species rides the ladder.** `LADDER_SPECIES` = oak, poplar, willow, fruit tree, pine, three rungs each under a species × rung handle block (the oak keeps `0xFEE0_0001..3`); the frame builder draws each tree's OWN species rung for this eye; every species' near rung carries the wind (authored factors: willow 1.4, poplar 1.1, oak 1.0, fruit 0.8, pine 0.55); the statics near bake skips exactly the ladder kinds (`ladder_species`, one rule for the frame builder, the bake and the instruments) and keeps the bush. Measured near rungs at the ladder seed: poplar 2,294 / willow 3,292 / fruit 1,608 / pine 3,378 tris (decks 256 / 360 / 200 / 432 cards — the pine deals the widest), ceilings locked. Cold A→B→A on Bystra, `perf_capture` (A = the instrument fix alone with every tree drawn, B = the ladder): GPU p50 "full scene" 10.92 / **10.78** / 12.63 ms (scene_pass 7.82 / 7.70 / 9.11), "full + 7v7" 16.15 / **15.14** / 17.47 (scene_pass 11.83 / 11.10 / 12.86), "gear NEAR" 19.90 / **19.53** / 22.21 — B at or under min(A1, A2) on every headline row, inside a sandwich whose own A1–A2 spread was 1.7–2.5 ms (A2 ran slower in every row from the same 60 °C / 139 MHz start), so the frame verdict is "no measurable cost", not "a measured saving"; the bake side IS a measured saving: Ostrogorsk statics 401,518 → 362,474 vertices (−24.9 k triangles out of the buffer and out of every shadow cascade) with 62 trees off-bake instead of 26. The row's own instrument, `flora_frame_probe` on Ostrogorsk, cold, A1 / B / A2: lineup 11.02 / **10.82** / 11.02 ms (B's baseline is a map with NO trees, 8.47; A's keeps 36 statics trees, 8.94), under-crown 11.06 / **11.06** / 11.09 — worst view PASS on both sides; the same probe run WARM straight after the golden re-record read 17.6 ms and FAIL on the lineup, which is the thermal lesson of Q7 again, not a cost. **Found on the way and fixed first: no battlefield golden and no `perf_capture` frame had ever drawn the ladder** — the look harness, the frame instrument and the per-map view probes submitted the grass ring alone since hero-flora phase 2, so every locked frame and every "full scene" number described a map without its oaks (the fifth instrument-fidelity defect); `battlefield_dressing_objects` + `register_battlefield_dressing_meshes` are the one path now (lock `the_review_dressing_draws_every_ladder_tree_the_battle_draws`), and the battlefield goldens are re-recorded with the trees in them (26 frames; the sniper contact crop's void ceiling re-derived 0.32 → 0.35 openly — the fruit tree by the fence now draws its near rung inside the crop, and the metric cannot tell a leaf from a shadow, O2). The six garage goldens drift on master independently of this PR (0.03–0.05 % of pixels, max delta 164; last blessed at `2f984ea1`, the vehicle changed under them with Z5/Z6/G7) and are NOT folded in here — the scope rule (re-blessed on their own 2026-09-04, `chore/garage-goldens-rebless`: seven frames, the 26 battlefield frames byte-identical on the same run). **F7b CLOSED (2026-09-04, `feat/flora-pine-retired`): the planted tree lines ride the ladder.** Per-station instances at the three-way fitted scale (the fill variant, not the position hash; `hosted_scale` does not re-fit them); the crown hull is its own instanced mesh (`0xFEE0_0060..`, Near and Mid only, 120 tris) so a windbreak at 400 m is an impostor, not a Mid deck in every shadow cascade. Hedge body, cover AABB and felled stumps stay in statics. Lock: `tree_line_stations_ride_the_ladder_at_their_fill_variant`. The frame sandwich of the ring-instanced + scope Near rungs is still F11's. | `scene_build/src/tree_lod.rs`, `tree_line.rs`, `client/src/look_harness.rs` (`battlefield_dressing_objects`) | W4 | the ladder takes a species per instance — done; the tree lines' stations on the ladder (F7b) — done; statics keep the far rung only — done; perf-measured on Bystra — F7's free-standing sandwich done, F7b's cascade win locked by the stations leaving statics |
| F8 | **Trees as data (route 2).** The owner's verdict on the F7 frames (2026-09-02, ~21:00): "these trees, these leaves, this vegetation look tragic, nowhere near real models". Diagnosis: the crown was 250–430 flat cards of 128 px SDF leaf blobs, the trunk a smooth tube in a flat tone, the pine a pole with tufts, the tree line a slab with capsules — a procedural mini-SpeedTree whose detail lived in geometry, while real models carry it in textures. Three routes were put to the owner (stay procedural; trees authored offline in Blender; CC0 models); the call was route 2, with online access for Blender, the Sapling Tree Gen extension and Poly Haven CC0 textures approved. **FIRST TREE LANDED (2026-09-02, `feat/flora-authored-trees`): the oak.** (1) Leaf clusters: eight 512 px sprites of a twig with 220–280 in-house oak leaves, Cycles under a uniform white world (albedo × occlusion + camera-space normals), tiled into a 2048×1024 block pasted into the bottom half of the foliage atlas (page 2048×1024 → 2048×2048, 22.4 → 44.7 MB, 2.1 % of the MX330); the oak's cards deal from those eight slots and are tinted white (the sprite owns its albedo). (2) Skeleton: Sapling grows the oak (scale 15, 22 limbs, 9 twigs per limb, hemispherical crown), the exporter keeps limbs ≥ 10 cm at eight sides and twigs ≥ 6.5 cm at four (near 2,104 tris, 29 pieces; mid 456), turns every Sapling leaf quad into a cross pair of cluster cards (322 / 162) and pulls any card whose stem hangs more than 0.35 m from exported wood onto it (155 of 161) — no floating clusters; `WOTTREE1` files embedded, odd seeds mirror the individual. (3) Bark: Poly Haven `jolcham_oak_bark_01` (CC0, 1 m × 2 m) as an albedo + normal pair on the foliage bind group, sampled triplanar in world space by every BARK fragment through a repeat sampler (the vertex tone only tints, normalised around the authored trunk tone); every species' trunk wears it, the impostor splat keeps the flat tone. Measured: `flora_frame_probe` worst view 12.1 ms (warm) PASS with the clusters, 14.6 ms with bark and the Sapling wood; locks: cluster pages, both rung files and the bark pair on golden hashes; wood within a raised LOD0 ceiling (2,000 → 2,400); the deck a cross-pair deck on the cluster block; sprites rooted at their bottom centre; the normal page flat outside the leaves; the mirrored individual valid. **Owed:** the other species (poplar, willow, fruit, pine, bush) the same way; per-species bark (a texture array); leaf-cluster density and colour by the owner's eye; the tree line on authored trees (F7b — done 2026-09-04); the cluster block sized for one species today. Frames: `target/flora_frame_under-crown.png`, `flora_lineup.png`. **SECOND AND THIRD PR THE SAME NIGHT (`feat/flora-oak-dense-crown`, `feat/flora-species-variants`):** the owner's verdict on the first oak ("work on the crown — leaves denser, branches better made; the same technology for the other species; a natural generator of sizes — small to big, thin to thick, low to tall, dense or not; and place them on the maps with sense"). The crown: clusters of 380–460 leaves (coverage ~45 %), 239 cross pairs of 0.85 m cards, twigs from 4.5 cm kept as four-sided wood, cards pulled onto wood (no floating clusters). Then EVERY species as data in FOUR variants: `bake_clusters.py --species` (a per-species table — outline, size, count, twig, albedo, hanging curtains for the willow, needle fascicles for the pine; four 512 px sprites per species in a 2048×512 row; the atlas 2048×4096, 89.5 MB locked) and `bake_tree.py --species` (a Sapling table per species; variants young / mature / old / sparse by scale, trunk ratio, branch counts, leaf density, crookedness; cards capped per species and area-preserved; the Mid rung keeps the Near's ground lift and its top half-metre of wood verbatim so the tips agree). `variant_of_seed` (a splitmix finaliser) names variant and mirror from any seed: the ladder ships species × variant × rung (72 uploads) and draws each instance's own variant from its position; the statics bake and the tree lines draw populations, the line picking per station the variant that fills its wall; a tree hosted inside a LOS box is fitted under the box top (`hosted_scale`) and the honesty lock measures that. Bark: a `texture_2d_array` of six CC0 tiles, the layer in the vertex's bark role (`4.0 + layer × 0.05`); the willow's first tile was half the oak's brightness and read as black limbs — Willow 02 replaced it. `species_probe`: every species alone, and a grove of its four variants. Ceilings re-set with measurements (LOD0 wood 12,000 — the old willow's pendulous wood; near rung 14,000; planted lines 260 k, Bystra 206 k; FILL_MIN 0.70). **Owed:** the pine's needle sprites (sparse, a thin crown), the willow's curtains (short brushes, not streamers), the bush's single stem, the tree lines' frame cost with authored stations (F7b — done 2026-09-04), map placement with ecological sense and per-map density (step 4 of the owner's programme — F4 redone), the owner's verdict on the species frames | `scripts/flora/*.py`, `assets/flora/`, `world_forge/src/tree/authored.rs`, `leaf_atlas.rs`, `renderer_wgpu/.../foliage_atlas.rs`, `scene.wgsl` (`bark_triplanar`) | W4 (first) | the oak: done; every other species: first pass done, in variants; map placement; the owner's verdict on the frames |
| F9 | **LOD honesty (route 2, the owner after playing, 2026-09-03 ~00:15).** "From afar they look tragic — the LOD kills the quality, don't overdo the budgets and limits. Approaching a tree it visibly changes its look; no game does that, it is a different tree after the swap. The willow looks retarded." What it was: the Mid rung (55–150 m) drew every second cross pair scaled ×√2 and thinner wood — a different crown; the far rung (150 m+) was two crossed quads over a 256×512 sprite SPLATTED on the CPU from that thinned deck; the willow's curtains were short brushes on fountain-arcing limbs over a bark tile half the oak's brightness. **FIXED (`feat/flora-lod-honest`):** the Mid rung draws the SAME card deck as Near (only the wood coarsens to four sides, plus the Near rung's top half-metre of wood verbatim so the tip never moves); the bands are Near 120 m / Mid 300 m / hysteresis 15 m (were 55 / 150 / 8); the impostor is RENDERED in Blender from the authored tree (`scripts/flora/bake_impostor.py`: the mature variant's wood with its bark tile and its cards cutting the species' own cluster sprites, two azimuths at 512×1024 under the white world plus the normal pass) and pasted into the atlas — the CPU splat is gone; the atlas page is 4096×4096 (178.9 MB both pages, locked — the owner took the priority off the limits) with the impostor rows at 512×1024 and two cluster blocks per row; the crossed quads span the exporter's window (`impostor.json`: `half_width_m`, `top_m`), shared data instead of a measured reach; the willow is re-grown (broad crown of gently bowing limbs, 2.6 m curtains of long parallel streamers with 1,000–1,200 leaves per sprite, 720 cards, Willow 02 bark). Every species' impostor pair is a fourth golden. Measured: — (the frame after these bands is the next cold sandwich's). **THE WILLOW IS RETIRED (2026-09-03 ~09:35, the owner: "remove that weeping willow, I don't want a willow at all"):** never planted (Bystra's bank willows are poplars, Mazurski's an orchard, Prokhorovka's balka willows poplars; the horizon mixes follow; lock `no_shipped_map_plants_a_retired_kind` over `RETIRED_KINDS`), not on the ladder, not in a line, no assets, no script table; `SceneryKind::Willow` / `TreeSpecies::Willow` stay as wire identity and draw nothing, like the retired imports. **The editor (2026-09-03, the owner: "can I use all these trees in the editor?"):** it used to upload the statics only, so since F7 a planted tree drew NOTHING in the viewport; it now registers the ladder, binds the leaf atlas and the bark and submits `tree_frame_objects` from its own eye (`editor/src/trees.rs`, locked: every palette tree is a drawn rung), and the willow is out of the palette (locked against `RETIRED_KINDS`). **The square far trees (2026-09-03 ~10:00, the owner after playing: "the farthest LOD looks tragic and SQUARE, too big a difference"):** the bake derived the sprite window's width from its height (`half_width = top / 4`, a 1:2 slot) and clipped every broad crown flat at both sides — oak, fruit tree, pine and bush all touched both frame columns (measured); the view now renders at the crown's own aspect with square pixels and is squashed into the 1:2 slot, the quad spanning the manifest window, so the whole crown is in the frame (locked: no opaque texel on the sprite frame; oak's window 5.0 → 10.3 m half-width). **The dithered cross-fade (2026-09-03 ~11:00, the owner: "do the dithered transition at 300 m"):** over 290–310 m both rungs are submitted, each with a complementary screen-door lane (`RenderObject::dither` → instance `tint.w`; a 4×4 Bayer threshold per pixel in `noise_common.wgsl`, the colour pass and the camera prepass keep exactly one rung per pixel, the shadow passes let the dominant rung cast alone); the impostor edge has no hysteresis any more — a continuous band has no edge to flicker on (`tree_lod::impostor_weight`, locked: both rungs in the band with lanes −w/+w rising with distance, solid outside; `oak_probe` view `fade_300m`). **THE PINE IS RETIRED (2026-09-04, the owner: "the pine is out entirely"):** the same way as the willow — never planted (Orliny's 44 pines are 20 oaks and 24 poplars, Mazurski's 28 are 16 oaks and 12 poplars; the horizon mixes follow: Orliny Oak 0.55 / Poplar 0.35 / FruitTree 0.10, Mazurski Oak 0.50 / Poplar 0.25 / FruitTree 0.25; `RETIRED_KINDS` and `no_shipped_map_plants_a_retired_kind` cover it), not on the ladder, not in a line (`plants_in_a_line` is oak and poplar), out of the editor palette, assets and bark tile deleted; `SceneryKind::Pine` / `TreeSpecies::Pine` stay as wire identity and draw nothing. The "pine's sparse crown" debt of F8–F11 dies with it. **Owed:** the owner's eye after driving; a cold perf sandwich with the ring instanced and the scope's Near rungs | `scripts/flora/bake_impostor.py`, `assets/flora/*/impostor_*.png`, `world_forge/src/tree/leaf_atlas.rs`, `authored.rs` (`impostor_pages`, `impostor_window`), `scene_build/src/tree_lod.rs` (bands) | W4 (first) | the swap invisible at 120 m, the sprite a tree at 300 m, the willow a willow — by the owner's eye |
| F10 | **Leaves 2.0 (the owner, 2026-09-03 ~11:00: "the leaves must be improved, corrected and refined — think hard about this").** What the 22 m frame and the cluster page showed: blades 15–23 cm (an oak leaf is 8–12) with a smooth lettuce outline, no midrib, no underside, no translucency, one green; and the crown lit as a tumble of cards, each answering the sun with its own quad plane — the cards facing away, or under the crown's own shadow-map lid, went BLACK. **Done (`feat/flora-leaves-2`):** (1) the cluster generator (`scripts/flora/bake_clusters.py`): real blade sizes at 2–3× the count (oak 1,050–1,200 a sprite), species margins (the oak's 4–5 rounded lobes with deep sinuses, the poplar's crenate teeth, a fine serration on the oval), a midrib crease, a two-sided material (darker glossier top, paler matte underside) with a translucent lobe, per-leaf age from the object's random (young tips yellower), a petiole that sets the blade OFF the twig (a row on the axis read as a bottle brush), rosettes at the shoot tips, phototropic layering with a per-leaf tumble, two-needle pine fascicles, pooled materials; (2) the lighting: every card carries the CROWN's normal (`authored::bent_card_normal` — outward from the centroid, lifted, blended 75/25 with the quad's facing; both rings of a card share it), the crown shades as a volume; `foliage_radiance`: the crown's self-shadow is porous (leaves receive the map at 60 %), the transmission lobe is strong (0.55 of the key) with a floor in shade, wrap 0.6, ambient ×1.2. Measured on the oak at 22 m: crown pixels 85k → 121k (fuller), no black quads (by eye); mean crown luminance 101 → 79 (denser interior in shade — honest). **Owed:** the impostor's colour vs the Mid deck's under the crown normals (a measured match), the judgement of every species' new sprites by the owner's eye, the pine's sparse crown (its rung geometry) | `scripts/flora/bake_clusters.py`, `assets/flora/*/clusters_*.png`, `world_forge/src/tree/authored.rs`, `scene_build/src/foliage.rs`, `renderer_wgpu/src/shaders/lighting_common.wgsl` | W4 | leaves that are the species' leaves, a crown that is a volume — the owner's eye |
| F11 | **LOD continuity (the owner, 2026-09-03 ~12:00: "let us think how to develop the LOD so the difference between distances — graphic, visual, readable, quality — shrinks; eliminate entirely the situation where I drive and see a tree change its graphics"), plus three defects: the pine's sparse crown, the impostor's colour vs the Mid deck under the crown normals, the far WOOD invisible ("on the horizon the tree itself is practically invisible, only the leaves").** The doctrine, five rules: (1) ONE source per tree — every rung is derived from the same authored individual (Mid = Near's deck; the impostor rendered from it), never a separate model; (2) NO pop anywhere — every swap is a dithered cross-fade band, 20 m wide, both rungs on screen with complementary Bayer lanes, no hysteresis (a continuous band has no edge): now BOTH edges, 120 m and 300 m (`tree_lod::rungs_at`, locked by a metre-by-metre sweep 1–600 m: always one or two objects, exactly one solid lane, pairs are adjacent rungs); (3) ONE lighting model across rungs — the impostor's normal page carries the CROWN normal the cards carry (`bake_impostor.py`: outward from the centroid, lifted, 75/25 with the quad), so the Mid deck and the impostor answer the sun alike; (4) the WOOD survives distance — a limb thinner than a pixel dies under the alpha test, so the impostor's wood is thickened by 0.10 m along its normals (`WOOD_THICKEN_M`: a trunk 0.5 → 0.7 m = three pixels at 300 m, a limb 0.14 → 0.34 m), invisible as an error, decisive for the silhouette; (5) PARITY IS MEASURED, not eyeballed — `look_harness::the_impostor_and_the_mid_rung_read_as_one_tree_across_the_band` renders the oak at 100 m (Near, the truth), just before the 300 m band (Mid) and just after it (impostor), and locks: the far rungs' crown colour within 15 % a channel of each other, each far rung's crown coverage within 25 % of the Near crown brought to its distance by perspective, and wood pixels present at both far distances. What the instrument found before it went green: the impostor drew 1.9× the crown and 7× the wood. Two causes, both fixed: (a) ONE impostor per species — the mature variant's — while the Near and Mid rungs are the variant's own tree, so a young oak became the mature one at 300 m: now ONE IMPOSTOR PER VARIANT (`bake_impostor.py` loops the four variants; 256 × 512 a view, eight views a species on a 2048 × 512 page; the window per variant in `impostor.json`; atlas slots per (species, variant, view) in the four rows above the cluster block; `impostor_window(species, variant)`, `impostor_rect(species, variant, which)`, quads per variant); (b) the two crossed quads seen from an oblique eye are TWO overlapping silhouettes — now the quads are two objects (quad B in the variant's spare handle slot) sharing the impostor's screen-door WINDOW by view azimuth (cos² / sin²), so the cross never doubles; the dither lane became a window `[lo, hi)` of the Bayer threshold (`RenderObject::dither`, instance bytes 84..92, location 15 in every foliage-cutting pass) and the rungs and quads of one tree PARTITION the unit interval — every pixel drawn by exactly one object, the object holding 0.5 casts the shadow (locked by the 1–600 m sweep). Also: Cycles' transparent bounces 16 → 128 (a crown is hundreds of cut-out cards deep; a low cap made the impostor a solid lid), the wood thickened in full only below the crown (0.10 m), a third of it inside (a twig thickened by 0.10 m is a limb). The pine: twice the cluster anchors per twig (8), more twigs (48/14), bigger cards (0.7 m), a 360-pair fill budget — a dense dark cone. **Second PR (2026-09-03 ~14:00):** the parity lock covers the 120 m band too (Near at 108 m vs Mid at 132 m: colour within 15 %, coverage within 25 % of 100 m, wood at both — measured: crown 5009 vs 4565 at 100 m, colour within 5 %); the wood's PIXEL GUARD in the scene vertex stage (`scene.wgsl`: every bark surface pushed out along its normal by `clamp(0.8·pixel − 0.04, 0, 0.5)` metres at its depth — nothing under 25 m, a 0.14 m limb 0.34 m at 300 m, the impostor's own thickening; pixel size from `fog_params.w` × depth / `ssao_params.w`); the horizon RING rides the ladder (`tree_lod::tree_frame_objects_with_backdrop`, the ring cached per map in `TreeLodState`; the backdrop statics bucket bakes EMPTY) so every ring tree takes the azimuth windows and the fade bands — baked as crossed quads it was two silhouettes from every oblique eye; the battle, the editor and every probe submit the ring this way (locked: every ring tree draws, from the map's middle as windowed impostors). **Third PR (2026-09-03 ~14:45, the owner in the scope at 16×: "some trees wear a grid; some leaves look detached and flat; some trees are paler, light-dotted"):** those were impostors and fade grains 300 m away shown at 16× as if at 19 m. The bands are judged at the APPARENT distance now — `TreeEye` (`tree_lod`): position, the lens's magnification `tan(48°/2)/tan(fov/2)` and its view cone; `distance / magnification` picks the rung, so the scope sees Near rungs, solid; and a tree outside the cone (its bounding radius widening the cone) is not submitted at all, which is what lets a few-degree scope draw every tree it sees in full without paying for the map behind it. The battle and the editor pass the camera; the instruments pass `TreeEye::at` (every tree, the reference lens — the goldens unchanged). Locked: at 16× a tree at 350 m is one solid Near rung, turned away it is not in the frame, at the reference lens it is the impostor. **Fourth PR (2026-09-03 ~14:55, the owner: "the dither grid still happens outside the scope, in the normal camera"):** the impostor's two quads shared the window by cos²/sin² — a mix of two DIFFERENT views of the tree at every intermediate azimuth, visible as a grid on every far tree and the whole horizon ring. Now exactly ONE quad draws by azimuth (A nearer the Z axis, B past the diagonal) and the screen-door hand-over exists only across ±4° around the diagonal (`IMPOSTOR_AZIMUTH_BAND_HALF_DEG`; locked: at 30° one solid quad, at 45° the 50/50 pair). The cost is honest: at the diagonal a single quad is foreshortened to 0.71 of its width — inside the 25 % coverage lock. **Fifth PR (2026-09-03 ~15:30, the owner on Bystra: "dithering still shows in places; some leaves look like dots, detached from the tree"):** the dots were the cluster sprites' deep mips — the coverage-PRESERVING chain held a twig sprite at its own 17 % and a card at 200 m thinned into a cloud of dots, while the impostor (rendered from every layer) was a mass, so the 300 m swap was dots → mass. Now `MipMode::AlphaCoverageGrowing` (`renderer_api::texture`): from level 3 on the cutout coverage grows +35 % a level to a 95 % ceiling — a card deck becomes the mass on the way there (locked: the cluster block's level 5 carries ≥ 8 points more area than level 2, no deep level thinner than the one before). Found on the way: `preserve_cutout_coverage` returned unscaled when the target reached past the texels with any alpha (the growing chain came out THINNER than the held one) — the rank now clamps to them. The fade bands are 10 m wide, not 20 (`*_FADE_HALF_M` 5). And the parity instrument stands on FLAT ground now: on Prokhorovka's relief a crest hid the trunk and the lower crown at 127 m and the instrument read it as a 26 % thinning of the rung — it now also renders the Near rung at the Mid rung's distance (the lens forced to 2×) and locks the two within 10 %. Measured on flat ground: 100 m 5438, 113 m 5530, 127 m 5616, 288 m 6061, impostor 5834 crown pixels brought to 100 m. **Owed:** the owner's eye after driving; a cold perf sandwich with the ring instanced and the scope's Near rungs | `scene_build/src/tree_lod.rs`, `scripts/flora/bake_impostor.py`, `scripts/flora/bake_tree.py` (pine), `client/src/look_harness.rs` | W4 | no visible swap at any distance while driving; the wood on the horizon |
| ~~F6~~ | ~~**No open flora row anywhere**; D5/D12/D13/D14 all closed; the backdrop comment claims kilometres at 40 m~~ **CLOSED with F1 (#642)**: the backdrop is the impostor ring past the border and its comment says what it is; the flora debt lives in this register (F2–F5) | `backdrop.rs` | W1 (first PR) | done |

### S — the shot, and the hit

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| ~~S1~~ | ~~**No dynamic light.** The renderer has the sun and nothing else; a 100 mm gun at dusk lights neither its glacis nor the ground, and an impact lights nothing. This is the single largest reason a shot reads as a decal~~ **CLOSED (2026-09-02, `feat/shot-lights-s1`).** The profile's six local slots (`LocalLight`, all-off outdoors, already read by the terrain, scene and vehicle shaders) are the pool the flash pours into. `LocalLight::muzzle_flash(position, recoil_scale)` / `impact_flash(position, strength)`: reach with the square root of the S3 momentum scale, energy linearly; `at_energy` dims without shrinking (a flash goes out, it does not contract). `fx/lights.rs` keeps a bounded pool of pulses born on the FX clock — the muzzle, a shell absorbed by the world, a strike on armour (a penetration harder than a bounce, HE hardest, water swallows the light) — and hands the six strongest live pulses to the frame's lighting between the weather sample and `set_scene_lighting`; the profile stays all-off, so the outdoor-profile lock holds and no golden moves. Measured: under Prokhorovka's golden evening the ground under a D-10's flash 0.582 → 0.907 luma (+0.325), the corner unchanged, the burned-out flash byte-identical to the profile. MX330 (`perf_capture`, new row "full + 7v7 + 6 shot lights"): two interleaved rotations on 2026-09-02 — without the early-out "full + 7v7" 29.78 → "+ 6 shot lights" 33.50 ms p50 wall (+3.72 ms; +5.68 ms scene_pass GPU); with the out-of-pool early-out in `local_pools` 28.10 → 28.80 ms p50 wall (+0.70 ms; +0.03 ms GPU on the 14:49 rotation), p95 31.0 ms. Locks: the pool law; the dusk ground ≥ +0.25 luma and byte-identity when the flash dies; a shot lights its frame and the light dies with the flash; a heavier round lights further and harder; six strongest of eight, strongest first, forty keep the pool bounded; what the shell died against sets the light | `renderer_api/src/lighting.rs`, `client/src/fx/lights.rs`, `app/render.rs`, `app/ingest.rs`, `renderer_wgpu/tests/local_lights.rs`, `examples/probe/perf_capture.rs` | W1 | done |
| S2 | **Sniper mode gets nothing.** The camera kick returns early when scoped, and most aimed shots are scoped. **Owner's decision 2026-09-02: the picture under the player's hand in the scope never moves** — not on fire, not on being hit. The first draft of this row (a rotational tremor) is withdrawn; the hill spring (#616) had already taught that a feel channel which moves the aim is a channel the player fights, and `docs/battle-camera-policy.md` law 0 says so | `client/src/camera/smoothing.rs:129-136`, `camera_feel.rs` ("leaves sniper rigid" stays a promise) | W1 | feedback that displaces nothing: the muzzle's flash and smoke in the lens (S1), the dispersion ring's bloom (already live), a ~120 ms pulse of the scope housing, the mechanical audio layer (S4); lock: a fire event in sniper leaves the view matrix bit-identical |
| ~~S3~~ | ~~**Nothing is caliber-scaled but audio and tracer width.** Flash 1.0→1.6 m, smoke 8 particles, dust ring 10, recoil stroke 12 m/s, hull impulse 0.16 rad/s, camera kick 0.9/0.5 m/s — all constants, identical for 75 mm and 128 mm~~ **CLOSED (2026-09-02, `feat/shot-momentum-s3`).** `ShellSpec::recoil_momentum_kg_mps` (mass × muzzle velocity) and `recoil_scale` = √(momentum / the D-10 BR-412's 14 052 kg·m/s), clamped 0.5–1.8: the T-54 keeps every number its channels were tuned on, the 12.8 cm Pak 80 is ~1.36×, the 7.5 cm KwK 42 ~0.67×. The scale rides the replicated shot (`FireEvent::recoil_scale`, from the shooter's gun and selected slot) into every channel: the flash's size and speed, the smoke's count and speed, the dust ring's count and speed, the barrel's stroke (`GunRecoil::kick`), the hull's rock (`HullAttitude::fire_impulse`), the camera's nudge (`fire_kick`). No wire change. Locks: the Pak 80 exceeds the KwK 42 on every channel (particles, flash size, barrel stroke, hull impulse, camera displacement) and the D-10 sits at 1.0 | `game_core/src/weapon.rs`, `client/src/fx/fire.rs`, `fx/emitters.rs`, `engine/src/components.rs`, `engine/src/attitude.rs`, `camera/smoothing.rs` | W1 | done |
| ~~S4~~ | ~~**No mechanical layer.** No breech clack at fire, no casing, no recoil-cycle metal, no dust shaken off the hull — one synthesized blast with nothing before or after it~~ **CLOSED (2026-09-02, `feat/shot-mechanics-s4`).** `BreechCycle` (`audio/src/voices/breech.rs`, pure DSP): after the report the tube slides back (a band of noise swelling and dying), the buffer takes the stroke (a low thud), the recuperator runs the gun out (a hiss), the tube seats in battery (the brightest clack of the cycle), the breech drops and the case rings out (an inharmonic cluster) and meets the floor with one bounce — every timing and the case's pitch from caliber (a 128 mm strokes 0.16 s, runs out 0.46 s and its case rings near 1.5 kHz; a 75 mm 0.09 s, 0.28 s, 2.35 kHz), exposed as a `BreechTimeline` for anything that wants to move with it. The mixer spawns it listener-local, a step under the report, for the player's OWN shot only — nobody hears a stranger's breech across a field. On the picture side the shot shakes dust off the deck: `FxSystem::hull_dust` lifts sandy motes off the turret ring's roof along and across the hull's heading, count by recoil momentum (S3), from `FireEvent::deck` (the turret ring posed with the hull). Locks: the case rings after battery and brighter than the slide; a bigger gun cycles longer and rings lower; bit-exact per seed; the own shot carries two voices and a far shot one; a Pak 80 shakes more dust than a KwK 42 and every mote lifts off the plates | `audio/src/voices/breech.rs`, `audio/src/mixer.rs`, `client/src/fx/emitters.rs`, `fx/fire.rs`, `app/ingest.rs` | W1 | done |
| ~~S5~~ | ~~**Being hit is mute in the body.** Camera shudder only; `HullAttitude` has `fire_impulse` and no `hit_impulse`; no "armour held" callout although the exactly-once lane already carries the absorbed-impact truth; no screen effect~~ **CLOSED (2026-09-02, `feat/hit-in-the-body-s5`).** `HullAttitude::hit_impulse(bearing_hull_rad, energy)`: an incoming shell rocks the STRUCK hull on the same springs as the shot and the terrain, from the side it came in — a frontal strike lifts the nose, a strike on the right side rolls the hull away, a rear strike dips the nose — at 0.22 rad/s × the shooter's round on the S3 momentum scale (a bounce as hard as a penetration: the plate held, the shell still pushed; HE ×1.3), settling in one nod. `PresentationWorld::apply_hit_impulse` carries it to every tank in view, not only the player's; the client derives the bearing from the hit point in the target's hull frame. The scope is rigid on a hit (the S2 decision): `damage_shudder` moves nothing in sniper — the lock went from "the scope visibly dips" to "the eye does not move by a bit". The verdict rides the direction arc: HELD in amber with the millimetres that held (effective armour / the shell's penetration), PEN in red with the millimetres that did not, both from the exactly-once damage lane, fading with the arc. Sound was already there (the plate's clang with the outcome in it). Locks: front/side/rear impulses by axis and a heavier round further, the hull settling level; the presentation cue reaches the struck entity and ignores an unknown one; the sniper eye byte-identical through a hit; a bounce draws HELD 162/150 in its colour and a pen PEN in its own | `engine/src/attitude.rs`, `engine/src/sync_cues.rs`, `client/src/app/ingest.rs`, `camera/smoothing.rs`, `hud/hit_direction.rs` | W1 | done |
| S6 | **The roadmap lists fire feel DONE**, so it was never audited | `docs/ROADMAP.md:21` | W1 (first PR) | reworded — done |
| ~~S7~~ | ~~**The number out-pixels the world 7:1 and outlasts it 8:1.** At 300 m in the scope a T-54 is 64 px wide; a bounce's whole world answer is ~60 px² of faint additive spark for 0.3 s; the damage number is 42 × 23 px of opaque saturated ink for 2.5 s (~400 px²)~~ **CLOSED (2026-09-02, `feat/hit-answers-s8`).** The number is demoted under the world: `DAMAGE_NUMBER_EM` 0.065 → 0.045, TTL 2.5 → 1.2 s, fade 0.8 → 0.5 s. Lock: at 300 m in the 8° scope a three-digit number's ink is at most twice the penetration flash's projected area (1.4 m at 300 m) and the number is gone inside a beat | `client/src/hit_indicator.rs` | W1 | done |
| ~~S8~~ | ~~**The target does not move when hit.** No hit impulse exists for any hull (`fire_impulse` is the only one); no turret jerk~~ **CLOSED (2026-09-02, `feat/hit-answers-s8`; the hull half landed with S5).** The hull: `HullAttitude::hit_impulse` (S5) — locked here against the row's bar: a landed round of the reference calibre deflects the target at least as far as its own shot rocks it. The turret: `TurretJerk` (engine component, a critically damped yaw offset on the ring, ω 12, ≤ 0.06 rad) kicked by the strike's tangential push relative to where the turret points — square on the front turns nothing, a cheek hit turns it away from the shell — from the four turret zones of the damage event, through `PresentationWorld::apply_turret_jerk`; the authoritative yaw is untouched. Locks: the presented turret flinches away from the blow by a visible fraction of a degree, never a spin, and the ring brings it back inside a second; an unknown hull flinches nothing | `engine/src/components.rs`, `engine/src/sync_cues.rs`, `engine/src/world.rs`, `client/src/app/ingest.rs` | W1 | done |
| ~~S9~~ | ~~**The penetration signature is one size and non-pens have no answer.** One 0.09 s flash + 7 grey puffs for every pen; no spall cloud on a bounce; HE on armour takes the same path as AP (no fireball, no smoke ring) because `shell_step.rs:134,175` emits no `ShellImpact` for tank hits~~ **CLOSED (2026-09-02, `feat/hit-signature-s9`).** The signature scales with the share of the pool the round took (`armor_hit_directed(.., damage_fraction)`: flash 1.4 m × 0.6–1.5, smoke 4–11 puffs) — a 900-HP hole reads bigger than a 120-HP graze. A non-pen answers with `bounce_spall`: five occluding grey puffs of spall and paint off the plate beside the sparks. And the sim emits a `ShellImpact` (surface `Hull`) for every strike on a live enemy, beside its damage event — the event ids interleave (damage, impact) — so the client draws an HE round's `he_burst_on_armour` from the impact's approach: a metres-wide hot core, a ten-puff occluding smoke ring thrown back along the shell, sparks; everything else about a hull strike (sparks, signature, light, clang) keeps riding the damage event, so nothing doubles. Locks: even the smallest pen out-areas the spark fan by ≥ 4× and a near-kill flashes bigger than a graze with more smoke; a bounce carries an occluding row and its sparks stay additive; the HE burst is more than twice a spark fan with a ≥ 2 m core and a ring thrown back; the sim's strike lands as a hull impact at the plate's hit point with the round's type, approach and calibre, and the event-truth ids still survive snapshot windows | `client/src/fx/impacts.rs`, `client/src/app/ingest.rs`, `sim/src/shell_step.rs`, `sim/src/state/event_truth_tests.rs`, `sim/tests/combat_pipeline.rs` | W1 | done |
| ~~S10~~ | ~~**Sparks detach from a crossing hull.** Tanks render interpolated one snapshot (50 ms) behind; the FX spawn at the world hit point — up to 0.5 m ≈ 10 px off at 300 m; the breach hole (reliable lane) can open 50 ms before the sparks~~ **CLOSED (2026-09-02, `feat/seated-fx-s10`).** A particle carries a `seat`: `Some(tank)` means its position and velocity are in that hull's local frame, and the batch draws it through the hull's INTERPOLATED pose — `render_state.interpolated_tank` → `fx::pose_of`, the same frame the breach decal already rides — while a seat whose hull is gone draws nothing. `FxSystem::seated(tank, &pose, |fx| …)` wraps the existing emitters: they keep speaking world space at the SIM pose the hit was resolved in, and the spawn converts each particle into hull-local (the exact inverse of the draw). The ingest seats the strike's sparks, signature and spall on the damage event's target, and the HE burst on armour on the target named by the damage event of the same shell. Locks: at a 10 m/s crossing (the sim's hull 0.5 m ahead of the drawn one) the seated signature's entry flash sits within 5 cm of the seated decal after a presented frame, where the world-space spawn hung ≥ 0.4 m behind the plate; a seat without its hull draws nothing and world particles do not care | `client/src/fx/particle.rs`, `fx/mod.rs`, `fx/seated_tests.rs`, `app/ingest.rs`, `app/frame_scene.rs` | W1 | done |
| ~~S11~~ | ~~**The armour clang is inaudible at range.** Gain 0.8 × 18/318 = 0.045 at 300 m (−27 dB), air low-pass to 3.3 kHz, 0.87 s delay — under the engine bed the pen thunk and the ricochet whine are one sound~~ **CLOSED (2026-09-02, `feat/hit-answers-s8`).** `VoiceSlot::positioned_with_reference`: a source carries its own reference distance; `ArmorStruck` wears 60 m (`ARMOR_STRUCK_REFERENCE_M`) where every other voice keeps 18 — steel on steel carries, so a 300 m hit sits at −17 dB instead of −27. Measured at 300 m: pen 0.0080 RMS / 944 Hz ZCR, ricochet 0.0055 / 868 Hz; at the old reference the same pen was 0.0027. Locks: both above a 0.003 RMS floor the old reference fails, the pen the louder answer (the interior thunk) by ≥ 1.25×, the two timbres ≥ 5 % apart in zero-crossing rate | `audio/src/spatial.rs`, `audio/src/mixer.rs` | W1 | done |
| ~~S12~~ | ~~**A landed pen gives the shooter nothing but pixels.** No camera micro-kick, no voice or ribbon; only the shooter's own hit shakes the camera (`ingest.rs:79-83`)~~ **CLOSED (2026-09-02, `feat/hit-answers-s8`).** `BattleCameraController::pen_kick(view_yaw, damage_fraction)`: the shooter's landed penetration nudges the third-person rig forward and down by the share of the target's pool the round took (0.6 / 0.3 m/s at a full share — a fraction of the shot's own kick, a confirmation not a second recoil); the scope stays rigid (S2) and keeps its hit marker. Fed from the damage lane where the shooter is the player. Locks in `camera_feel.rs`: a 30 % share nudges the rig forward and slightly down and recovers, a 90 % share ≥ 2.5× further, the sniper eye byte-identical | `client/src/camera/smoothing.rs`, `client/src/app/ingest.rs` | W1 | done |
| ~~S13~~ | ~~**The player's own shot is presented from the 20 Hz snapshot.** Every channel of the shot — flash, light, smoke, deck dust, barrel stroke, hull rock, camera nudge, report, breech cycle — is driven from `snapshot.shots_fired`, so between the trigger and the flash there are 0–50 ms of random delay even in the local battle, plus half the round trip remotely~~ **CLOSED (2026-09-02, `feat/own-shot-s13`).** `OwnShotPrediction` (`client/src/app/own_shot.rs`) predicts the one fact the server will decide, by the server's own rule read from the client's copy of the state (live hull, gun and rack standing, a loaded slot, the reload — the latest snapshot's remainder less the ticks since it, or the client's own lockout after a shot it already predicted, whichever is longer): accepted → `fire_predicted_shot` fans a `ShotFired` out in the trigger's own fixed tick, from the tick-accurate predicted pose, through the very path the replicated shot takes (`resolve_shots` → `apply_fire_events`); held (inside `FIRE_BUFFER_S`) → the fan-out is scheduled for the tick the reload ends; refused → nothing. The replicated `ShotFired` that follows is matched by count in `ingest.rs` and skipped; a prediction the server never confirms expires after 0.35 s and realigns the count — one flash without a shell, never a missing flash for a shell that left. Also: `fx/seated_tests.rs` gained G7's two spring velocities (S10 and G7 landed in parallel lanes and the client lib tests did not compile on master) | `client/src/app/own_shot.rs`, `own_shot_tests.rs`, `loop_step.rs` (`predict_own_shot`, `fire_predicted_shot`), `ingest.rs` | W1 | `the_shot_is_in_the_tick_after_the_trigger_before_any_snapshot` (particles ≥ 10, the shot's light in the frame's slots, the barrel in recoil, exactly one fan-out, one tick after the trigger with no snapshot crossed), `the_replicated_twin_is_skipped_so_nothing_plays_twice` (one fan-out after the snapshot crosses, zero unconfirmed), `a_click_inside_the_reload_predicts_no_second_flash`, `a_held_click_fires_when_the_reload_ends`, `an_unconfirmed_prediction_expires_instead_of_swallowing_the_next_shot`, `a_prediction_skips_its_replicated_twin_and_nothing_else`; `shot_probe` (Bar 1) still owed as the frame-level instrument |

### Q — the frame

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| Q1 | **Three whole-mesh re-uploads on the render thread.** Any HE round on terrain: the entire ground mesh via `create_buffer_init` (a fresh GPU allocation, never scaled to the dirty span, `ground.rs:287-308`); the whole card meadow when the crater touches it (`dressing.rs:42-53`); any cover phase step or scar: the worker re-bakes only dirty buckets, then the harvest reassembles all `STATICS_BUCKET_COUNT` buckets and uploads the lot (`terrain.rs:22-36`). The J fixes bounded the *frequency*; the per-event cost is still the whole mesh | `client/src/app/render.rs:84-96,177-216`, `renderer_wgpu/src/scene_renderer/{ground,dressing,terrain}.rs` | Q lane | persistent capacity-reserved buffers with `write_buffer` over the dirty vertex span; statics as per-bucket buffers so a bucket re-bake uploads that bucket; lock: `battle_age_cost` prints uploaded MiB per event and it scales with the change, not the mesh |
| Q2 | **No hitch instrument on a live battle.** `perf_capture` has p50/p95/p99/max on a static, sim-free path; `battle_age_cost` sizes the ground row but not the statics row; nothing measures "the longest frame and what ran in it" under sim + render + audio + network together. Found closing F1: `perf_capture` and twelve more probes never bound the leaf atlas, so from Drzewa 3.0 PR6 every card and impostor in their frames was an opaque white quad — the 2026-08-09 numbers were taken with that instrument. Fixed in F1 (`bind_battle_foliage_atlas` + the source lock `probe_foliage_atlas.rs`). Found closing F7: the look harness, `perf_capture` and the view probes never submitted the instanced tree ladder, so every battlefield golden and every "full scene" frame time since hero-flora phase 2 was a map without its oaks — fixed with `battlefield_dressing_objects`, the one path the battle, the goldens and the instrument now share | `client/examples/probe/perf_capture.rs:613,672`, `battle_age_cost.rs:237-285` | Q lane (first) | `battle_age_cost` extended: the statics row, and a live `battle_host` session firing HE on a schedule with the wall-clock-minus-fence percentile method — p99/max on the frame that harvests a real crater or collapse |
| ~~Q3~~ | ~~**The last honest number is over budget and 23 days stale.**~~ **CLOSED (2026-09-02: the cold rotation in Q7 is the honest number — 11.89 / 14.86 / 17.96 ms GPU p50 on `3377bb72`, and the warm state is the open problem, named there.)** 2026-08-09, MX330, 1×: full scene 11.95 ms, full + 7v7 15.38 ms, 7v7 with gear forced NEAR 17.88 ms — 1.21 ms over before any hitch; no combined worst case (dense trees + close 7v7 + a hitch) has ever been measured; the release probe binary predates ~100 commits | memory `battle-frame-budget-2026-08`, `docs/ROADMAP.md:48-49` | Q lane | a fresh A→B→A on current master as the lane's first PR; the deficit assigned per item with a measurement, never fleet-wide; every renderer row in this register (S1, F1, F4, N2, N6, H5, B3) lands with its own delta |
| Q4 | **A blocking `std::sync::Mutex` on the real-time audio thread**, shared with the main thread's `with_engine`; the docstring claims it never blocks the callback's cadence, which nothing enforces | `client/src/audio_out.rs:6,48,89-93` | Q lane | a lock-free parameter mailbox (triple buffer or atomics); lock: the audio callback path contains no lock |
| ~~Q5~~ | ~~**The physics arithmetic depends on which crates are in the build.**~~ **RETRACTED and CLOSED (2026-09-02, `fix/gate-arithmetic-parity-q5`).** What was seen closing A8: `ramming_a_live_hull_does_not_buzz_the_camera` failed 3 of 3 under `cargo test -p client` on clean master (16 corrections against 12) and passed under the workspace gate. The first explanation — `glam` on `nostd-libm` in one graph and `std` in the other — was wrong: `cargo tree -e features` resolves `glam`, `glamx`, `num-traits`, `libm` and `parry3d` IDENTICALLY for `-p server`, `-p client` and `--workspace` (verified before writing this). The real cause was build state: the main tree's incremental cache dated 2026-08-22 produced a client test binary whose physics disagreed with the same source compiled fresh; one `CARGO_INCREMENTAL=0` rebuild and the lock passed, and every incremental run since agrees. Closed with two rules rather than a fix-once: the gate's answer may not depend on what a previous build left behind (first written as `CARGO_INCREMENTAL=0` on every run; **amended by Q6**: that cost a full recompile of every changed crate per run, so the rule is now "never kill a build, `cargo clean -p` after one that was, and `verify.ps1 -Deep` for the daily run and whenever a number looks wrong for no reason"), and `quality/tests/math_backend_parity.rs` locks the hazard that was suspected: the server and the client resolve every math crate to the same features, and the workspace gate resolves them exactly as a client-only build does. **The lock earned its place on its first run**: `num-traits` resolved to `{i128, libm}` in the `server` graph and `{default, std, i128, libm}` in the `client` graph — the dedicated server's `Float` trigonometry (parry3d ← nalgebra ← simba) on `libm`, the client's on `std`'s intrinsics. Pinned by `num-traits = { features = ["std"] }` carried by `physics`, so every graph with the hull physics carries the same arithmetic. The deeper choice — `libm` everywhere, for cross-platform replay determinism — moves every physics number and belongs to G7's re-measurement, not to a gate PR | `scripts/verify.ps1`, `crates/tooling/quality/tests/math_backend_parity.rs`, `Cargo.toml`, `crates/runtime/physics/Cargo.toml` | Q lane | the gate independent of stale state (as amended by Q6); server ≡ client ≡ gate on arithmetic features — done |
| ~~Q6~~ | ~~**The gate costs more than the work.**~~ **CLOSED (2026-09-02, `feat/gate-diet`).** The owner: "buildy, pełne verify itd. trwają za długo… przy każdym punkcie zabiera to za dużo czasu." Measured on the reference laptop: 25–35 minutes per PR, three causes. (1) A fresh worktree per PR — cargo keys a workspace crate's artifacts by its path, so every PR recompiled all 33 crates and every test target into the shared `target` as if new; the per-session worktree rule was written against two sessions clobbering one tree, not against one session keeping one tree. (2) `--all-targets` with codegen: forty probe modules and the client's test targets rebuilt for a change that touched neither, then clippy's check of the same targets. (3) Q5's `CARGO_INCREMENTAL=0` on every run: minutes per changed crate instead of seconds. Fixed as rules: one worktree per session for the whole session, branches inside it; a PR gate (`scripts/verify-pr.ps1 -Crates …`: fmt, clippy over the whole workspace and all targets in check mode, `quality`, the touched crates' tests — no example or bench codegen) and the full gate once a day over what landed and before any merge touching examples, benches, wire, replays or physics numbers; incremental back on, `-Deep` for the daily run and for doubt. `verify-quick.ps1` retired (the PR gate with no crates named is its successor; it still compiled every example). Not done, awaiting a measurement: `rust-lld` as the MSVC linker and rustc's parallel front-end (`-Z threads`, available on the pinned nightly) — both change the profile hash, so each is one cold rebuild to test | `scripts/verify-pr.ps1`, `scripts/verify.ps1`, `docs/engineering-rules.md`, `CLAUDE.md` | Q lane | the gate costs what the PR costs, not what the day costs — done |
| Q7 | **The frame drifts with the heat of the box, not with the code — and the budget must hold at the sustained clock.** A COLD rotation on 2026-09-02 15:25 (`perf_capture`, master `3377bb72`, MX330 at 57 °C / 139 MHz at launch, nothing else running; log `wot-audit/scratch/perf-cold-3377bb72.log`) gives GPU p50 "full scene" 11.89 ms, "full + 7v7" 14.86 ms, "7v7 gear NEAR forced" 17.96 ms — Q3's 11.95 / 15.38 / 17.88 of 2026-08-09 to within half a millisecond, after every renderer row landed since (Drzewa 3.0, the tree lines, the light campaign, Z1, S1). The three WARM rotations of the same day (12:20, 12:31, 14:49 — after a full gate, builds and two auditors on the box) gave 14.9 / 16.7 / 17.7 ms and 19.5 / 21.4 / 21.6 ms for the same two rows: the MX330 reaches 74 °C inside the 100 s of the probe itself and throttles, and a player's MX330 is that box after ten minutes of battle. So there is no regression to attribute, and there IS a budget problem: the cold margin at 7v7 is 1.8 ms, the sustained-clock frame is 3–5 ms over, and the p95 (20.3 ms cold, 27–36 ms warm) is over in every state. Where it goes is the same in every rotation: `scene_pass` 73–83 % of the frame; no toggle buys more than ~2 ms (no shadows −1.0 GPU, no SSAO −0.3, no card meadow −0.5, no near ring −0.8); 7v7 adds +2.0 ms of `scene_pass`, +0.4 shadow and +0.4 SSAO prepass, the near gear another +3.1; the cost scales 1:1 with pixels (garage 1080p→720p ×2.26 for ×2.25 pixels); CPU is 0.3 + 0.9 ms. The frame is FILL-bound: `terrain.wgsl` pays two texture taps, two field-quilt value noises and four hashes (static per plot, recomputed per fragment), the grain with its gradient, a strata noise (weighted by steepness but always evaluated), the puddle pool (evaluated on dry maps too), the furrow sin/cos, a rotated 4-tap PCF plus the far cascade's 2×2, the cloud and AO taps, the six-slot pool loop and a two-`pow` fog on every ground fragment; `scene.wgsl` does the same for statics plus an atlas tap behind `discard`; `vehicle.wgsl` takes 12 triplanar taps and two 3-D noises per fragment. The one-look promise (60 FPS on the MX330) has no test that knows the frame time (`grep 16.667` finds only triangle budgets) **Diet step 1 LANDED (2026-09-02, `perf/terrain-diet-1`): the field quilt per vertex, strata only on steep fragments, the puddle pool only when the look fills puddles, the ground grain faded out 300–450 m, furrows inside 150 m.** Cold A→B→A (rested to ≤ 60 °C before each run, both sides built and copied aside, marker-checked): GPU p50 "full scene" 11.85 / **10.08** / 11.83 ms (scene_pass 8.98 / **7.15** / 8.96: −1.8 ms, −20 %), "full + 7v7" 15.83 / **14.95** / 16.34 (scene_pass 11.76 / 10.57 / 12.13), "gear NEAR" 17.39 / 17.38 / 19.04 — the near-gear row is the vehicle shader's, not the ground's. Two instrument lies found and locked on the way: the vertex stage read a FRAGMENT-only binding and wgpu refused the pipeline silently (`ground_layout_matches_the_shader`, the probe now has a tracing subscriber); two worktrees on one target dir hand each other's binaries around (the sandwich runs copies). Still owed: cards behind `discard`, the vehicle triplanar near-only, the far cascade at one tap, Q1's per-bucket uploads, then the opt-in frame lock. **Diet step 2 MEASURED AS A LOSS and not landed (2026-09-02, `perf/terrain-diet-2`, reverted):** a vehicle detail reach (90–140 m: past it one projection per map instead of the triplanar three, the geometric normal, the grain and dust noises at their means) plus the far cascade at one tap past 300 m. Cold A→B→A on top of step 1: "full scene" 9.88 / **9.70** / 9.72 (scene_pass 7.08 / 6.91 / 6.89 — nothing), "full + 7v7" 13.31 / **14.16** / 13.28 (scene_pass 9.36 / **9.98** / 9.34: +0.6 ms), "gear NEAR" 15.66 / **16.66** / 15.80 (scene_pass 10.89 / **11.60** / 10.92: +0.7 ms); A1 and A2 agree to 0.15 ms. The per-fragment eye distance and the dynamic branch are paid by EVERY vehicle fragment while almost every vehicle fragment in a battle view sits inside the reach; the near-gear row is not fill — it is the running gear's geometry (the vertex side), so the next vehicle step is the gear LOD's distances, not its shader. Kept from the attempt: `every_shipped_shader_validates` (ten shaders; the vehicle shader had no parse test and a broken edit reached the probe as an "uncaptured GPU error" instead of a failed test). | `wot-audit/scratch/perf-cold-3377bb72.log`, `wot-audit/stark.md` §1, `terrain.wgsl:66-227`, `scene.wgsl:379-443`, `vehicle.wgsl:286-400` | W1 (first) | the per-pixel diet in small PRs, each with a cold A→B→A sandwich (the box rested to ≤ 60 °C before each run): the field quilt's lanes baked into a per-plot texture at splat resolution instead of ~12 hashes per fragment; the strata noise only on steep fragments and the puddle pool only when the look fills puddles; the grain and its gradient faded out by eye distance the way the micro octave already is; cards without the full shade behind `discard`; the vehicle's triplanar blend only in the near rung; the far cascade at one tap past 300 m. Then an opt-in test in the shape of the look goldens: GPU p50 "full + 7v7" ≤ 16.67 ms − 2 ms of thermal margin on this machine's release binary, cold |

### R — roles

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| R1 | **No concealment stat.** `spotting.rs:72-77` returns a fleet-wide binary; a Jagdtiger and a T-34-85 hide identically, and view range has two values (400 × 5, 440 × 3) | `sim/src/spotting.rs`, `tank.rs:132` | W2 | a per-vehicle concealment factor and an authored view range from the dossier; lock: the bigger silhouette is seen farther, stationary |
| R2 | **One dispersion factor.** Movement, hull traverse and turret traverse share `movement_bloom_mrad` times fleet constants 0.35 / 0.25; no gun can be "bad on the move, fine on the turret" | `sim/src/aim_dispersion.rs:41-43` | W2 | three authored factors per gun; lock |
| R3 | **No ground pressure.** `SuspensionModule` has no track width; terrain resistance is per-material only, so a 32 t T-34 and a 75 t Jagdtiger handle mud identically | `terrain/src/ground.rs:87-99`, `physics/src/contact.rs:45` | W2 | track width on the module; pressure from mass and contact patch; soft-ground resistance scales with it; lock: the heavy hull is slower on mud and equal on cobble |
| R4 | Crew proficiency pinned at 1.0 for all | `crew.rs:54` | decided | stays — proof, never power |

### U — interface and garage

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| U1 | **`ui_kit` is a triangle emitter, not a toolkit.** 1 097 lines of `push_x(&mut Vec<HudVertex>, …)` in clip space with a manual aspect; no layout containers, anchors, padding, scissor, DPI (nothing reads `scale_factor`), focus, hover or press state, text wrapping, second size class, one font at one weight | `crates/ui/ui_kit/src/`, `font/bake.rs:11-16` | U (parallel) | a layout layer (rect tree, edge anchors, row/column with padding, measured text, a `Ui` context carrying aspect, DPI and scissor), theme size classes and a second weight, a hover/press/focus machine, `set_scissor_rect` in the HUD pass — **→ `docs/interface-program.md` F4, F6, F7 (2026-09-05)** |
| U2 | **245 vertex-equality tests** across `client/src/hud` and `app/garage` are the real cost of any redesign | `hud/reticle_overlay_tests.rs` (30), `garage/actions.rs` (26), … | U (first) | a semantic draw list keyed by element name; tests query it; the vertex-equality count ratchets to zero as elements migrate — **→ interface-program F5** |
| U3 | **ASCII only.** The atlas bakes 0x20–0x7E; unknown glyphs are silently skipped; `ui_strings.rs` forbids non-ASCII | `ui_kit/src/font/bake.rs:42`, `font/layout.rs:225` | U | Latin Extended-A baked; the ASCII rule removed; lock: an unknown glyph renders visibly, never skips; a Polish string in a golden — **→ interface-program F3** |
| U4 | **22 HUD elements on hard-coded floats**; absent entirely: kill feed, team lists, scoreboard, ping | `client/src/hud.rs:174-262` and `hud/*.rs` | U | the HUD is a layout description; the missing elements exist; the sight locks and `reticle_strip` are preserved unchanged — **→ interface-program H2 (team lists), H21 (layout and editor), H25 (the reticle)** |
| U5 | **No product shell**: no settings, keybinds, battle results, lobby, localization; Escape offers no exit from a cold-booted garage (`garage/mod.rs:255-259`) | `docs/ROADMAP.md:88-89`; the hala-4 shell items W2–W6 — battle results, local battle history, SETTINGS, Latin-2 font, BATTLE palette — all five still open when that register was retired 2026-09-03 (`git show d2c6fcaa:docs/hala-4-program/plan.md`) | U | the screens exist with goldens; `escape_always_offers_a_way_out` — **→ interface-program P8 with P1, P6, P7** |
| U6 | **The stat column is nine anonymous numbers** — no labels (the test concedes it, `stats.rs:136-137`), no bars although `push_bar` exists, no compare; every plate is the same `PANEL` at 0.86 alpha, so the crew column carries the weight of the nine numbers that decide the fight | `app/garage/panels/stats.rs:25-29`, `layout.rs:63-69` | U | labelled rows with bars against the roster min/max; a plate hierarchy in the theme; lock: `every_stat_row_prints_its_own_label`; `garage_screen` re-blessed — **→ interface-program G1** |
| U7 | **"Which tank am I in and why" is unanswerable.** `VehicleClass::label()` exists and is printed only in the tech tree; the nameplate is tier + nation | `panels/nameplate.rs:31`, `stats.rs:20-22`, `techtree.rs:69-77` | U | the nameplate names the class and role; lock — **→ interface-program G4** |
| U8 | **Ammo is illegible and unexplained.** Designations at 0.016 under the screen's own 0.022 floor (`inspector_legend.rs:22-24` vs `loadout.rs:54-55`); no pen/damage per shell; the rack creed unexplained | `panels/loadout.rs:49-74` | U | pen/damage per slot, a one-line rack rationale; lock: `no_garage_string_renders_below_the_legibility_floor` — **→ interface-program G5** |
| U9 | **No press state, no tooltips, no keybind legend.** One white-8 % hover wash for the whole screen; 14 keys bound and only `[R] REPAIR` printed | `garage/overlay.rs:57-59`, `garage/actions.rs:176-247` | U | three-state controls (`PRESSED` token, `every_clickable_has_three_states`), tooltips, a hint strip with its golden — **→ interface-program G6** |
| U10 | **BACK wears the commit red** (`techtree.rs:98` reuses `BATTLE`) and the SIGNAL red is outside the palette (hala-4 W6, unowned since that register was retired) | `panels/techtree.rs:98`, `docs/art-direction-program.md` | U | `signal_red_is_only_worn_by_commit` — **→ interface-program G7** |
| U11 | **Clicking a module on the 3D tank does nothing** — `hit_test_hangar` knows strip rects only; the turret cannot be rotated | `garage/overlay.rs:157-209` | U | hero hit-testing opens the slot; turret rotation on drag — **→ interface-program G8** |

### B — buildings

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| B1 | **The silhouette is flush-cut.** No geometry crosses the collision AABB: roofs end at `half_width` (`push_gable:1790`), no eaves overhang, ridge, hip or valley pieces, no chimney, dormer, gutter or downpipe (grep = 0) — every skyline is a rectangle with a triangle on it and nothing casts an overhang shadow | `world_forge/src/building.rs:55-138,1790`; `target/ostrogorsk_avenue.png` | W5 | every style's roof projects ≥ 0.25 m past the wall with eave, ridge and chimney parts; a `building_views` review example at 15/40/150 m per style intact + damaged |
| B2 | **No ground connection.** `append_building` seats the mesh at `center.y − half.y` on one sampled point; no footing course, dirt apron or grass at the wall — the buildings sit on the lawn like game pieces | `scene_build/src/battlefield.rs:1355-1440`; `target/bystra_town_lane.png` | W5 | a footing course meeting the sampled ground along the perimeter, a dirt apron decal, grass at the wall; lock: `a_building_meets_its_ground` sampling the seam |
| B3 | **Every building is unique merged triangles** in the 1 000 m static buffer — a mullion is fresh geometry each time | `battlefield.rs::append_building`, `scene_resources.rs` | W5 | the kit-of-parts decision above; lock: the town's static-mesh triangle count drops and the frame delta is recorded |
| B4 | **Walls are one flat albedo** with the shared detail octave: no brick or plaster coursing, no dirt streaking, no AO in the reveals — pierced windows read as painted past 40 m | `building.rs::building_palette:1454`, `scene.wgsl` | W5 | coursing and streak terms per wall role, AO in reveals; lock: an albedo-variance floor per facade in the review view |
| B5 | **A ruin is a heap of 7–10 slabs** with no relation to the intact mass; no standing walls, no visible floors | `building.rs:1520` | W5 | a ruined form keeping ≥ 2 wall planes and a floor slab, with its own honesty test |
| B6 | **Repetition.** 7 styles × 4 wall colours × 3 roof colours, all detached rectangles with the ridge along the long axis; the Ostrogorsk square is six near-identical white blocks | `building.rs:55-138`; `target/ostrogorsk_square.png` | W5 | the grammar produces L-shapes, annexes, terrace rows, yards and shop fronts; lock: a footprint-shape histogram per town with a floor on non-rectangles |

### T — terrain

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| T1 | **The 5 m grid is the root of two other rows.** It turns a 12 m hill into a tent (G7) and puts 2–4 velocity creases per second under the camera (C4). 1 m map-wide is refused (25× samples; the ground bake is already 362 ms of a 517 ms map swap) | all five blueprints `cell_m: 5.0`, `terrain/src/heightmap.rs` | W5 | a 2.5 m ring near the player, chunk-streamed, measured (map-swap ms, frame ms, G7's crease count); map-wide 2.5 m only with that measurement |
| T2 | **No erosion.** Heightfields are authored by gesture; hills have no erosion lines, no drainage, no talus | `map_forge` editor, `orliny_talus.png` | W5 | a hydraulic-erosion pass as an editor bake step (zero runtime cost); goldens re-blessed with a slope histogram lock |
| ~~T3~~ | ~~**The ground tiles to the horizon.** One woven noise carpet at every distance, no macro colour/normal variation at 100–400 m.~~ **CLOSED (2026-09-04, `feat/teren-2-ground-material`).** The terrain fragment no longer evaluates a lattice. Each splat layer has its own baked 512² / 10 m tile (tangent normal + shade + height: grass clumps, straw stubble, dirt clods, rock plates) uploaded as a `texture_2d_array` with a box mip chain, plus one 256² / 160 m macro tone tile tapped at two scales (near period and 3.83× rotated) so the 160 m repeat never shows inside a 1000 m map. The height blend at splat borders is `w × (h + 0.15)³`. The hardware mip is the filter — no per-fragment footprint fade (O1's ground half is this fetch, not a hand-rolled `octave_reach`). Bit 1 still bends the lighting normal (now from the tile, not `value_noise_grad`); bit 2 now gates the tile taps (the micro octave is gone; the bit is not renamed). Anisotropy landed at 2 after a measured sandwich on the MX330 (full-scene p50 B8 26.66 / B4 25.42 / B2 24.38 vs A master 25.66 ms): eight cost more than it bought. Locks: `the_ground_detail_is_a_mipmapped_material` (bindings, one tap per present layer, height blend, two macro taps, no `ground_grain` / `micro_grain` / `octave_reach` in the fragment, constants mirror the bake, mip model at the battle lens); bake locks `every_tile_is_seamless_at_its_edges`, `the_tiles_are_neutral_on_average`, `the_four_layers_are_distinct_materials`, `every_frame_is_periodic_in_the_tile`; GPU lock `the_ground_material_varies_the_mid_field_and_grains_the_near_field_without_retinting` (flat all-grass field: mid-field block-mean spread ≥ 0.0035, near grain > 2.5× tiles-off, mean luma drift < 1 %). Battlefield goldens re-recorded (26 frames). The 0.12 lattice bend T6 left open is this fetch. Statics' generic ground in `scene.wgsl` still reads `ground_grain` | `renderer_api/src/ground_detail.rs`, `terrain.wgsl`, `tests/ground_material.rs`, `tests/goldens/look/prokhorovka_clear_afternoon.png` | W4 | one macro variation fetch; a variance metric on the mid-field — done |
| T4 | **Cliffs and slopes are underused.** `GroundClassifier` has slope rules and a `steep` layer but no triplanar cliff material | `terrain/src/ground.rs`, `terrain.wgsl` | W4 | triplanar cliff keyed on slope; review views `orliny_talus`, `ostrogorsk_canyon` |
| T5 | **Roads are painted vertex colour** with a baked camber normal: no edges, kerbs or decals; tracks and craters are the only decals | `terrain_maps.rs:15`, `battlefield.rs:1506-1559` | W5 | road edge geometry and decals through the instanced path; lock: draw-call count and the O1 meadow metric |
| ~~T6~~ | ~~**Every meadow is ploughed, and the SSAO shades a flat plane.** The owner, 2026-09-04, Bystra then Prokhorovka, standing still a few metres from the ground: sharp horizontal ZIGZAG waves across the grass, long darker bands converging into the distance "in different directions", kinks where the bands meet; the moment the tank moves the waves strobe and seem to fade. Two causes, both in the shipped profile. (1) "Teren B1" (`TERRAIN_FURROWS`, bit 64, bought back 2026-08 by a ±0.05 ms cost probe and never judged on a frame): a 1.25 m sine with 6 % albedo and a 0.10 normal corrugation, masked by `quilt.z × (grass + straw)` — the VEGETATION share, so every meadow of every map was a ploughed field — with the plough angle hashed per 50 m plot at the VERTEX and interpolated across each 5 m triangle: a plot across the view printed horizontal waves, a plot along it printed converging dark streaks, and at every plot border the interpolated direction swung through the triangle (the kinks and fans). Static when the eye is still because the pattern is world-anchored; strobing in motion because a 1.25 m sine at 1–3 px per period is under Nyquist the moment it moves. (2) `ssao.wgsl` compared every tap's depth against the CENTRE pixel's depth, so on open ground from a 2 m eye — where the depth already falls 4 cm per pixel at 10 m, past the 3.5 cm bias — the taps toward the camera always "occluded": a 2–3 % darkening of the whole flat field carrying the spiral's rotation noise as a faint diagonal weave, most visible under the ambient-heavy overcast~~ **CLOSED (2026-09-04, `fix/meadow-furrows-ssao-plane`).** The furrow sine is deleted from `terrain.wgsl` (the varying, the quilt's angle and mask lanes, the sine, `FURROW_PERIOD_M`); bit 6 of `ShaderDetailMask` is RETIRED, not reused — it is out of `canonical()` and out of `FULL`. The rule it leaves behind, locked in `the_meadow_is_not_ploughed`: the per-vertex quilt lane carries TONES (two scalars), never an ANGLE — a 5 m interpolation cannot carry a direction. Worked land returns in Teren 2.0 as a MATERIAL (splat layer + texture with mips, on the fields the map author paints), not as a sine on the grass. The SSAO pass now judges each tap against the pixel's LOCAL PLANE (`dpdx/dpdy` of linear depth predict the tap's expected depth; a silhouette's nonsense slope falls back to flat above `PLANE_EDGE_SHARE`), so a flat plane reads exactly zero and only a sample in front of the plane — a real crease, a hull on the dirt — occludes. GPU lock `a_flat_field_does_not_occlude_itself` (a 128² field from a 2.2 m eye, ambient rig: SSAO on vs off byte-identical; on the old shader the same frame had 3 438 px darkened, max 8/255); the crease lock `with_ambient_on_the_crease_still_darkens` still holds. Battlefield goldens re-recorded (26 frames; garage byte-identical): the diff of every mid-field frame is the stripe field itself. Left open on purpose: the 0.12 fine-grain normal bend (a gentle 0.6 m ripple within 25 m) — it is a real relief, filtered, and is re-judged with T3's macro fetch, not removed blind | screenshots 2026-09-04 (Bystra ×3, Prokhorovka ×1); `terrain.wgsl`, `ssao.wgsl`, `lighting_quality.rs` | W4 | the meadow is not ploughed; a flat plane has no AO; the quilt lane carries no direction — done |

### N — sky and weather

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| N1 | **The golden evening fails the policy** — lavender-milk top to bottom, no warm horizon band, no visible sun, a flat khaki field with no long shadows; the profile's god rays 0.15 and bloom are gated off on the canonical tier, so none of the golden-hour machinery reaches the frame. This is D4's proof | `target/prokhorovka_golden_evening.png`, `lighting.rs`, `lighting_quality.rs::canonical()` | W4 | a golden asserting the lower-quartile luma ≤ 0.28 and horizon R/B ≥ 1.6 in the played band |
| N2 | **Clouds have no volume.** A two-tone lerp `mix(shade, lit, dot(dir,sun)·0.5+0.5)`: no Beer–Lambert, powder, silver lining, self-shadow or thickness; the overcast lid is a structureless gradient over 45 % of the frame | `sky.wgsl:159-163`, `target/prokhorovka_overcast.png` | W4 | the 2D cloud lighting model (3–4 marched taps where cloud > 0); lock: cloud-region max/min luma ≥ 2.0 on the clear-afternoon golden with the shipped 3-octave mask; Δ ≤ 0.8 ms |
| N3 | **The drawn cloud field and the shadow cloud field are different noises** (Hoskins FBM with a ridged octave on the dome vs periodic splitmix value noise on the ground, `shadow_common.wgsl:47-48` admits "not pixel-exact") — a shadow can land under open sky | `sky.wgsl:37-74,132-137`, `cloud_map.rs:24-70` | W4 | one field: the dome samples the baked tile (or both evaluate one function); lock: shadow coverage under a drawn cloud ≥ 0.9 |
| N4 | **Weather is frozen on four of five maps.** `static_program = map != BystraValley || seed == 0`; the sun travels 2.5° and only there | `weather_timeline.rs:56,117-125` | W4 | a program per map; the fairness lock extended over the timeline, not the static look |
| N5 | **Rain does not wet the world.** Geometry streaks with a private hard-coded wind (`rain.wgsl:14`), no splash, no puddle ripples, wetness darkening at 0.08 gloss — the rain frame reads as dry grass with lines over it | `rain_pipeline.rs:15`, `terrain.wgsl:133-140`, `target/bystra_town_rain.png` | W4 | screen-space streaks + a real wet response (albedo ×0.72 on soaked ground, Fresnel sheen on roads, puddle ripples); lock: ground luma under rain ≤ 0.75× dry on the Bystra golden |
| N6 | **The sky is a two-stop gradient** `mix(horizon, zenith, pow(up, 0.42))` with per-profile constants; no sun colour ladder by elevation; the reflection curve (`sqrt`) disagrees with the dome's | `sky.wgsl:93-94`, `lighting_common.wgsl:77-80` | W4 | the scattering-LUT decision; lock: a sun-elevation sweep asserting monotone warmth as elevation falls and zenith saturation ≥ horizon saturation; Δ ≤ 0.15 ms |
| N7 | **God rays and bloom exist and never ship** (`GOD_RAYS` not in `canonical().shader_detail`, `bloom_mips: 0`) | `post.wgsl:37-63`, `lighting_quality.rs` | W4 (after N2) | the buy-back protocol (interleaved cold-GPU A/B, Δ ≤ 0.5 ms on the evening view) decides each; rays through a structured sky pay, through a flat lid do not |
| N8 | **Three winds.** Grass and foliage read the storm heading; rain has its own constant; smoke, dust and FX have none | `meadow_common.wgsl:50-52`, `rain.wgsl:14`, `fx.wgsl` | W4 | one `wind_params` lane; lock: no shader declares a private wind constant (mirrors `every_sunlit_pass_takes_the_cloud_shade`) |
| N9 | **Float determinism is per operating system, and the session's door hashes the heightmap bit for bit.** About 150 calls of `sin/cos/atan2/acos/asin/exp/powf/ln/tan` in the tick path (`sim`, `physics`, `game_core`, `terrain`) and ten in `map_forge` resolve to the platform's libm (UCRT on Windows, glibc on the Hetzner server the design names); `battlefield_hash` folds every heightmap sample's bits and the client ends the session on a mismatch — a Windows client and a Linux server may never pair, and every "bit for bit" lock in the repository (replays, `physics_policy.rs`, `session.rs` local≡remote) runs two passes in ONE process on ONE machine. Replays, map goldens and bake hashes are pinned on Windows and run nowhere else; L3 (replays from the server's input log) and the map hash as anti-cheat stand on the untested assumption; `math_backend_parity` compares Cargo features on one box and says nothing about the OS | `map_forge/src/golden.rs:23-35`, `client/src/app/session.rs:580-587`, `quality/tests/math_backend_parity.rs:545-547`; `wot-audit/reed.md` §2, `doom.md` §4.7 | W2 (before the first remote playtest) | half a day for the binary answer: build `server` and run `cargo test -p map_forge -p sim` under WSL, compare `goldens.ron` and the replay pins with Windows; then a `u64` golden of every `TankState`'s bits after 600 ticks of a 7v7 bot battle, run on both systems. If they differ by an ulp (both auditors expect it): a deterministic libm behind `game_core::math` wrappers for the tick path and the map compiler, one re-pin of the fixtures |
| ~~N10~~ | ~~**The local host filters the snapshot differently from the remote one, and the lobby seats by hash order.** The remote host runs `filtered_for_viewer_with_observers` (the viewer's radio gate, the union with the viewer's own eyes); the local host — the only mode a human has ever played — runs `filtered_for_viewer` without either, while the sim's team mask already excludes a radio-dead observer's sightings: a player with a destroyed radio goes blind to what is in front of their own eyes. `remote.rs` picks the battle's seats by iterating a `HashMap` — the order (slot, spawn, garage pick) is random per process~~ **CLOSED (2026-09-02, `fix/local-host-filter-parity-n10`).** One filter, one truth: `LocalAuthoritativeServer::view_for` applies `filtered_for_viewer_with_observers` with the live observer masks at all three local cuts (the tick's snapshot, `latest_snapshot_for_player`, the vehicle change), exactly the remote host's call. Seats go out in HELLO order: `RemoteClient::hello_seq` is stamped when a crew first establishes, `start_battle` sorts the established crews by it before taking `LOBBY_FULL_PLAYERS` — never the map's iteration order. Two narrow test doors on the local host (`destroy_module_for_test`, `place_for_test`), in the shape of `knock_out_for_test`. What stays open: the `session_id`-in-the-clear takeover is N5's (identity); the negative lock "a `Disconnect` from a foreign address with a valid `session_id` does not free the seat" lands with it | `battle_host/src/local.rs` (`view_for`), `remote.rs` (`hello_seq`, `start_battle`), `tests/local_authoritative_server.rs`, `tests/remote_battle.rs` | W2 (with N9) | `a_radio_dead_player_still_sees_the_hull_in_front_of_their_own_eyes` (player and one enemy alone on open ground 40 m apart, every other hull a kilometre away, the radio shot out: the observer mask proves the player's own eyes see the enemy, and the player's cut carries it); `seats_go_out_in_hello_order_not_table_order` (three crews hello in five orders, twice each, on a `MemoryHub`: the k-th hello gets the k-th seat on every run) |
| N11 | **Weather is presentation only, and legibility is a floor — true today by absence, locked by nothing.** The owner's directive of 2026-09-02 (game-design.md reconciliation row 9) revokes §13.3's rain view-range cut and §4's rain μ: the sim never had them (spotting, physics and `game_core` carry no weather), but no rule stops the next PR from adding one, and the only gameplay effect weather has — whether the player can read the picture — is measured at 400 m while the parked view range is 440 m, where RainSqualls (0.356) and DawnFog (0.362) already break the 35 % rule in five looks (V0) | `sim/src/spotting.rs` (no weather), `physics/src` (no rain μ), `scene_build/src/weather.rs:104-118`, `docs/game-design.md` row 9 | W1 (rule) + W4 (picture) | a `quality` gate test: `sim`, `physics`, `game_core` and `net` depend on no crate that knows a weather profile or a look (the DAG rule, with the allowlist at zero); the fog fairness lock moved to `max(TankSpec::view_range_m)` and RainSqualls/DawnFog brought under 35 % by density; a per-profile legibility golden for every map × weather (the 26 battlefield looks already recorded), so a weather that hides a hull the sim says is visible fails a test, not a playtest |

### H — water

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| H1 | **Every hull drowns at 1.5 m.** `DROWN_DEPTH_M` is fleet-wide; no fording depth exists in `TankSpec` or the dossiers (real: T-54 1.4 m, Tiger I 1.6 m); `BOT_DEEP_WATER_M = 1.1` is a second constant | `sim/src/drowning.rs:14-21`, `battle_host/src/bot_routes.rs:121` | W2 | `ford_depth_m` per vehicle from the dossier; drowning and the bots' line derive from it; lock per vehicle |
| H2 | **Drowning is silent until the engine is dead.** `submerged_s` is never replicated; no HUD countdown, no bubble or gurgle; the first signal is an engine-dead `DamageEvent` at 2 s, death ≈ 6 s | `sim/src/tank_state.rs:82`, `net` (absent) | W2 | `submerged_s` on the wire (append-only), the rack-countdown widget mirrored, audio at t = 0 |
| H3 | **Bots ride down slick banks** (register H1 of the tracks program: 2.107 m past the line) — on a descending wet bank reverse thrust < gravity + drag, the escape freezes heading and slides | `terrain/src/ground.rs:66-83`, `bot_routes.rs:291` | W2 | the escape steers along the depth contour (bearing scored by the depth gradient); lock: no bot exceeds its ford depth over all soak seeds |
| H4 | **HE on water is undamped** — a full `burst_he_splash` at the waterline | `sim/src/shell_step.rs:144` | W2 | splash attenuated by depth at the burst; lock |
| H5 | **The river reads as plastic.** Sky-only Fresnel (a mirror with nothing in it under overcast), both depth-tint endpoints desaturated (`river_mute` 0.74), a hard polygon shoreline, a stray concentric ripple, a skirt seam, and at 300 m the fog blend to `sky_horizon_rgb` erases the channel entirely; the bridge floats with no reflection or waterline | `shaders/water.wgsl:51-62,155,172`, `target/bystra_bridge.png`, `target/bystra_panorama.png` | W4 | half-resolution SSR for the water pass (measured), a shore-foam band, the aerial blend capped for water so the channel survives 300 m, the ripple and seam fixed; lock: a `bystra_bridge` golden with the bridge's reflection present |
| H6 | **No wet hull, no rain ripples.** Fording leaves no darkening or drip; puddles are gloss only | `motion_fx.rs:70-172`, `terrain.wgsl:135-140` | W4 | wet-hull gloss/darkening decay after fording with drip particles; rain ripples on water and puddles |

### O — the picture

The light's register stays in [art-direction-program.md](art-direction-program.md); this program
feeds it rather than duplicating it. D8 (contact AO on one vehicle), D9 (dirt lane never populated),
D15 (nothing to look at up close), D17 (pastel showcase) are W3 outputs — they close with K3 and
K6. D4 (no dark mass on the steppe) closes with N1 and B1; D18 (Orliny's borrowed light) with N4.

| ID | Defect | Evidence | Wave | Closes when |
|---|---|---|---|---|
| O1 | **The card meadow reads as a wavy moiré** in the mid-field band, most visible at grazing light. **HALF CLOSED (2026-09-04, `feat/terrain-grain-footprint-fade`)** — the GROUND's share: the owner's three close-ups showed the concentric "fingerprint" ripples on bare ground between the tufts, not on the cards. Root: `terrain.wgsl` evaluated the procedural grain (0.6 m fine octave + its light-catching gradient, the 0.31 m crumb, the 1.25 m furrow sine) once per fragment out to 300 m with no mip chain, so from a tank's eye 3 m up every octave went under Nyquist at ~25–40 m and beat against the pixel grid. Now every octave is filtered by the fragment's pixel footprint (`dpdx/dpdy` of the world position → `octave_reach`: full at ≥ 4 px per period, folded to its MEAN at 2 px, so no brightness step), independent of the lens — a scope narrows the footprint and the grain follows it out. CPU mirror `renderer_api::grain_octave_reach` + `ground_pixel_footprint_m`; lock `the_ground_grain_fades_by_pixel_footprint_not_by_metres` (the crumb is gone by 25 m, the fine grain by 40 m, the broad octave by 150 m at the battle lens; monotone, no step; alive at 100 m through a 16× scope). Battlefield goldens re-recorded. **Correction 2026-09-04 (same day, T6):** the owner came back with the fix in and the waves still there — close, sharp, static when the tank stands still and strobing when it moves. The footprint filter had cured the grain's aliasing and NOT the picture: the zigzag waves and the darker slanted bands were the furrow sine (T6), which this row's first pass filtered instead of questioning. The lesson stays here: a diagnosis of "aliasing" was accepted from three screenshots without rendering the offending frame and turning features off one at a time — that toggle series (`WOT_GPU_DETAIL`, `WOT_SSAO`, overcast vs clear) is what found it the second time, in an hour. **Correction 2026-09-04 (same day, T3):** the footprint fade itself is gone. Teren 2.0 replaced the terrain lattice with baked mipmapped tiles; `grain_octave_reach` and `ground_grain_filtered` left with their only consumer. The ground half of this row is closed by T3. What stays open is the row as written: the CARDS' share, with its metric | `target/flora_lineup.png`, `target/prokhorovka_evening_contact.png` | W4 | a metric derived from a frame judged good (look-metric-validation rule), then a floor |
| O2 | **The picture's own metric lies in the direction of "fine".** The dark-plane lock counts linear luma < 0.25 — sRGB 0.54, the screen's middle grey — as shadow, so every evening passes with 40–48 % "dark" while 0.7–2.0 % of its pixels are actually dark (sRGB < 0.30); the FLOOR/TARGET table and "the evening looks are not the problem" stand on that threshold. The art-direction reference frame looks away from the sun (`review_views.rs:373-374` eye +x, `lighting.rs:464` key −x), so the profile's warm horizon, scatter and long shadows never reach the frame and the antisolar clouds come out lavender (`sky.wgsl:161` `cloud_shade = ambient*2.5+0.15`). The bloom lock asserts `bloom_weight > 0` while `canonical()` has `bloom_mips: 0`. Rule 2 measures albedo swatches (≤ 0.45) while the grass PIXELS sit at 0.60–0.66 saturation and a bush at 0.72 — the most saturated object in the frame. Five weather profiles come out as one photograph tinted (Mazurski's evening 2.5 % warmer than its afternoon; the lock compares evening with OVERCAST ×1.10 and passes). Rows closed on paper that the picture still shows: D3 (the milk turned lavender), F3 (the hedge body reads as the slab, `target/bystra_treeline.png`), D19 (the openings are there, the town is still boxes — B1/B2/B4), D21 (cloud shadows ship and no patch is visible in any golden) | `client/tests/look_goldens.rs:264,641`, `renderer_api/tests/look_locks.rs:131-147,373`, `lighting_quality.rs:88-102`, `scene_build/src/review_views.rs:373-374`; `wot-audit/mysterio.md` §4.1–4.2 | W1 (with Q7's diet) | a second threshold `deep_shade` (linear < 0.07) with a FLOOR per frame and the TARGET 0.08 — 0 ms, and it reopens D4 on every outdoor frame; a `ReviewView` for Prokhorovka's evening INTO the sun; grass saturation measured on golden PIXELS as the lock; a profile-distance lock per map (key elevation ≥ 8°, R/B ≥ 0.15); `bloom_mips ≥ 1` in `canonical()` with a measurement, or the assert deleted; D3b and F3b reopened with the frame as the evidence |

### V — visibility as a resource

The owner's brief (2026-09-02): spotting is the hidden core of the genre — the whole map design
and the whole light class stand on visibility being a resource. Keep the idea, throw out the
table: one raymarch, two questions (a shell integrates armour, a sight line integrates
occlusion), deterministic and explicit.

| # | defect | where | wave | done means |
|---|---|---|---|---|
| V0 | **The eye and the shell march the terrain differently, and scenery lies in both directions.** Sight samples the terrain every `terrain_sight_step_m` (2 m) with `SIGHT_GRAZE_SLACK_M` 0.3 m of slack; the shell marches at 1 m with no slack and dies `radius_m` (≤ 0.10 m) above the ground — a ridge 5–30 cm above the sight line is seen through and shot into. A bush carries an interior occlusion hull ("shows no daylight through") while `line_of_sight` tests only `StaticCoverObject` and terrain: the player hides behind what the server treats as air. A `TreeLine`'s LOS box is opaque to its full height while the picture is opaque to 0.75 h: from a rise, an invisible 4–5 m blocks the shot. The fog fairness lock asks at 400 m (`sim::VIEW_RANGE_M`) while the parked view range is 440 m: RainSqualls 0.356 and DawnFog 0.362 at 440 m break the 35 % the policy calls inviolable, in five looks (`bystra_rain`, `ostrogorsk_rain`, `bystra_dawn_fog`, `orliny_dawn_fog`, `mazurski_overcast`) | `game_core/src/sight.rs:12`, `sim/src/spotting.rs:157-171,221-245`, `sim/src/shell_trace/terrain.rs:4-31`, `world_forge/src/tree/mod.rs:489-494`, `scene_build/src/tree_line.rs:91-95`, `scene_build/src/weather.rs:104-118`; `wot-audit/reed.md` §4, `mysterio.md` §4.10 | W2 (before V1) | one terrain march for sight and shell (V1's precondition); a property lock on random ridges: `line_of_sight == true ⇒ first_terrain_impact == None` on the same segment less the radius; every `SceneryKind` with an occlusion hull > 0 m² must carry `optical_density > 0` in spotting — until V1, the bush hull is half-transparent; `the_line_is_opaque_where_the_box_is` to 1.0 or the LOS box to 0.75 h (a data change, a golden re-bless); the fog lock at `max(TankSpec::view_range_m)` with RainSqualls −11 % and the dawn valley −18 % density |
| V1 | **A bush hides nothing and a tree line hides everything.** Spotting is a LOS ray against cover boxes: binary. Scenery — every bush, every crown — is transparent to spotting; a TreeLine box is opaque. No optical density anywhere | `sim` spotting, `terrain::scenery` | W2 | foliage `SceneryKind`s carry an optical density per metre; the spotting ray integrates density along its length (the shell's own march, the second question); a target is spotted when the integral stays under the observer's budget; scenery still never blocks a shell; locks on a bush at 5 m vs 50 m and on shell/LOS parity |
| V2 | **No camouflage stat, one view range.** Every tank is equally visible; view range is 400/440 by era | `TankSpec::view_range_m` | W2 | `TankSpec::camouflage` (the scalar the threshold moves by) per vehicle from the dossiers — a T-34-85 is not a Tiger II — locked per vehicle like traverse |
| V3 | **The budget is invisible.** The player cannot see how far they can be seen or what eats it | HUD | W2 | a HUD readout of the visibility budget (the range at which the nearest observer resolves you) and its eaters (moving, firing, no bush); deterministic and explicit — **→ `docs/interface-program.md` H14 (2026-09-05)** |
| V4 | **No sixth sense; auto-spot at 50 m unverified** | `sim` spotting | W2 | sixth sense for everyone from the first battle (the spotted flag reaches its target with a delay the register measures); a lock that a tank at 50 m is always spotted whatever the density |

### L — the ledger (honesty as a feature)

| # | defect | where | wave | done means |
|---|---|---|---|---|
| L1 | **The garage inspector does not answer "how thick, here"** | `garage/panels/inspector` | W4 | point at the hull, read the effective thickness through the SAME resolver the shell uses (`resolve_traced_impact`), with the angle; lock: inspector == shell on 1 000 points — **→ `docs/interface-program.md` G11 (2026-09-05)** |
| L2 | **No battle log** | client | W4 | a post-battle screen listing every shot fired and received — shell, distance, pen vs effective, zone, result — fed by the battle-event stream, nothing invented — **→ `docs/interface-program.md` P4 with P2, P3 (2026-09-05)** |
| L3 | **Replays are fixtures, not a screen** | `sim` replays | W5 | the server's input log saved per battle; an in-game viewer that replays it through the same sim; the replay-exact lock is the feature |

## Wave plan

**W1 — Widok.** Everything the player sees first, none of it touching the authoritative hull.
Order: F1 (the cones: one PR, the biggest area of any frame), the register rewording (F6/Z4/S6/A2),
A1 (one penetration resolver — the "shoot for 0" fix), A3–A6, A8 (gravity + the visible shell),
A7 (the sniper review view, then the scope), F2, F3, Z1–Z3, S3 (the momentum function), S1 (the
lights, with their perf sandwich), S2, S4, S5, S7–S12. Gate: every W1 row closed; the frustum kit
gone; the lights' cost recorded.

**W2 — Jazda.** The authoritative hull and everything that reads it. G7 first (the sprung hull,
with G1's one ground truth folded in), then G4, G3, G2 (the wire), G5, G6, K10, then the camera
C1, C2, C3 (re-measured on the sprung hull), C4, then R1–R3 and the water sim H1–H4, then
**visibility as a resource** — V1 (the density march), V2 (camouflage per vehicle), V4 (sixth
sense and the 50 m floor), V3 (the budget on the HUD) — the owner's brief of 2026-09-02, placed
after the hull because the light class's whole reason to exist is this lane and it must be
measured on the hull that ships. Gate: the crest-walk continuity lock, the per-vehicle wallow
lock, `mobility_baseline` byte-identical, replays re-pinned once for the wire bump; the spotting
parity lock (shell march == sight march) green on every map.

**W3 — Kuźnia 2.0.** K7 first (the documents), then the seam (K1, K2), then the benchmark to the
bar (K0, K9, K11–K15), then seven vehicle migrations — one PR each, with its dossier, its P1 parity
gate, its P2 thicknesses and its K0 overlay — then the three capabilities the kernels lack (K4, K5,
K6), each proven on the T-54 and rolled to the fleet in the same wave. P3 closes with the last
migration. Gate: the K3 inventory gate and the K0 overlay green for every roster vehicle;
D8/D9/D15/D17 closed in the light's register.

**W4 — Obraz.** The sky and the air first (N6, N2, N3, N1, N4, N8, N5, N7), then the ground's
distance (~~T3~~, T4), the water's look (H5, H6), the flora's near rungs (F7; F4 and F5 left the plan 2026-09-04), O1,
D18, and the ledger's first two screens — L1 (the inspector answers "how thick, here") and L2
(the battle log). Gate: every look golden at its target; each renderer row with its own measured
delta; the inspector == the shell on a thousand points.

**W5 — Miasto i Ziemia.** Buildings and the terrain's structure: B3 first (the kit and the
instanced path — it pays for everything after it), then B1, B2, B4, B5, B6; T2 (erosion in the
editor), T1 (the 2.5 m ring, measured), T5; L3 (the replay viewer over the server's input log).
Gate: `building_views` per style at three distances, the footprint-shape histogram, the map-swap
and frame measurements; a saved battle replays bit-identically in the viewer.

**Q — the frame**, a lane from day one, in parallel with W1: Q2 (the instrument) first, then Q3
(the fresh number and the per-item deficit), Q1 (the uploads), Q4. Every renderer row in every wave
lands with the lane's measurement.

**U — the interface** is its own program since 2026-09-05: `docs/interface-program.md` (the
owner's verdict: the interface is rebuilt from a design, not repaired row by row). The premise
above — "files no other wave touches" — was false the week it was written: nine commits from the
A, S, G and Z lanes edited `crates/apps/client/src/hud/` between 2026-09-01 and 2026-09-05. That
program's ownership rule replaces it: during its HUD wave the HUD files belong to it, and any lane
that needs a HUD element goes through its draw list.

A wave is done when every row in it is closed, its locks are in the ratchet, and the roadmap's
"DONE" lines it touched are reworded. A row closes with a number or a frame, never with a sentence.

## Verification

The merge gate is `scripts/verify.ps1` (see `CLAUDE.md`). In addition, for this program:

- Any renderer change (S1, F1, F4, N2, N6, H5, B3) lands with an MX330 A→B→A measurement (single
  runs vary by 3–5 ms with thermals); the budget moves per item, never fleet-wide.
- Any sim number that moves (Z2, G2, G7, A8, H1–H4, R1–R3) re-pins the replay fixtures in the same
  PR and says why; `mobility_baseline.rs` rows stay byte-identical under G7.
- Any wire change (G2, G7, H2) is append-only and bumps `PROTOCOL_VERSION`.
- Goldens are blessed deliberately, in the PR that moves them, with the before/after in the message.
- Each vehicle migration (K3) ends with the close-up review under the model-logic bar and the K0
  overlay, not with a triangle count.
