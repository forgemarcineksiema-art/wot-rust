# T-34-85 — reference dossier

> **UNRESEARCHED STUB (2026-08-03).** The T-34-85 was the one playable vehicle with no dossier;
> this file closes that gap honestly — it records that the research has NOT been done and invents
> nothing. Anchor cells stay empty until the "dossier and measure" pass runs (the per-vehicle
> protocol in `docs/vehicle-forge-policy.md`); the pack's own TODO marks the same debt
> (`vehicle_forge/src/packs_t34.rs:33`).

## Identity

Needs dossier. The game models a T-34-85; the precise production year/batch/factory configuration
— and what this model deliberately is NOT — is undecided until researched.

## Reference anatomy (anchor numbers)

Needs dossier — no invented numbers. Every future row must carry a source.

| Dimension | Value | Source | Confidence | Encoded as |
| --- | ---: | --- | --- | --- |
| Hull length (over tracks) | | needs dossier | | `DimensionKind::HullLength` |
| Width (over tracks) | | needs dossier | | `DimensionKind::HullWidth` |
| Height to turret roof | | needs dossier | | `DimensionKind::HeightToTurretRoof` |
| Overall length (gun forward) | | needs dossier | | `DimensionKind::OverallLengthWithGun` |
| Road wheel diameter | | needs dossier | | `DimensionKind::RoadWheelDiameter` |
| Turret plan (L × W) | | needs dossier | | `RatioKind::TurretLengthToWidth` |
| Turret ring position | | needs dossier | | `RatioKind::TurretRingPositionOnHull` |

## Form rules (what makes it *this* tank)

Needs dossier.

## Shape locks

Needs dossier. Today the T-34-85 has NO `DimensionTarget`s (the dimension gate does not measure
it), a `cupola_height` derived from the hitbox apex (0.33 m — and a derived value is shootable
armor), no `glacis_ports`, no `turret_loft` (its turret armor volume is a swept circle, not the
casting), and no `.visual.ron`. These are the fleet-debt items the dossier pass owns
(`docs/vehicle-forge-policy.md`, "Open Fleet Debt").

| Rule | Lock |
| --- | --- |
| (needs dossier) | |

## Sources

Needs dossier. The reference pack today cites only broad photo/technical references
(`vehicle_forge/src/packs_t34.rs`) — none graded, none tied to an anchor.

## Gameplay translation

Needs dossier.

## Known deviations & follow-ups

- The entire dossier is unresearched; the "dossier and measure" pass owns filling this file
  before any T-34-85 shape PR moves the blueprint.
